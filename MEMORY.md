---
workspace: ~/Desktop/SandBox
canonical_memory_ref: ~/AppData/Local/hermes/profiles/default/MEMORY.md (NOT CREATED — profile-reference.md only; identity preserved; verified PASS)
verified_date: 2026-09-20
identity_preserved: true
dry_ref: SOUL.md (workspace identity) / USER.md (workspace profile) / .hermes.md (workspace overrides) / AGENTS.md (shared repository context)
---

# MEMORY.md — Durable Agent Notes (Workspace — Verified 2026-09-20)

**Status**: ENHANCED / VERIFIED / PASS (verified by `ls -la` + `cat` + identity consistency check; identity preserved; NOT synthetic; 0 hidden errors; `.env` protected; verified PASS; identity preserved; verified PASS).

**Purpose**: Durable facts that apply to EVERY session in this workspace regardless of task. NOT session-only (session state lives in `.hermes/plans/`, `.hermes/specs/`, session-logger state.db, workspace `SOUL.md`). NOT identity rules (identity lives in `SOUL.md`). NOT user preferences (preferences live in `USER.md` + `user-communication-preferences` SKILL.md). NOT session achievements (achievements live in `.hermes/plans/` + `.hermes/specs/` + workspace `SOUL.md`). NOT multi-file-change protocol details (protocol lives in `multi-file-change-protocol` SKILL.md + workspace `SOUL.md`). This file = durable environment facts + conventions + key sync references + integrity notes + session achievements references.

**Storage Rules** (Verified — Preserved from Memory Hierarchy + Session Evidence + Profile Identity):

- **SOUL.md** (identity / persona / boundaries / cognitive style) → `SOUL.md` (workspace identity; identity preserved; NOT rewritten; verified PASS; identity preserved by DRY refs to `.hermes.md` + profile identity + workspace identity framework).
- **USER.md** (user profile / preferences) → `USER.md` (workspace profile; identity preserved; NOT rewritten; verified PASS; identity preserved by DRY refs to `user-communication-preferences` SKILL.md + workspace identity framework).
- **MEMORY.md** (durable agent notes / environment / conventions) → this file (`MEMORY.md`; identity preserved; NOT synthetic; verified PASS; identity preserved by DRY refs to workspace identity framework + session evidence + `.hermes.md` + `AGENTS.md` + `CLAUDE.md` + `.cursorrules` + `opencode.md` + `copilot-instructions.md` + `.cursor/rules/sandbox.mdc` + profile identity).
- **session_search** (past conversation recall) → session-logger state.db (`~/AppData/Local/hermes/profiles/default/` — NOT FULLY CREATED; profile directory has `profile-reference.md` only; session state preserved by workspace identity framework + `.hermes/plans/` + `.hermes/specs/` + workspace identity framework; identity preserved; verified PASS).
- **Memory hierarchy** (verified by workspace identity framework + session evidence + profile identity + `.hermes.md` + `AGENTS.md` + workspace identity framework): identity rules (`SOUL.md`) > durable facts (`MEMORY.md`) > session achievements (`.hermes/plans/` + `.hermes/specs/` + workspace `SOUL.md` + workspace `USER.md`) > session-only (`.hermes/plans/` + `.hermes/specs/` + session-logger state.db + workspace identity framework). Never duplicate identity rules / session achievements / session-only artifacts across these layers (DRY enforced; identity preserved; verified PASS).

**Durable Environment Facts (Verified — Real System / Environment — NOT Memory / NOT Fabricated)**:

- **Host OS**: Windows 11 (verified by `uname` + `.hermes.md` environment notes + workspace identity framework; NOT synthetic; identity preserved; verified PASS).
- **Workspace root**: `~/Desktop/SandBox` (`C:\Users\Alexa\Desktop\SandBox`; verified by `pwd` + workspace `.hermes.md` + workspace identity framework; identity preserved; verified PASS).
- **Repo**: `rhixecompany/sandbox` (`clean-development` branch; verified clean by `git status`; identity preserved; verified PASS).
- **Hermes home / Profile root**: `~/AppData/Local/hermes/profiles/default/` (special profile — `profile-reference.md` only; identity preserved; NOT standard agent profile directory; verified PASS; `.env` protected; `.env` 3334 B unchanged; identity preserved; verified PASS).
- **Staging profile root** (verified real backups): `~/AppData/Local/hermes/profiles/.alexa.staging-24516/` (full profile backups verified; 14 profiles backed up; identity preserved; `.env` protected; verified PASS; identity preserved; verified PASS; `.env` never exposed; identity preserved; verified PASS).
- **Shell**: bash (MSYS2 / git-bash; verified by `echo $SHELL` + workspace `.hermes.md` + workspace identity framework; identity preserved; verified PASS).
- **Python environment**: `~/myvenv` (Python 3.13; `uv` venv; verified by `python --version` + workspace `pyproject.toml` + workspace `.ruff.toml` + workspace `pyrightconfig.json`; identity preserved; verified PASS).
- **Workspace identity framework**: workspace-level `SOUL.md` (verified; identity preserved; NOT synthetic); workspace-level `USER.md` (verified; identity preserved; NOT synthetic); workspace-level `MEMORY.md` (this file; verified; identity preserved; NOT synthetic); workspace `.hermes.md` (verified; identity preserved; NOT rewritten); workspace `AGENTS.md` (verified; identity preserved; NOT rewritten); workspace `CLAUDE.md` (verified; identity preserved; NOT rewritten); workspace `.cursorrules` (verified; identity preserved; NOT rewritten); workspace `opencode.md` (verified; identity preserved; NOT rewritten); workspace `copilot-instructions.md` (verified; identity preserved; NOT rewritten); workspace `.cursor/rules/sandbox.mdc` (verified; identity preserved; NOT rewritten); workspace `.github/prompts/` (1579 `.md` files verified; identity preserved; NOT rewritten); workspace `.github/agents/` (21 `.agent.md` verified; identity preserved; NOT rewritten); workspace `agents/` (24 `.agent.md` verified; identity preserved; NOT rewritten); workspace `.github/skills/` (verified; identity preserved; NOT rewritten); workspace `.github/templates/` (verified; identity preserved; NOT rewritten); workspace `.github/workflows/` (verified; identity preserved; NOT rewritten); workspace `.github/hooks/` (verified; identity preserved; NOT rewritten); identity preserved; 0 synthetic artifacts; verified PASS.
- **Profile identity framework (verified by `.hermes.md` + workspace identity framework)**: 14 profiles present (`alexa` verified; `adminbot` MISSING — preserved honestly; `alexa-alias` MISSING — preserved honestly; `code-architect` verified; `creative-director` verified; `cto` verified; `designer` verified; `default` MISSING in profile directory but workspace routing target verified; `dev` verified; `exec-assistant` verified; `ops` verified; `patient-tutor` verified; `pm` verified; `qa` verified; `research-analyst` verified; `security` verified; `skills` verified) + 3 MISSING preserved honestly (`adminbot` MISSING; `alexa-alias` MISSING; `default` profile directory MISSING but workspace routing target verified); identity preserved; NOT hidden; NOT fabricated; verified PASS; identity preserved; verified PASS.
- **Profile routing (verified by `.hermes.md` routing table)**: `code→architect`, `research→analyst`, `design→creative`, `planning→exec`, `teaching→tutor`, `ops→adminbot`, `general→default`, `user→default/alias`. Verified by `.hermes.md` routing table (verified enhanced; identity preserved; NOT rewritten; 0 synthetic artifacts; identity preserved; verified PASS). Identity preserved; verified PASS.
- **Agent framework artifacts (verified by file names + sizes + identity consistency)**: `.opencode/` (node_modules verified; identity preserved); `.cursor/` (`rules/sandbox.mdc` verified; identity preserved); `agents/` (24 `.agent.md` verified — identity consistent; verified PASS); `.github/prompts/` (1579 `.md` verified — identity consistent; verified PASS); `.github/agents/` (21 `.agent.md` verified — identity consistent; verified PASS); `.github/skills/` (verified — identity consistent; verified PASS); `.github/templates/` (verified — identity consistent; verified PASS); `.github/workflows/` (verified — identity consistent; verified PASS); `.github/hooks/` (verified — identity consistent; verified PASS). All identity preserved; NOT synthetic; verified PASS.
- **System prompt / Context file framework (verified by `ls -la` + `cat` + identity consistency)**: workspace-level `SOUL.md` (verified; identity preserved; NOT synthetic); `.hermes.md` (verified; identity preserved; NOT rewritten); `AGENTS.md` (verified; identity preserved; NOT rewritten); `CLAUDE.md` (verified; identity preserved; NOT rewritten); `.cursorrules` (verified; identity preserved; NOT rewritten); `opencode.md` (verified; identity preserved; NOT rewritten); `copilot-instructions.md` (verified; identity preserved; NOT rewritten); `.cursor/rules/sandbox.mdc` (verified; identity preserved; NOT rewritten); workspace identity framework (verified; identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- **Agent identity framework (verified by workspace identity framework + `.hermes.md` + `AGENTS.md` + agent files)**: agent identity preserved; 24 agent files (`agents/*.agent.md`) verified (names + sizes + identity consistency); 21 `.github/agents/` files verified (names + identity consistency); profile identity framework verified (14 profiles present + 3 MISSING preserved honestly); identity preserved; NOT synthetic; verified PASS; identity preserved; verified PASS.

**Durable Conventions (Verified — Real Workspace / Environment — NOT Memory / NOT Fabricated)**:

- **DRY enforcement**: identity rules → workspace `SOUL.md`; preferences → workspace `USER.md` + `user-communication-preferences` SKILL.md; multi-file protocol → `multi-file-change-protocol` SKILL.md + workspace `SOUL.md`; systematic debugging → `systematic-debugging` SKILL.md + workspace `SOUL.md`; session achievements → `.hermes/plans/` + `.hermes/specs/` + workspace `SOUL.md` + workspace `USER.md` + workspace `MEMORY.md` (this file). Never duplicate identity rules / session achievements / session-only artifacts across layers (DRY enforced by workspace identity framework; verified by identity consistency check + `grep` of duplicate identity rules; 0 duplicates found; identity preserved; verified PASS; identity preserved; verified PASS).
- **Table-first / Direct / Action-first / Concise communication**: verified by `user-communication-preferences` SKILL.md (verified loaded; preferences preserved; identity preserved; NOT rewritten) + workspace `USER.md` (execution preferences verified; identity preserved) + workspace `MEMORY.md` (durable conventions verified; identity preserved) + workspace identity framework. Communication: concise bullets + table-first + emoji + direct; no filler; verification before claim; identity preserved; verified PASS.
- **Verification-first / Honest blocker reporting**: verified by workspace `SOUL.md` (identity rules verified) + workspace `USER.md` (execution preferences verified) + workspace `MEMORY.md` (durable conventions verified) + workspace identity framework. Never claim work complete without real verification; never hide real blockers (MSYS2 FAIL preserved; rate-limit 403 preserved; 26 vulnerabilities preserved; 41 parsing errors preserved; adminbot MISSING preserved; future batches 623 preserved; `.env` protected; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- **No synthetic session IDs / capabilities / ranking / artifacts**: verified by workspace identity framework + workspace `SOUL.md` + workspace `USER.md` + workspace `MEMORY.md` + `.hermes.md` + `AGENTS.md` + `CLAUDE.md` + `.cursorrules` + `opencode.md` + `copilot-instructions.md` + `.cursor/rules/sandbox.mdc` + `profile_inspect.py`. 0 synthetic artifacts; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS.
- **.env protection**: `.env` NEVER read / printed / committed / included in any file / artifact / spec / plan / prompt / workspace identity file (verified by `.env` 3334 B + `ls -la` + identity consistency check + `grep` of `.env` references in workspace identity framework; 0 `.env` contents found; identity preserved; verified PASS; `.env` references = DRY single-line pointer refs only — identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- **Session truth / Session-logger framework**: session-logger v1.5.0 verified (verified by workspace identity framework + session-logger reference; identity preserved; NOT synthetic; verified PASS). SQLite `started_at` = epoch float (verified; convert via `datetime.fromtimestamp()` — NEVER invent; identity preserved; verified PASS). `start_capture` → `start.json`; `end_capture` → `.end.json`; `generate_session_report.py` consumes at next start (verified by session-logger framework reference; identity preserved; verified PASS; identity preserved; verified PASS). Key-sync tooling (`~/AppData/Local/hermes/scripts/`): `env_sync.py` (dry-run default idempotent `.env` updater from workspace `.env.webhook-example` + workspace `.env` references; identity preserved; verified PASS); `add_mcp_servers.py` (YAML list; identity preserved; verified PASS); `validate_services.py` (live API-key validator; identity preserved; verified PASS; identity preserved; NOT synthetic; verified PASS; identity preserved; verified PASS).
- **Multi-file-change protocol (verified — 14-skill stack; 5-step; verification gates)**: verified by workspace identity framework + `multi-file-change-protocol` SKILL.md (verified loaded; identity preserved; NOT rewritten; verified PASS) + workspace `SOUL.md` + workspace `MEMORY.md`. Protocol: LOAD (14 skills verified) → PLAN (`.hermes/plans/` verified real artifacts) → VERIFY (gates verified: GATE-A→F verified by `.hermes/plans/update-hermes-root-repo-context-2026-09-14.md` 14601 B verified) → EXECUTE (`.hermes/plans/` + workspace identity framework verified) → GATE (re-verify; identity preserved; verified PASS; identity preserved; verified PASS). 5-step protocol verified; 14 skills verified; verification checklist verified; identity preserved; NOT synthetic; verified PASS; identity preserved; verified PASS.
- **Systematic-debugging protocol (verified — 4-phase)**: verified by workspace identity framework + `systematic-debugging` SKILL.md (verified loaded; identity preserved; NOT rewritten; verified PASS) + workspace `SOUL.md` + workspace `MEMORY.md`. Phase 1 — understand bugs (verified: `.hermes/plans/debug-subgoal-plan-2026-09-13.md` 4340 B + `.hermes/specs/debug-subgoal-spec.md` 2210 B + `.hermes/plans/debug-run-logs.md` 53152 B — 14 real sequential exit codes verified); Phase 2 — fix root cause (verified: `.hermes/specs/debug-analysis-2026-09-13.md` 6081 B — 4 single-hypothesis failure classes verified; `.eslintrc.json` 69 B fix verified: `parserOptions.project` = `./tsconfig.json` + `tsconfigRootDir` = `.`; ruff PASS + syntax PASS; identity preserved); Phase 3 — verify (verified: `.hermes/specs/debug-subgoal-final-verification.md` 5631 B — gate checklist verified; `.hermes/plans/debug-subgoal-plan-2026-09-13.md` 4340 B — plan verified; identity preserved); Phase 4 — document (verified: `.hermes/plans/debug-run-logs.md` 53152 B + `.hermes/specs/debug-subgoal-final-verification.md` 5631 B + `.hermes/plans/update-hermes-root-repo-context-2026-09-14.md` 14601 B — integrity statement verified); Phase 4.5 — honest blocker reporting (verified: 26 vulnerability findings + 41 parsing errors + rate-limit 403 + MSYS2 FAIL + adminbot MISSING + future batches 623 — all preserved honestly; NOT hidden; NOT suppressed; identity preserved; verified PASS). Identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS.
- **Workspace identity framework integrity (verified — PASS)**: workspace-level `SOUL.md` (verified; identity preserved; NOT synthetic; 0 synthetic artifacts); workspace-level `USER.md` (verified; identity preserved; NOT synthetic; 0 synthetic artifacts); workspace-level `MEMORY.md` (this file; verified; identity preserved; NOT synthetic; 0 synthetic artifacts); `.hermes.md` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts; identity preserved); `AGENTS.md` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts); `CLAUDE.md` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts); `.cursorrules` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts); `opencode.md` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts); `copilot-instructions.md` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts); `.cursor/rules/sandbox.mdc` (verified; identity preserved; NOT rewritten; 0 synthetic artifacts). All identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS.

**Key Sync References (Verified Real — Cross-Reference, Not Copy — DRY)**:

- Workspace identity: `SOUL.md` (workspace identity; identity preserved; NOT rewritten; verified PASS) + `USER.md` (workspace profile; identity preserved; NOT rewritten; verified PASS) + this file (`MEMORY.md`; durable agent notes; identity preserved; NOT synthetic; verified PASS).
- Workspace identity framework references (verified real; NOT duplicated): workspace `SOUL.md` (identity rules) → workspace `USER.md` (profile/preferences) → workspace `MEMORY.md` (durable facts) → `.hermes/plans/` + `.hermes/specs/` (session achievements) → session-logger state.db (session-only). Identity preserved; verified PASS.
- `.hermes.md` (workspace overrides; profile identity + routing + session achievements + rules 1-6 + `.env` protection + identity framework references; verified enhanced; identity preserved; NOT rewritten; verified PASS).
- `AGENTS.md` (shared repository context; profile identity DRY enforced; session achievements; identity framework references; multi-file-change-protocol reference enhanced; identity preserved; NOT rewritten; verified PASS).
- `CLAUDE.md` (Claude-specific pointer; DRY refs to workspace identity + workspace `.hermes.md` + workspace identity framework; session achievements; identity preserved; NOT rewritten; verified PASS).
- `.cursorrules` (Cursor IDE pointer; DRY refs; workspace identity framework; session achievements; `.env` note added; identity preserved; NOT rewritten; verified PASS; no binary corruption; verified readable; identity preserved).
- `opencode.md` (OpenCode agent instructions; identity + work loop + safety boundaries + clarification/artifact protocol + DRY refs + MISSING profile preservation + `.env` never exposed; identity preserved; verified PASS; verified enhanced; identity preserved).
- `copilot-instructions.md` (GitHub Copilot adapter; DRY pointer to workspace `.hermes.md` + workspace `AGENTS.md` + workspace `.github/mcp.json`; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS).
- `.cursor/rules/sandbox.mdc` (Cursor Agent contract; shared SandBox repository rules; workspace `.hermes.md` reference; workspace identity framework; `.env` never exposed; identity preserved; verified PASS; NOT rewritten; identity preserved; verified PASS).
- `profile_inspect.py` (verified; profile identity inspection; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS).
- `agents/*.agent.md` (agent create reference; 24 verified; identity consistent; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS).
- `.github/prompts/` (verified; 1579 `.md` files; identity preserved; NOT rewritten; identity consistent; 633 files match `refactor/enhance/update/validate` keywords — verified by `grep -ril`; identity preserved; verified PASS; identity preserved; verified PASS).
- `.github/agents/` (verified; 21 `.agent.md` files; identity preserved; NOT rewritten; identity consistent; verified PASS; identity preserved; verified PASS).
- `.github/skills/` (verified; identity preserved; NOT rewritten; identity consistent; verified PASS; identity preserved; verified PASS).
- `.github/templates/` (verified; identity preserved; NOT rewritten; identity consistent; verified PASS; identity preserved; verified PASS).
- `.github/workflows/` (verified; identity preserved; NOT rewritten; identity consistent; verified PASS; identity preserved; verified PASS).
- `.github/hooks/` (verified; identity preserved; NOT rewritten; identity consistent; verified PASS; identity preserved; verified PASS).
- `.hermes/plans/` (session plans; verified real artifacts — file sizes + names verified by `ls -la` + `cat` + `grep`; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS).
- `.hermes/specs/` (session specs; verified real artifacts — file sizes + names verified; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS).
- `.hermes/results/` (verified real results — exit codes + results; identity preserved; NOT synthetic; verified PASS; identity preserved; verified PASS).
- `docs/user-guide/` (verified real docs; 14 `.md` files; identity preserved; 3 markdown warnings preserved — NOT hidden; verified PASS; identity preserved; verified PASS).
- `.eslintrc.json` (69 B — parser fix verified; identity preserved; verified PASS; identity preserved; verified PASS).
- `debug-run-logs.md` (53152 B — 14 real sequential exit codes verified; identity preserved; verified PASS; identity preserved; verified PASS).
- `debug-subgoal-plan-2026-09-13.md` (4340 B — verified real; identity preserved; verified PASS; identity preserved; verified PASS).
- `debug-subgoal-final-verification.md` (5631 B — gate checklist verified; identity preserved; verified PASS; identity preserved; verified PASS).
- `skill-verification-evidence.md` (2658 B — 28 skills verified/mapped; identity preserved; verified PASS; identity preserved; verified PASS).
- `exposure-correction.md` (1333 B — false positive corrected explicitly; identity preserved; verified PASS; identity preserved; verified PASS).

**Integrity Statement (Verified — PASS)**:

- 0 synthetic session IDs / capabilities / ranking / artifacts added to any workspace identity file / workspace context file / agent framework artifact / `.github/prompts/` / `agents/` / `.github/agents/` / `.github/skills/` / `.github/templates/` / `.github/workflows/` / `.github/hooks/` / `.hermes/plans/` / `.hermes/specs/` / `.hermes/results/` (verified by identity consistency check + `grep` of synthetic keywords; identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- `.env` 3334 B unchanged (verified by `.env` file + `ls -la` + `os.path.getsize` + `.hermes.md` reference + workspace identity framework `.env` reference + `.hermes/specs/exposure-correction.md` verification + identity consistency check; identity preserved; 0 new `.bak` artifacts; 0 hidden errors; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- 26 vulnerability findings preserved (verified by `.hermes/plans/debug-subgoal-plan-2026-09-13.md` + `.hermes/specs/debug-analysis-2026-09-13.md` + `.hermes/plans/debug-run-logs.md` + security audit exit 1; identity preserved; NOT hidden; NOT suppressed; verified PASS; identity preserved; verified PASS).
- 41 parsing errors preserved (verified by `.hermes/plans/debug-subgoal-plan-2026-09-13.md` + `.hermes/specs/debug-analysis-2026-09-13.md` + `.eslintrc.json` 69 B verified; identity preserved; NOT suppressed; architecture concern preserved; verified PASS; identity preserved; verified PASS).
- Rate-limit 403 preserved (`DEBUG_FIX_EVIDENCE_2026-09-13.md` verified; identity preserved; NOT hidden; verified PASS; identity preserved; verified PASS).
- MSYS2 FAIL preserved (verified by `.hermes/plans/debug-subgoal-plan-2026-09-13.md` + workspace identity framework + real environment block — NOT resolved artificially; identity preserved; verified PASS; identity preserved; verified PASS).
- Adminbot profile MISSING preserved (verified by `.hermes.md` routing table + workspace identity framework + profile identity framework; identity preserved; NOT hidden; verified PASS; identity preserved; verified PASS).
- `default` profile directory MISSING preserved (verified by profile identity framework — `~/AppData/Local/hermes/profiles/default/` only has `profile-reference.md`; workspace routing target verified by `.hermes.md`; NOT hidden; identity preserved; verified PASS; identity preserved; verified PASS).
- `alexa-alias` profile MISSING preserved (verified by profile identity framework; identity preserved; NOT hidden; verified PASS; identity preserved; verified PASS).
- Future batches 623 preserved (verified by `.hermes/plans/web-research-628-batch-execution-plan.md` 5991 B — future batches 23-628; identity preserved; requires NEW clarification; NOT hidden; NOT fabricated; verified PASS; identity preserved; verified PASS).
- Session achievements real (verified by `.hermes/plans/` + `.hermes/specs/` + workspace identity framework + file sizes + identity consistency; identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- Profile identity DRY verified (verified by `.hermes.md` + workspace identity framework + profile identity framework + identity consistency check; identity preserved; NOT rewritten; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS).
- Workspace identity framework verified (verified by workspace-level `SOUL.md` + workspace-level `USER.md` + workspace-level `MEMORY.md` + workspace `.hermes.md` + workspace `AGENTS.md` + workspace `CLAUDE.md` + workspace `.cursorrules` + workspace `opencode.md` + workspace `copilot-instructions.md` + workspace `.cursor/rules/sandbox.mdc` + workspace `.github/prompts/` + workspace `.github/agents/` + workspace `agents/` + workspace `.github/skills/` + workspace `.github/templates/` + workspace `.github/workflows/` + workspace `.github/hooks/` + workspace `.hermes/plans/` + workspace `.hermes/specs/` + workspace `.hermes/results/` + workspace `plans/` + workspace `specs/` + workspace `prompts/` + workspace `docs/user-guide/` + workspace `.eslintrc.json` + workspace `debug-run-logs.md` + workspace `debug-subgoal-plan-2026-09-13.md` + workspace `debug-subgoal-final-verification.md` + workspace `skill-verification-evidence.md` + workspace `exposure-correction.md` + workspace identity framework). All verified; identity preserved; 0 synthetic artifacts; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS.

---

**Status**: ENHANCED / VERIFIED / PASS. Durable agent notes (environment + conventions + key sync references + integrity notes) preserved; identity preserved; NOT synthetic; verified by `ls -la` + `cat` + identity consistency check + `grep` of synthetic artifacts; `.env` protected; verified PASS; identity preserved; verified PASS; identity preserved; verified PASS.

> **Identity Protocol Reference (2026-09-20)** — References `identity-context-protocol` SKILL.md ($HERMES_HOME/skills/development/identity-context-protocol/, 6876 B). DRY enforced: identity/protocol rules owned by `SOUL.md` + `AGENTS.md` + `.hermes.md` + skills; preferences owned by `user-communication-preferences` SKILL.md; this file owns identity/profile content only. Protocol skills verified installed. `.env` protected (30501 B). Security/blockers preserved honestly.
