# Python-projects
**Python**: PEP 8, `snake_case`, type annotations, `ruff`, `mypy`.
**Structure**: standalone single-file scripts; `if __name__ == "__main__":`; stdlib → third-party imports; module docstring.
**Security**: no embedded secrets; validate HTTP responses; prefer local image processing.
