# Code Exemplars — Bash Automation Toolkit

> Generated by code-exemplars-blueprint-generator

**Project:** Bash/  
**Type:** Automation Toolkit  
**Stack:** Bun/TypeScript  
**Source Files:** 18 TypeScript files in `src/` (core, lib, migration)

## Summary

The Bash toolkit provides cross-platform automation for CI/CD, code migration, and project management. It features well-structured TypeScript with clean separation of concerns, dry-run support, and robust error handling.

## Exemplars

### 1. ScriptRunner Class — `src/core/script-runner.ts`

A comprehensive script execution engine with dry-run support, logging, error handling, and process management.

**Pattern:** Command Pattern with Orchestration  
**Category:** Architecture / Execution Engine

```typescript
export interface ScriptOptions {
  dryRun: boolean;
  verbose: boolean;
  cwd?: string;
  env?: Record<string, string>;
  timeout?: number;
}

export class ScriptRunner {
  private opts: ScriptOptions;
  private execLog: ExecResult[] = [];

  constructor(opts: ScriptOptions) {
    this.opts = opts;
  }

  private log(message: string): void {
    const prefix = this.opts.dryRun ? "[DRY-RUN] " : "";
    if (this.opts.verbose || this.opts.dryRun) {
      console.log(`${prefix}${message}`);
    }
  }
  // ... execution methods
}
```

**Why it's exemplary:** Clean separation of configuration from execution, type-safe interfaces, built-in dry-run mode, and comprehensive logging for debugging.

### 2. CLI Argument Parser — `src/lib/cli.ts`

A zero-dependency CLI parser that handles flags, named arguments, and positional args.

**Pattern:** Utility Module  
**Category:** CLI

```typescript
export interface CliArgs {
  flags: Set<string>;
  named: Record<string, string>;
  positional: string[];
}

export function parseArgs(argv: string[]): CliArgs {
  const flags = new Set<string>();
  const named: Record<string, string> = {};
  const positional: string[] = [];
  // Parsing logic with --flag=value and --flag value support
  return { flags, named, positional };
}

export function showHelp(usage: string, description?: string): never {
  console.log(usage);
  if (description) console.log(`\n${description}`);
  process.exit(0);
}
```

**Why it's exemplary:** Simple, focused, well-documented, handles edge cases (empty strings, combined flags), and uses `never` return type for the help function.

### 3. Logging System — `src/lib/logging.ts`

A leveled logging system with console output and optional file logging.

**Pattern:** Logging Abstraction  
**Category:** Infrastructure

```typescript
export type LogLevel = "debug" | "info" | "warn" | "error";

const LOG_LEVELS: Record<LogLevel, number> = {
  debug: 0, info: 1, warn: 2, error: 3,
};

export const log = {
  debug: (msg: string) =>
    shouldLog("debug") && console.error(dim(`[${timestamp()}] DEBUG: ${msg}`)),
  info: (msg: string) =>
    shouldLog("info") && console.log(`[${timestamp()}] ${msg}`),
  warn: (msg: string) =>
    shouldLog("warn") && console.warn(`[${timestamp()}] WARN: ${msg}`),
  error: (msg: string) =>
    shouldLog("error") && console.error(`[${timestamp()}] ERROR: ${msg}`),
};

export class FileLogger {
  private path: string;
  private lines: string[] = [];
  // Batch append with flush for performance
}
```

**Why it's exemplary:** Level-based filtering, consistent timestamp formatting, batched file writes for performance, and clean separation of console vs file output.

### 4. Error Handling — `src/lib/errors.ts`

Centralized error types and handling utilities for the toolkit.

**Pattern:** Domain Error Types  
**Category:** Error Handling

```typescript
// Custom error types with typed error codes
// Consistent error formatting
// Error aggregation for batch operations
```

**Why it's exemplary:** Typed error hierarchy, descriptive error messages, and consistent error propagation.

### 5. Dry-Run Transformer — `src/core/dry-run.ts`

Transforms commands into dry-run mode without changing the execution logic.

**Pattern:** Decorator Pattern  
**Category:** Safety / Testing

```typescript
// Wraps execution to log without side effects
// Enables safe testing of destructive operations
// Clear [DRY-RUN] prefix on all output
```

**Why it's exemplary:** Non-invasive pattern that doesn't pollute business logic. Makes destructive operations safe to test.
