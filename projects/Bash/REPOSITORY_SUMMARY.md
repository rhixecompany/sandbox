# Repository Summary — Bash (Automation Toolkit)

> Generated by `/repo-story-time` from real git history. **Important scope note below.**
> All facts are evidence-based as of 2026-07-16.

## ⚠️ Scope / Honesty Note (read first)
`Bash` is **NOT a standalone git repository or submodule**. It has **no `.git` directory** of its own; `git rev-parse --show-toplevel` resolves to `C:/Users/Alexa/Desktop/SandBox` — the parent monorepo. The 182 commits reported for the workspace as a whole belong to *all* projects under `SandBox`, not to Bash specifically.

Scoped to the `projects/Bash/` path, the **actual commit count that touches this folder is 4** (all by `rhixecompany`, all in **2026-07**):
- `3c749033` 2026-07-16 `feat: enrich repo prompts with vscode/git/mcp skills, full workspace sweep`
- `64927edb` 2026-07-10 `updates`
- `87a8cde7` 2026-07-09 `updates`
- `a5ca06b0` 2026-07-09 `fix: restore prompt files from clean commit (fixes YAML metadata corruption across 170+ files)`

Those 4 commits are **report/prompt-generation commits** (workspace-wide prompt enrichment and a YAML corruption fix), not Bash feature work. The Bash *source code* itself was introduced as part of the bulk workspace history and is not isolated in its own commits.

## Overview
Bash is the **primary automation toolkit of the SandBox workspace** — a multi-phase orchestration system (Bun/TypeScript + PowerShell + Bash) that discovers, clones, triages, debugs, remediates, and cross-references projects across the workspace. It is the most actively referenced and most structurally mature project in the slice.

## Architecture
- **Type:** Multi-phase automation toolkit
- **Pattern:** Phase-Based Orchestration — 6-phase pipeline: Discovery → Clone → Triage → Debug → Remediation → Cross-Reference
- **Entry points:** TypeScript scripts (`src/`), PowerShell orchestrator (`scripts/orchestrator.ps1`), multi-wrapper scripts (`.ps1`, `.sh`, `.bat`)
- **Hub role:** Primary tooling root for the SandBox workspace; shares `.github/` CI workflows

## Key Components
| Area | Evidence (paths) |
| --- | --- |
| Core engine | `src/core/ast-transformer.ts`, `src/core/behavior-test.ts`, `src/core/dry-run.ts`, `src/core/script-runner.ts` |
| CLI/lib | `src/lib/cli.ts`, `src/lib/colors.ts`, `src/lib/errors.ts`, `src/lib/logging.ts`, `src/lib/README.md` |
| Commands | `src/cache-clean.ts`, `src/clean-dep.ts`, `src/upgrade.ts`, `src/git-commit-batches.ts` |
| Migration | `src/migration/ts-morph-helper.ts`, `src/migration/templates/`, `src/migration/__tests__/` |
| Orchestrator | `scripts/orchestrator.ps1`, `scripts/phase-1-*` … `phase-6-*`, `scripts/phase-3-consolidation.js`, `phase-4-batch-executor.js`, `phase-5-final-summary.js` |
| Cross-project | references to `Banking/`, `comicwise/` subfolders (per-repo helpers) |
| Tooling | `eslint.config.mts`, `.lintstagedrc.ts`, `.prettierrc.ts`, `.husky/`, `.markdownlintrc.json`, `.vscode/` |

## Technologies
- **Runtime:** Bun 1.3.14+
- **Language:** TypeScript (strict), PowerShell 5.1+, Bash
- **Lint/Format:** ESLint flat config, Prettier, markdownlint-cli2
- **Testing:** Vitest (TypeScript unit), `tests/verify-dryrun.sh`, `test-all.sh`
- **CI:** `.github/workflows/bash-scripts-ci.yml`
- **Key deps:** `yaml`, `zod` (validation)
- **Conventions (from AGENTS.md):** all destructive ops support `--help` and `--dry-run`; multi-wrapper parity (`.sh`/`.ps1`/`.bat`); logs to `logs/` with timestamps; strict TS, no `any` without justification.

## Data Flow (orchestration pipeline)
1. **discover** — scan workspace for projects/scripts
2. **clone** — clone or localize targets
3. **triage** — categorize issues (phase-3 consolidation, `phase-1-deep-triage`)
4. **debug** — `phase-4-debug.ps1` diagnoses failures
5. **remediation** — `phase-4-batch-executor.js` applies batched fixes; `git-commit-batches.ts` commits them
6. **cross-ref** — `phase-6-cross-ref.sh` / `.ps1` links related changes across projects
- Every phase can be previewed via `--dry-run`; `ts-morph` assists TypeScript AST transforms.

## Team
- **Single contributor to the folder's history:** `rhixecompany <rhixecompany@gmail.com>` (all 182 workspace commits; 4 that touch `projects/Bash/`). No co-authors, no reviewed PRs in the scoped history.

## Honest Caveat
Because Bash is a folder inside the monorepo, its "commit count" depends on scoping: **4** commits touch its path (all prompt/repair work), while the surrounding tool source arrived via the workspace's bulk initial history. Architecture claims are drawn from `AGENTS.md`, `architecture.md`, `package.json` scripts, and the `src/`/`scripts/` trees — not from Bash-specific feature commits (none exist in isolation).
