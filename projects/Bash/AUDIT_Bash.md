# Audit Report: Bash

> Read-only repo-management audit (Phases 0, 2, 3). Destructive phases (1: branch deletion/push, 4: CI creation) HELD for user approval. Generated by focused subagent.

## Overview
- **Type:** Multi-phase automation toolkit (Bun/TypeScript + PowerShell + Bash). 6-phase pipeline: Discovery → Clone → Triage → Debug → Remediation → Cross-Reference. Primary tooling root for the SandBox workspace.
- **Stack:** Bun 1.3.14+ (packageManager pinned), TypeScript strict, PowerShell 5.1+, Bash. ESLint flat config, Vitest, Husky, lint-staged, markdownlint, cspell.
- **AGENTS.md present:** Yes (41 lines, accurate).
- **README.md present:** Yes (plus many OPERATIONS/ORCHESTRATOR docs).

## Disk Usage
- **Source (excl. .git, node_modules, dist, build):** 2.2M (328 git-tracked files).
- **node_modules:** none installed locally (bun.lock present; `bun install --frozen-lockfile` per AGENTS.md).
- Note: large `bun.lock` (277KB) and many `*.ps1` orchestrator scripts.

## Entrypoint
- **Primary TS scripts:** `src/` (`cache-clean.ts`, `clean-dep.ts`, `git-commit-batches.ts`, `upgrade.ts`, `core/`, `lib/`, `migration/`).
- **Orchestrator:** `orchestrator-unified.ps1` (modes: discover/clone/triage/debug/remediation/cross-ref) + `.sh`/`.bat` parity wrappers.
- **Run:** `bun install --frozen-lockfile`, `bun run format|typecheck|lint:strict`, `bun test` (vitest), `powershell -File orchestrator-unified.ps1 -Mode discover`.

## Gitignore Audit
File exists (166 bytes). Bun/TS-oriented:
- ✅ `node_modules/`, ✅ `dist/`, ✅ `build/`, ✅ `*.pyc`, ✅ `__pycache__/`, ✅ `.DS_Store`, ✅ `Thumbs.db`, ✅ `.idea/`, ✅ `.ruff_cache/`, ✅ `*.log`, ✅ `Bash/logs/`.
- ❌ **Missing `.env`** — **significant**: this is a dotenv/dotenv-safe project (`dotenv`, `dotenv-safe` in deps; `Bash/scripts/phase-5-verify-install.sh` likely loads `.env`). No `.env*` ignore rule → risk of committing secrets.
- ⚠️ `venv/` not listed — minor (Python usage limited; `__pycache__`/`*.pyc` covered).
- ⚠️ `.next/` N/A.
- **Verdict:** ⚠️ **Gap: `.env` not ignored** despite the project clearly using environment files. Add `.env*` immediately.

## Dependency Audit
- **Manifest:** `package.json` (present) + **`bun.lock` (present, 277KB)** → reproducible. `bun pm ls` succeeded (1088 installed packages).
- **Key deps:** zod@^4.4.3, yaml@^2.9.0; dev: eslint@10.4.0, typescript-eslint@8.59.4, vitest@4.1.7, prettier@3.8.3, husky@9.1.7, cspell, markdownlint-cli2, sharp@0.34.5 (trusted), tsx, globby, js-yaml, ajv.
- **bun audit availability:** `bun` v1.3.14 installed; `bun audit` not run (read-only). Lockfile present so `bun audit` is viable.
- **Flags / known-bad:**
  - ℹ️ `@types/node@latest` and `typescript@latest` use the **`latest` dist-tag** instead of a pinned range → non-deterministic dev installs (mitigated by committed bun.lock, but `bun update` could shift them).
  - ℹ️ Otherwise deps are current/modern (ESLint 10, Vitest 4) — healthy.
- **Recommendation:** Add `.env*` to `.gitignore`; consider pinning `@types/node`/`typescript` to explicit ranges; run `bun audit`.

## Branch State
- **Local branches:** `* development` (current), `master`, `production`.
- **Remote branches:** `origin/HEAD -> origin/master`, `origin/development`, `origin/master`, `origin/production`.
- ⚠️ **`master` branch exists (local AND remote)** — non-standard branch; **also `origin/HEAD` points to `master`** (unlike the other 5 projects which point to `production`). This repo treats `master` as its primary, conflicting with the dev/prod convention used elsewhere.
- **origin/HEAD** → `master` (inconsistent with siblings).

## Destructive Phases HELD
- ⛔ **Phase 1 (branch deletion / push):** NOT executed. `master` NOT deleted, no pushes.
- ⛔ **Phase 4 (create CI):** NOT executed (note: a `.github/workflows/bash-scripts-ci.yml` is already referenced in AGENTS.md — pre-existing, not created by this audit).
- Read-only except this AUDIT report.
