# Audit Report: Resume_maker

> Read-only repo-management audit (Phases 0, 2, 3). Destructive phases (1: branch deletion/push, 4: CI creation) HELD for user approval. Generated by focused subagent.

## Overview
- **Type:** CLI document generator (JSON → Markdown + PDF resume/cover letter/LinkedIn guide/interview prep).
- **Stack:** Bun-first TypeScript (strict). Single runtime dep `markdown-pdf`. ESLint + prettier + markdownlint + cspell. Pipeline: parse → validate → normalize → generate → convert.
- **AGENTS.md present:** Yes (42 lines, accurate).
- **README.md present:** Yes (+ README.html, folder-structure).

## Disk Usage
- **Source (excl. .git, node_modules, dist, build):** 665K (43 git-tracked files).
- **node_modules:** 111M (present, correctly ignored).
- `bun.lock` present (83KB). `output/` dir present (generated docs, ignored via `output/` rule).

## Entrypoint
- **CLI:** `index.ts` (module entry). Run: `bun index.ts --input sample-input.json` / `bun index.ts -i alexander-input.json -o resume -f both`.
- **Scripts:** `bun run build|start|typecheck|lint|lint:fix|lint:md|lint:spell`.

## Gitignore Audit
File exists (396 bytes). Bun/TS-oriented:
- ✅ `node_modules`, ✅ `dist`, ✅ `out`, ✅ `output/`, ✅ `.env` (+ `.env.local/.test.local/.production.local/.env.development.local`), ✅ `.eslintcache`, ✅ `.cache`, ✅ `*.tsbuildinfo`, ✅ `.idea`, ✅ `.DS_Store`, ✅ `*.tgz`, ✅ `coverage`/`*.lcov`.
- ⚠️ `*.pyc` / `__pycache__/` / `venv/` not listed — **N/A** (no Python).
- ⚠️ `/build` not listed — N/A (uses `dist`/`out`/`output`).
- ⚠️ `.next/` N/A.
- **Verdict:** ✅ Adequate for a Bun/TS CLI. `.env*` properly ignored.

## Dependency Audit
- **Manifest:** `package.json` (present) + **`bun.lock` (present)** → reproducible. `bun pm ls` succeeded (381 installed packages).
- **Runtime deps:** `markdown-pdf@^11.0.0` (single). **Dev deps:** @types/bun, @typescript-eslint/*, cspell, eslint@10.3.0, eslint-config-prettier, eslint-plugin-prettier, markdownlint-cli2, prettier@3.8.3.
- **bun audit availability:** `bun` v1.3.14 installed; `bun audit` not run (read-only). Lockfile present → viable.
- **Flags / known-bad:**
  - ℹ️ `markdown-pdf@^11` — wraps an old `pdfkit`/`electron`-era stack; known to be finicky on newer Node/Bun and occasionally pulls transitive native deps. Works but monitor for breakage. Single point of failure for PDF output.
  - ℹ️ Otherwise minimal, modern, healthy dependency surface.
- **Recommendation:** Run `bun audit`; consider an alternative PDF path (e.g. `@resume-json`/puppeteer or `md-to-pdf`) if `markdown-pdf` breaks on Bun upgrades.

## Branch State
- **Local branches:** `* development` (current), `master`, `production`.
- **Remote branches:** `origin/HEAD -> origin/master`, `origin/development`, `origin/master`, `origin/production`.
- ⚠️ **`master` branch exists (local AND remote)** — non-standard branch; **`origin/HEAD` points to `master`** (like Bash, unlike the other 4 projects which point to `production`).
- **origin/HEAD** → `master` (inconsistent with the dev/prod convention).

## Destructive Phases HELD
- ⛔ **Phase 1 (branch deletion / push):** NOT executed. `master` NOT deleted, no pushes.
- ⛔ **Phase 4 (create CI):** NOT executed.
- Read-only except this AUDIT report.
