# Workflow Analysis — Resume_maker (Job Docs Generator)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Resume_maker
**Type:** CLI Document Generator
**Stack:** Bun / TypeScript
**Architecture Pattern:** Pipeline Processing
**Entry Point:** `index.ts`

A CLI tool that generates job-hunting documents (resume, cover letter, LinkedIn guide, interview prep) from structured JSON input to Markdown and PDF formats.

## Workflows

### Workflow 1: Resume Generation (JSON → Markdown → PDF)

**Description:** End-to-end generation of a resume document from structured JSON input data.

**Entry Points:**
- `bun index.ts --input <file.json>`
- `bun index.ts -i alexander-input.json -o resume -f both`

**Steps:**
1. **CLI Parsing**: `parseArgs` parses `--input`, `--output`, `--format` flags
2. **File Loading**: Reads JSON input file from filesystem
3. **Data Validation**: `validateResumeData()` validates name, title, summary, experience, education, skills, contact info
4. **Data Processing**: `processUserInput()` normalizes and trims input data
5. **Resume Generation**: `generateResumeMarkdown()` constructs Markdown string using formatters:
   - `formatContactSection()` — email, phone, LinkedIn, GitHub, website
   - `formatSummarySection()` — professional summary
   - `formatExperienceSection()` — job history with highlights
   - `formatProjectsSection()` — curated project descriptions
   - `formatEducationSection()` — degrees and coursework
   - `formatSkillsSection()` — bulleted skill list
6. **Markdown Output**: Writes `.md` file to `output/` directory
7. **PDF Conversion**: Converts Markdown to PDF via `markdown-pdf`
8. **Summary**: Reports generated output paths

**Sequence Diagram:**
```
User                    index.ts                    Filesystem              markdown-pdf
  |                        |                           |                       |
  |-- bun index.ts ------->|                           |                       |
  |   --input data.json    |                           |                       |
  |                        |                           |                       |
  |                        |-- read JSON input ------->|                       |
  |                        |<-- raw resume data -------|                       |
  |                        |                           |                       |
  |                        |-- validateResumeData()    |                       |
  |                        |   (name, title, exp, ed)  |                       |
  |                        |                           |                       |
  |                        |-- processUserInput()      |                       |
  |                        |   (normalize + trim)      |                       |
  |                        |                           |                       |
  |                        |-- generateResumeMarkdown()|                       |
  |                        |   formatContact/Summary/  |                       |
  |                        |   Experience/Projects/    |                       |
  |                        |   Education/Skills        |                       |
  |                        |                           |                       |
  |                        |-- write Markdown file --->|                       |
  |                        |                           |                       |
  |                        |-- convert to PDF -------->|                       |
  |                        |<------ PDF generated -----|                       |
  |                        |                           |                       |
  |<-- output paths -------|                           |                       |
```

**Error Handling:**
- Validation failure → throw descriptive error listing missing fields
- Missing input file → catch file read error, display usage help
- PDF generation failure → report error, Markdown file still saved

**Test Patterns:**
- `validateResumeData()` unit tests for each validation rule
- `generateResumeMarkdown()` snapshot tests for Markdown output
- `scripts/smoke-resume.ts` — smoke test for end-to-end generation
- TypeScript strict typing catches data shape mismatches

---

### Workflow 2: Multi-Document Generation

**Description:** Generating multiple document types (resume, cover letter, LinkedIn guide, interview prep) from a single JSON input.

**Entry Points:**
- `bun index.ts --input sample-input.json -f both`

**Steps:**
1. Loads JSON input data
2. Validates core data (name, contact, experience, education)
3. Generates **Resume** Markdown (main output)
4. Generates **Cover Letter** from experience highlights
5. Generates **LinkedIn Guide** from skills and summary
6. Generates **Interview Prep Q&A** from project descriptions
7. Converts each to Markdown and optionally PDF
8. Saves all documents to `output/` directory

**Error Handling:**
- Per-document generation is isolated; one failure doesn't block others
- Missing optional data (e.g., projects) → skip that document type gracefully

**Test Patterns:**
- E2E test with `sample-input.json`
- Verify all output files exist in `output/`
