# 🏗 Technology Stack Blueprint - mcp-servers/python

**Project Path:** `projects/mcp-servers/python`
**Generated:** 2026-07-28
**Status:** Active — Python MCP Server Reference Implementation

---

## Core Technologies

| Category | Technology | Version | License |
|----------|-----------|---------|---------|
| **Language** | Python | >=3.11 | PSF |
| **MCP SDK** | mcp[cli] | >=1.28.0 | MIT |
| **Build System** | hatchling | Latest | MIT |
| **Package Manager** | uv / pip | Latest | MIT |

---

## Architecture

**Pattern:** MCP (Model Context Protocol) Server

- **Transport:** STDIO
- **Tools:** Greeting tool, File search tool
- **Resources:** File system access

### Project Structure

```
mcp-servers/python/
├── main.py                 # Entry point
├── pyproject.toml          # Project config (PEP 621)
├── uv.lock                 # Locked dependencies
├── README.md
└── __pycache__/
```

---

## Dependencies (`pyproject.toml`)

### Production

```toml
dependencies = [
    "mcp[cli]>=1.28.0",
]
```

### Build System

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

### Entry Point

```toml
[project.scripts]
python-mcp-server = "main:main"
```

---

## MCP Server Implementation (`main.py`)

```python
#!/usr/bin/env python
"""Python MCP Server with greeting and file search tools."""

from mcp.server import Server
from mcp.types import Tool, TextContent
import os
import json

app = Server("python-mcp-server")

@app.list_tools()
async def list_tools() -> list[Tool]:
    return [
        Tool(
            name="greet",
            description="Greet a person by name",
            inputSchema={
                "type": "object",
                "properties": {"name": {"type": "string"}},
                "required": ["name"],
            },
        ),
        Tool(
            name="search_files",
            description="Search for files in a directory",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {"type": "string"},
                    "pattern": {"type": "string"},
                },
                "required": ["path"],
            },
        ),
    ]

@app.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    if name == "greet":
        return [TextContent(type="text", text=f"Hello, {arguments['name']}!")]
    elif name == "search_files":
        path = arguments["path"]
        pattern = arguments.get("pattern", "*")
        # Implementation...
        return [TextContent(type="text", text=json.dumps(results))]
    raise ValueError(f"Unknown tool: {name}")

async def main():
    from mcp.server.stdio import stdio_server
    async with stdio_server() as (read, write):
        await app.run(read, write, app.create_initialization_options())

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

---

## Commands

```bash
# Install dependencies
uv sync
# or: pip install -e .

# Run server
python -m main
# or: python-mcp-server

# Development
uv run python main.py

# Type check (if mypy configured)
mypy main.py

# Lint (if ruff configured)
ruff check main.py
```

---

## Quality Tools

| Tool | Purpose | Config |
|------|---------|--------|
| **mypy** | Type checking | `pyproject.toml` |
| **ruff** | Linting/formatting | `pyproject.toml` |
| **pytest** | Testing | `pyproject.toml` |

---

## License

MIT

---

*Generated by Hermes Agent Technology Stack Blueprint Generator*
