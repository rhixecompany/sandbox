# Code Exemplars — Rhixecompany Comics

> Generated by code-exemplars-blueprint-generator

**Project:** projects/rhixecompany-comics/  
**Type:** Full-Stack Comic Platform  
**Stack:** Django REST + Next.js + PostgreSQL  
**Source Files:** Extensive Python (Django backend), TypeScript (Next.js frontend with shadcn/ui)

## Summary

A comprehensive comic reading platform with Django backend (DRF, Celery, Scrapy) and Next.js frontend (TypeScript, shadcn/ui, Prisma). The largest project in the collection with well-organized modules across both stacks.

## Exemplars

### 1. Django REST Views — `backend/apps/comics/views.py`

Class-based viewsets with nested serialization and permissions.

**Pattern:** DRF ViewSets  
**Category:** API Views

```python
# RESTful endpoints for comics, chapters, bookmarks
# Nested serialization for related data
# Permission-based access control
# Pagination and filtering support
```

**Why it's exemplary:** Clean ViewSet pattern with proper HTTP method mapping, queryset optimization, and granular permission classes.

### 2. Django Models with Relations — `backend/apps/comics/models.py`

Rich Django models with complex relationships (Genres, Authors, Artists, Categories).

**Pattern:** Complex Domain Modeling  
**Category:** Data Modeling

```python
# Comic model with multiple M2M relationships to genres, authors, artists
# Chapter model with ordering and hierarchy
# Bookmark and reading progress tracking
# Comment system with nested replies
```

**Why it's exemplary:** Demonstrates Django ORM's relationship features including M2M through tables, foreign key hierarchies, and model inheritance patterns.

### 3. Next.js shadcn/ui Components — `frontend/src/components/ui/`

Extensive collection of Radix UI primitives wrapped as shadcn/ui components.

**Pattern:** Component Library Architecture  
**Category:** UI Components

```tsx
// 60+ UI components: Accordion, Button, Card, Dialog, DropdownMenu, etc.
// Consistent styling with Tailwind CSS
// Accessible (ARIA-compliant) via Radix primitives
// Variant system with class-variance-authority (CVA)
```

**Why it's exemplary:** Demonstrates scalable component library architecture, consistent API design (cn() utility, CVA variants), and accessibility standards.

### 4. Frontend Auth System — `frontend/src/auth.ts`, `auth-providers.ts`, `auth-adapter.ts`

Modular NextAuth authentication with multiple providers and custom adapter.

**Pattern:** Pluggable Auth Architecture  
**Category:** Authentication

```typescript
// Multiple auth providers (credentials, OAuth)
// Custom adapter for database integration
// Middleware for protected routes
// Session management and role-based access
```

**Why it's exemplary:** Clean separation of auth concerns into config, providers, and adapter modules. Extensible provider pattern.

### 5. Seed Orchestrator — `frontend/src/scripts/seed/`

Comprehensive database seeding system with dependency graph, batch processing, and progress tracking.

**Pattern:** Data Seeding Pipeline  
**Category:** Data Operations

```typescript
// Dependency-ordered seeding with graph resolution
// Batch processing for large datasets
// Progress tracking and logging
// Conflict resolution and deduplication
// Transaction management
```

**Why it's exemplary:** Sophisticated seeding system that handles entity dependencies, batch operations for performance, and transaction safety. Demonstrates production-grade data pipeline design.
