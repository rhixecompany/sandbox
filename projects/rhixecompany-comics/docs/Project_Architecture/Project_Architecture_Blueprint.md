# RhixeCompany Comics — Dual-Stack Platform Architecture Blueprint

## Project Overview

- **Project Name:** RhixeCompany Comics
- **Project Type:** Comic Platform (Dual Stack)
- **Architecture Pattern:** Dual-Stack (Django + Next.js)
- **Technology Stack:** Django 4.x + Next.js 16 + Celery
- **Stack Type:** Dual-stack (Django + Next.js)

## Technology Stack Details

| Category | Technology |
|---|---|
| Backend Framework | Django 4.x + Django REST Framework (DRF) |
| Frontend Framework | Next.js 16 (App Router) |
| Language (Backend) | Python 3.10+ |
| Language (Frontend) | TypeScript (strict) |
| Task Queue | Celery + Redis |
| Database | PostgreSQL |
| Containerization | Docker Compose |
| Deployment | Docker |

## Directory Structure

```
rhixecompany-comics/
├── AGENTS.md                 # Project context
├── .env.example              # Environment template
├── backend/                  # Django backend
│   ├── manage.py             # Django management
│   ├── requirements.txt      # Python dependencies
│   ├── config/               # Django configuration
│   │   ├── settings/
│   │   ├── urls.py
│   │   ├── wsgi.py
│   │   └── celery.py         # Celery config
│   ├── apps/                 # Django applications
│   │   ├── comics/           # Comic management
│   │   ├── users/            # User management
│   │   └── api/              # REST API endpoints
│   ├── tasks.py              # Celery task definitions
│   └── tests/                # Backend tests
├── frontend/                 # Next.js frontend
│   ├── package.json          # Node dependencies
│   ├── next.config.ts        # Next.js config
│   ├── src/
│   │   ├── app/              # App Router pages
│   │   ├── components/       # React components
│   │   ├── lib/              # Utility functions
│   │   └── styles/           # CSS modules
│   └── public/               # Static assets
├── docker-compose.yml        # Docker Compose
├── docs/
│   ├── architecture.md       # Architecture documentation
│   └── consolidation-patterns.md  # Consolidation patterns
├── .github/workflows/        # CI pipeline
│   └── ci.yml
├── RESEARCH_REPORT.md        # Research documentation
└── copilot-instructions.md   # AI instructions
```

## Architectural Patterns

### Dual-Stack Architecture
- **Django Backend**: REST API (DRF), database management, Celery task orchestration
- **Next.js Frontend**: Server Components, client interactivity, static optimization

### API Communication
- Backend serves REST API at `/api/` via DRF
- Frontend consumes backend API directly
- Shared environment variables across stacks

### Async Task Processing
- Celery handles background tasks (scraping, processing, notifications)
- Redis as message broker and result backend
- Celery Beat for scheduled periodic tasks

## Data Flow

```
User Browser → Next.js Frontend (App Router)
                    ↓
            fetch() / Axios API Calls
                    ↓
         Django REST API (/api/)
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
    Django Views           DRF Serializers
        ↓                       ↓
    Django ORM              Validation
        ↓                       ↓
    PostgreSQL DB         Celery Worker
                              ↓
                          Redis Broker
```

## Key Dependencies

- **Backend:** `django>=4.0`, `djangorestframework`, `django-cors-headers`
- **Async:** `celery`, `redis`
- **Frontend:** `next@16`, `react@19`, `typescript`
- **Database:** `psycopg2-binary`, `postgresql`
- **Dev:** `pytest`, `pytest-django`, `eslint`, `prettier`

## Architecture Decisions

1. **Dual stack separation** — Independent backend/frontend deployment with clear API boundaries
2. **Celery for async** — Background task processing for scraping and data processing
3. **REST API at `/api/`** — Consistent API prefix on both stacks
4. **Redis broker** — Lightweight, fast message broker for Celery
5. **Docker Compose** — Unified development environment for both stacks

## Extensibility Points

- Add new Django apps for additional features
- Add new Next.js pages and components
- Add new Celery tasks for background processing
- Implement GraphQL alongside REST API
- Add real-time features via WebSocket

---

*Generated by architecture-blueprint-generator — comprehensive analysis*
