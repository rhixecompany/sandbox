# Workflow Analysis — xamehi.tv (Django + React Streaming)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** xamehi.tv
**Type:** Movie/Media Streaming Dashboard
**Stack:** Django REST + React 17 + Material-UI + Redux + PostgreSQL
**Architecture Pattern:** Dual-Stack (Django API + React SPA)
**Entry Point:** Django REST API (`/api/`), React SPA (`frontend/`)

A media streaming dashboard application with Django REST backend and React frontend (Material-UI, Redux). Features movie/series browsing, search, user management, and PayPal integration.

## Workflows

### Workflow 1: Movie/Series Browsing & Streaming

**Description:** User browses movies and series, views details, and streams video content.

**Entry Points:**
- **Backend API:** `video/views/movies_views.py`, `series_views.py`
- **Frontend:** React components with Material-UI, Redux state management
- **Models:** `video/models.py` — Movie, Series models

**Steps:**
1. User lands on React frontend (Material-UI themed dashboard)
2. Frontend dispatches Redux action to fetch movies/series list
3. Redux saga/thunk calls Django REST API (`/api/movies/`, `/api/series/`)
4. DRF serializers return JSON data
5. Frontend renders movie/series cards with Material-UI Grid
6. User filters by genre, rating, or year via `video/filters.py`
7. User clicks on a movie/series → detail page loads
8. Detail view fetches additional metadata (cast, trailers, related content)
9. User clicks play → video player (video-react) loads stream
10. User interactions (ratings, reviews, watchlist) POST back to API
11. JWT authentication validates user requests via `SimpleJWT`

**Sequence Diagram:**
```
React SPA               Django REST API             PostgreSQL              External APIs
    |                        |                          |                       |
    |-- Browse -------->     |                          |                       |
    |   GET /api/movies/     |                          |                       |
    |                        |-- query movies --------->|                       |
    |                        |   (filter, sort, page)   |                       |
    |                        |<-- movie list -----------|                       |
    |<-- movie cards ---------|                          |                       |
    |                        |                          |                       |
    |-- select movie ------->|                          |                       |
    |                        |-- query movie detail ---->|                       |
    |                        |   + related content      |                       |
    |                        |<-- full movie data ------|                       |
    |<-- movie detail -------|                          |                       |
    |    + metadata          |                          |                       |
    |                        |                          |                       |
    |-- play video --------->|                          |                       |
    |                        |-- fetch stream URL ----->|                       |
    |                        |   (video source)         |                       |
    |                        |<-- stream URL -----------|                       |
    |<-- video player -------|                          |                       |
    |                        |                          |                       |
    |-- rate/review -------->|                          |                       |
    |   POST /api/ratings/   |                          |                       |
    |                        |-- validate JWT --------->|                       |
    |                        |-- INSERT rating -------->|                       |
    |                        |<-- saved -----------------|                       |
    |<-- confirmation -------|                          |                       |
```

**Error Handling:**
- Movie not found → 404, show "not found" in React
- Video stream unavailable → show error message, suggest alternative
- JWT expired → refresh token flow or redirect to login
- CORS issues → `django-cors-headers` configured for `localhost:3000`
- API errors → Redux error state with user-friendly messages

**Test Patterns:**
- Backend: `python manage.py test` — `video/tests.py`
- Frontend: `cd frontend && npm test`
- CORS + proxy troubleshooting in development

---

### Workflow 2: User Authentication & Profile Management

**Description:** User registration, login, profile management via django-allauth and SimpleJWT.

**Entry Points:**
- `video/views/user_views.py` — user registration and auth views
- `video/urls/user_urls.py` — auth endpoint routing
- `video/decorators.py` — custom decorators for access control

**Steps:**
1. User navigates to sign-up page
2. Frontend collects email, username, password
3. POST to `/api/auth/register/` via DRF endpoint
4. Backend validates input (django-allauth)
5. User account created, profile initialized
6. JWT tokens issued via SimpleJWT (access + refresh)
7. Frontend stores tokens, redirects to dashboard
8. User can update profile (avatar, preferences) via `/api/users/me/`
9. django-allauth handles social auth and email verification

**Error Handling:**
- Duplicate email → validation error from backend
- Weak password → django-allauth password validators
- Token expired → auto-refresh via interceptor
