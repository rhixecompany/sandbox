# xamehi.tv — Streaming Platform Architecture Blueprint

## Project Overview

- **Project Name:** xamehi.tv
- **Project Type:** Video Streaming / Media Platform
- **Architecture Pattern:** Django Full-Stack + React SPA
- **Technology Stack:** Django REST + React 17 + Material-UI
- **Stack Type:** Django

## Technology Stack Details

| Category | Technology |
|---|---|
| Backend Framework | Django + Django REST Framework (DRF) |
| Language (Backend) | Python 3.x |
| Frontend Framework | React 17 |
| UI Library | Material-UI 4 |
| State Management | Redux |
| Authentication | SimpleJWT, django-allauth |
| Database (prod) | PostgreSQL |
| Database (dev) | SQLite |
| Payments | PayPal |
| Video Player | video-react |
| CORS | django-cors-headers |
| Deployment | Gunicorn + WhiteNoise |

## Directory Structure

```
xamehi.tv/
├── AGENTS.md                 # Project context
├── myenv/                    # Python virtual environment (optional)
├── backend/                  # Django backend
│   ├── manage.py             # Django management
│   ├── requirements.txt      # Python dependencies
│   ├── config/               # Django configuration
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── apps/                 # Django apps
│   │   ├── videos/           # Video content management
│   │   ├── users/            # User management
│   │   ├── payments/         # Payment processing
│   │   └── api/              # REST API endpoints
│   └── tests/                # Backend tests
├── frontend/                 # React frontend
│   ├── package.json          # Node dependencies
│   ├── public/               # Public assets
│   ├── src/
│   │   ├── components/       # React components (Material-UI)
│   │   ├── pages/            # Page components
│   │   ├── store/            # Redux store
│   │   ├── services/         # API services
│   │   └── styles/           # Theme/styles
│   └── tests/                # Frontend tests
├── .github/workflows/        # CI pipeline
│   └── ci.yml
├── RESEARCH_REPORT.md        # Research documentation
├── copilot-instructions.md   # AI instructions
└── .vscode/                  # VS Code config
```

## Architectural Patterns

### Django + React SPA Pattern
- Django REST API serves as backend
- React SPA consumes API with Material-UI components
- CORS configured via `django-cors-headers`
- JWT authentication via SimpleJWT

### Proxy Development Pattern
- Frontend dev server at `localhost:3000`
- Backend API at `127.0.0.1:8000`
- Frontend proxies API requests to backend during development
- CORS headers configured for production

### Video Streaming
- video-react for player component
- REST API for video metadata and streaming URLs
- PayPal integration for premium content

## Data Flow

```
User Browser → React SPA (Port 3000)
                    ↓
              Axios API Calls
                    ↓
         CORS Proxy (Dev) / Direct (Prod)
                    ↓
         Django REST API (Port 8000)
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
    Django Views           DRF Serializers
        ↓                       ↓
    Django ORM              SimpleJWT Auth
        ↓                       ↓
    PostgreSQL DB         PayPal API
```

## Key Dependencies

- **Backend:** `django`, `djangorestframework`, `django-cors-headers`, `django-allauth`, `djangorestframework-simplejwt`
- **Frontend:** `react@17`, `@material-ui/core@4`, `redux`, `react-redux`, `axios`, `video-react`
- **Payments:** `paypal-rest-sdk`
- **Dev:** `pytest`, `pytest-django`, `jest`, `@testing-library/react`

## Architecture Decisions

1. **React 17 + Material-UI 4** — Mature, well-tested UI framework for media platforms
2. **SimpleJWT + allauth** — JWT authentication with social login support
3. **django-cors-headers** — Cross-origin requests between frontend and backend
4. **video-react** — React-native video player for streaming content
5. **PayPal integration** — Payment processing for premium/subscription content

## Extensibility Points

- Add video streaming with adaptive bitrate (HLS/DASH)
- Implement user subscriptions and tiered access
- Add content recommendation engine
- Integrate additional payment providers
- Add social features (comments, ratings, sharing)

---

*Generated by architecture-blueprint-generator — comprehensive analysis*
