# Code Exemplars — Xamehi.TV

> Generated by code-exemplars-blueprint-generator

**Project:** projects/xamehi.tv/  
**Type:** TV/Movie Streaming Platform  
**Stack:** Django REST + React 17  
**Source Files:** Python (Django models, views, serializers, URLs), JavaScript/JSX (React frontend)

## Summary

A streaming platform for movies and TV series built with Django REST Framework backend and React 17 frontend with Material-UI. Includes MongoDB integration alongside PostgreSQL and user management features.

## Exemplars

### 1. Video Views — `video/views/movies_views.py`

DRF views for movie and series endpoints with search, filtering, and pagination.

**Pattern:** DRF View Functions  
**Category:** API Views

```python
# Movie listing with search and genre filters
# Series listing with episode grouping
# Category browsing
# Paginated responses
```

**Why it's exemplary:** Clean DRF view function pattern with query parameter handling, consistent pagination, and search/filter logic.

### 2. Serializers — `video/serializers.py`

DRF serializers for video content with nested episode data.

**Pattern:** Nested Serialization  
**Category:** API Serialization

```python
# Movie serializer with genre and cast relationships
# Series serializer with nested episode list
# User action serializers (favorites, watch history)
```

**Why it's exemplary:** Handles complex nested data structures (series → seasons → episodes), demonstrates DRF's serializer relationships.

### 3. URL Routing — `video/urls/`

Modular URL configuration for video resources.

**Pattern:** App-Level URL Routing  
**Category:** Routing

```python
# series_urls.py — episode and series endpoints
# user_urls.py — user-specific actions
# Clean separation by domain
```

**Why it's exemplary:** Well-organized URL modularization that mirrors the view structure, making it easy to navigate and maintain.

### 4. MongoDB Views — `video/pymongo_views.py`

Integration with MongoDB for flexible content metadata storage.

**Pattern:** Hybrid Database Pattern  
**Category:** Data Storage

```python
# PyMongo integration alongside Django ORM
# Flexible schema for content metadata
# Aggregation pipeline usage
```

**Why it's exemplary:** Demonstrates hybrid storage pattern using both PostgreSQL (structured) and MongoDB (flexible) for different data needs.
