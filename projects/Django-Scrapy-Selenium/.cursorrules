[package-manager]
tool = pnpm
lockfile = pnpm-lock.yaml

[commands]
install = pnpm install
dev = pnpm dev
build = pnpm build
lint = pnpm lint
test = pnpm test
format = pnpm format
type-check = pnpm type-check

[quality]
pre_commit = pnpm validate