# Django-Scrapy-Selenium — Scraping Platform Architecture Blueprint

## Project Overview

- **Project Name:** Django-Scrapy-Selenium
- **Project Type:** Web Scraping Platform
- **Architecture Pattern:** Django Full-Stack + ETL Pipeline
- **Technology Stack:** Django 4.x + Scrapy + Selenium + Celery
- **Stack Type:** Django

## Technology Stack Details

| Category | Technology |
|---|---|
| Backend Framework | Django 4.x |
| REST API | Django REST Framework (DRF) |
| Language | Python 3.10+ |
| Web Scraping | Scrapy, BeautifulSoup4 |
| Browser Automation | Selenium |
| Task Queue | Celery + Redis/RabbitMQ |
| Database (prod) | PostgreSQL |
| Database (dev) | SQLite |
| Frontend | Tailwind CSS Dashboard (Node.js) |
| Containerization | Docker Compose |
| WSGI Server | Gunicorn |
| CI/CD | GitHub Actions |

## Directory Structure

```
Django-Scrapy-Selenium/
├── AGENTS.md                 # Project context
├── requirements.txt          # Python dependencies
├── .env.example              # Environment template
├── config/                   # Django configuration
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── scraper/                  # Scrapy spider app
│   └── spiders/              # Spider definitions
├── selenium_app/             # Selenium scripts
├── src/                      # Node.js frontend source
│   └── scrape.js             # JS scraping entry point
├── templates/                # Django templates
├── static/                   # Static assets
├── node_modules/             # Frontend dependencies
├── .github/workflows/        # CI pipeline
│   └── ci.yml
├── RESEARCH_REPORT.md        # Research documentation
├── copilot-instructions.md   # AI instructions
└── .vscode/                  # VS Code config
```

## Architectural Patterns

### ETL Pipeline Pattern
The scraping platform follows an Extract-Transform-Load pipeline:

1. **Extract**: Scrapy spiders + Selenium automation collect data from web sources
2. **Transform**: BeautifulSoup and Django models process/validate data
3. **Load**: Data persisted to PostgreSQL via Django ORM

### Dual Scraping Strategy
- **Scrapy**: For static/simple HTML scraping with built-in crawling
- **Selenium**: For JavaScript-rendered pages requiring browser interaction
- Celery orchestrates async scraping tasks

### Task Queue Pattern
- Celery workers handle long-running scraping tasks
- Redis/RabbitMQ as message broker
- Results stored in Django database

## Data Flow

```
User Request → Django Views/REST API
                    ↓
              URL Router
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
    Scrapy Spider          Selenium Driver
        ↓                       ↓
    BeautifulSoup          DOM Parsing
        ↓                       ↓
        └───────────┬───────────┘
                    ↓
            Django ORM Models
                    ↓
            PostgreSQL Database
                    ↓
          ┌─────────┴─────────┐
          ↓                   ↓
    Tailwind Dashboard    REST API (DRF)
```

## Key Dependencies

- **Python:** `django>=4.0`, `djangorestframework`, `scrapy`, `selenium`, `beautifulsoup4`
- **Async:** `celery`, `redis`
- **Database:** `psycopg2-binary` (prod), `sqlite3` (dev)
- **Dev:** `pytest`, `pytest-django`
- **Frontend:** `tailwindcss`, `node.js`

## Architecture Decisions

1. **Dual scraping engines** — Scrapy for static pages, Selenium for JS-heavy sites
2. **Celery for async processing** — Long-running scrapers managed via task queue
3. **DRF for data access** — RESTful API for scraped data consumption
4. **Docker Compose** — Consistent dev environment with all services

## Extensibility Points

- Add new Scrapy spiders in `scraper/spiders/`
- Add new Selenium automation scripts
- Add new Celery task definitions
- Extend Django models for new data types
- Add API consumers via DRF views

---

*Generated by architecture-blueprint-generator — comprehensive analysis*
