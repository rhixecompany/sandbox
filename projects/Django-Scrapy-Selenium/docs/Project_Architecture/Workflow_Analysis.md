# Workflow Analysis — Django-Scrapy-Selenium

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Django-Scrapy-Selenium
**Type:** Web Scraping Platform
**Stack:** Django 4.x + Scrapy + Selenium + Celery + Redis/RabbitMQ
**Architecture Pattern:** Orchestrated Scraping Pipeline
**Entry Point:** Scrapy spiders + Django management commands + Celery tasks

A comprehensive web scraping platform combining Django for data management, Scrapy for scalable crawling, Selenium for JavaScript-rendered pages, and Celery for async task processing.

## Workflows

### Workflow 1: Scheduled Scraping Pipeline

**Description:** Automated scraping pipeline triggered on schedule or via admin, processing pages through Scrapy/Selenium, storing results in Django models.

**Entry Points:**
- `scrapy crawl spider_name` (direct)
- `celery -A config worker -l info` (async)
- Django admin panel (manual trigger)

**Steps:**
1. Celery Beat triggers scheduled scraping task
2. Celery worker picks up the task
3. Task determines spider type based on target URL/content type
4. For static pages: Scrapy spider crawls and parses HTML
5. For JavaScript-rendered pages: Selenium webdriver loads and extracts content
6. Raw data is cleaned/sanitized (remove scripts, normalize text)
7. Cleaned data is passed to Django ORM for storage
8. Django signals may trigger post-processing (e.g., data validation)
9. Results are accessible via Django admin and API endpoints
10. Logs are written for monitoring and debugging

**Sequence Diagram:**
```
Celery Beat            Celery Worker           Scrapy/Selenium          Django ORM           Admin/API
    |                       |                       |                       |                    |
    |-- scheduled task ---->|                       |                       |                    |
    |                       |                       |                       |                    |
    |                       |-- determine spider -->|                       |                    |
    |                       |   type + target       |                       |                    |
    |                       |                       |                       |                    |
    |                       |   if static:          |                       |                    |
    |                       |-- Scrapy crawl ------->|                       |                    |
    |                       |   parse HTML           |                       |                    |
    |                       |                       |                       |                    |
    |                       |   if JS-rendered:     |                       |                    |
    |                       |-- Selenium extract --->|                       |                    |
    |                       |   WebDriverWait       |                       |                    |
    |                       |   + parsing           |                       |                    |
    |                       |                       |                       |                    |
    |                       |<-- scraped data ------|                       |                    |
    |                       |                       |                       |                    |
    |                       |-- sanitize data ----->|                       |                    |
    |                       |   (remove scripts,    |                       |                    |
    |                       |    normalize)         |                       |                    |
    |                       |                       |                       |                    |
    |                       |-- store to ORB ------>|                       |                    |
    |                       |   scrape results      |--- INSERT records --->|                    |
    |                       |                       |                       |                    |
    |                       |                       |                       |-- view results ---->|
    |                       |                       |                       |   via dashboard    |
```

**Error Handling:**
- `robots.txt` compliance check → respect crawl delays
- Rate limiting → configurable delays and user-agent rotation
- `StaleElementReferenceException` → Selenium retry pattern
- Spider crash → Celery retries with backoff
- Sanitize scraped data before storing/display (XSS prevention)

**Test Patterns:**
- `pytest` for Django models/views
- Manual spider testing via `scrapy crawl spider_name`
- Selenium test scripts in `src/scrape.js`

---

### Workflow 2: Manual Scraping via Selenium Script

**Description:** One-off scraping of JavaScript-rendered pages using standalone Node.js Selenium script.

**Entry Points:**
- `node src/scrape.js`

**Steps:**
1. User runs `node src/scrape.js`
2. Script initializes Selenium WebDriver with ChromeDriver
3. Navigates to target URL
4. Uses explicit `WebDriverWait` for dynamic content to load
5. Extracts data using consistent `By` selectors
6. Handles `StaleElementReferenceException` with retry
7. Formats and saves scraped data
8. Calls `driver.quit()` in `finally` block

**Error Handling:**
- `driver.quit()` guaranteed via `finally` block
- Retry on `StaleElementReferenceException`
- `robots.txt` checked + polite delays applied
- ChromeDriver/profiles not committed to VCS
