# Code Exemplars — University Library JSM

> Generated by code-exemplars-blueprint-generator

**Project:** projects/university-libary-jsm/  
**Type:** Library Management System  
**Stack:** Next.js 15 + Drizzle + Neon + Redis  
**Source Files:** TypeScript/TSX (app, components, database, lib, hooks, emails)

## Summary

A full-featured library management system built with Next.js 15, Drizzle ORM, Neon PostgreSQL, and Upstash Redis. Demonstrates modern Next.js patterns including App Router, server components, database migrations, and email workflows.

## Exemplars

### 1. Drizzle Schema — `database/schema.ts`

Type-safe database schema definition with Drizzle ORM.

**Pattern:** ORM Schema Definition  
**Category:** Data Modeling

```typescript
// Database tables defined with Drizzle ORM syntax
// Relations between tables
// Type inference for queries
```

**Why it's exemplary:** TypeScript-native schema definition provides full type safety from database to UI, with auto-generated query types.

### 2. Database Seed — `database/seed.ts`

Seed script with Drizzle ORM for populating development data.

**Pattern:** Database Seeding  
**Category:** Data Operations

```typescript
// Drizzle ORM-based seeding
// Transaction-safe batch inserts
// Relationship-aware data population
```

**Why it's exemplary:** Uses Drizzle's transactional API for safe batch operations, demonstrates relationship handling in seed data.

### 3. NextAuth with Drizzle — `auth.ts`

Authentication integration with Drizzle adapter for database sessions.

**Pattern:** Auth with Custom Adapter  
**Category:** Authentication

```typescript
// NextAuth.js v5 configuration
// Drizzle adapter for session/user storage
// Credential and OAuth provider support
```

**Why it's exemplary:** Clean integration of NextAuth with Drizzle ORM, demonstrating custom adapter pattern for non-standard ORMs.

### 4. Library Middleware — `middleware.ts`

Next.js middleware for route protection and rate limiting.

**Pattern:** Middleware-Based Access Control  
**Category:** Security

```typescript
// Route protection with NextAuth
// Upstash Redis rate limiting
// Role-based redirects
```

**Why it's exemplary:** Demonstrates Edge-compatible middleware patterns with serverless Redis for rate limiting and protected route handling.

### 5. Email Templates — `emails/`

React Email components for transactional emails.

**Pattern:** Component-Based Email Templates  
**Category:** Email

```tsx
// React Email components using @react-email/components
// Type-safe email template rendering
// Responsive email design patterns
```

**Why it's exemplary:** Uses React components for email templates, ensuring type safety and reusability across the application. Demonstrates modern email development workflow.
