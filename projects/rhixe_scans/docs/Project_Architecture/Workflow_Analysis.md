# Workflow Analysis — Rhixe Scans (Comic Reader)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Rhixe Scans
**Type:** Comic/Manga Reader Platform
**Stack:** Next.js 15 + Prisma 6 + PostgreSQL + Tailwind + Zustand + TanStack Query + Stripe/PayPal + UploadThing + Resend
**Architecture Pattern:** Next.js App Router + Server Actions + Data Access Layer
**Entry Point:** API routes, Server Actions (`src/lib/actions/`), Database queries (`src/lib/data/`)

A sophisticated comic reading platform with user authentication, comic/chapter management, bookmarks, subscriptions, WebSocket support, and image optimization.

## Workflows

### Workflow 1: Comic Reading with Progress Tracking

**Description:** User browses available comics, starts reading a chapter, and has reading progress automatically tracked.

**Entry Points:**
- **Pages:** Comic listing, detail, reader pages (Next.js App Router)
- **Actions:** `src/lib/actions/comic.actions.ts`, `chapter.actions.ts`, `bookmark.actions.ts`
- **Data:** `src/lib/data/comic.ts`, `chapter.ts`, `comicimage.ts`, `chapterimage.ts`

**Steps:**
1. User visits home page → fetches featured/popular comics
2. User browses by genre/category/author/artist via `genre.ts`, `category.ts`, `author.ts`, `artist.ts`
3. User clicks a comic → detail page loads comic metadata, chapters, images
4. User selects chapter → reader interface loads chapter images
5. Chapter images are optimized via Next.js Image + `UploadThing`
6. Reading progress auto-saves via bookmark actions (`bookmark.actions.ts`)
7. User can bookmark, rate, or comment on comics
8. User preferences stored via Zustand + sync to server

**Sequence Diagram:**
```
User/Client             Next.js App              Prisma/DB              UploadThing          Stripe/PayPal
    |                       |                        |                       |                    |
    |-- Browse ------------>|                        |                       |                    |
    |   comics              |-- query comics ------->|                       |                    |
    |                       |   (genre, author, etc) |                       |                    |
    |                       |<-- comic list ---------|                       |                    |
    |<-- comic grid --------|                        |                       |                    |
    |                       |                        |                       |                    |
    |-- select comic ------>|                        |                       |                    |
    |                       |-- query comic + ------>|                       |                    |
    |                       |   chapters + images    |                       |                    |
    |                       |<-- full data ----------|                       |                    |
    |<-- comic detail ------|                        |                       |                    |
    |                       |                        |                       |                    |
    |-- read chapter ------>|                        |                       |                    |
    |                       |-- fetch chapter ------>|                       |                    |
    |                       |   image URLs           |                       |                    |
    |                       |                        |-- serve images ------>|                    |
    |                       |<-- optimized images ---|                       |                    |
    |<-- reader view -------|                        |                       |                    |
    |                       |                        |                       |                    |
    |-- bookmark/page ----->|                        |                       |                    |
    |   progress            |-- upsert bookmark ---->|                       |                    |
    |                       |   (comic_id, chapter,  |                       |                    |
    |                       |    page, timestamp)    |                       |                    |
    |                       |<-- saved --------------|                       |                    |
```

**Error Handling:**
- Comic not found → 404 page with similar comics
- Chapter image missing → fallback placeholder image
- Auth check → NextAuth v5 middleware on protected routes
- Zod validation on all action inputs (`src/lib/validators.ts`)
- Prisma errors → typed error handling in data layer

**Test Patterns:**
- `paypal.test.ts` — PayPal integration tests
- `executeAction.ts` — server action wrapper for consistent error handling

---

### Workflow 2: Subscription & Premium Access

**Description:** User subscribes to premium tier for ad-free reading, early access, and exclusive content.

**Entry Points:**
- **Payments:** Stripe + PayPal integration
- **Auth:** NextAuth v5
- **Email:** Resend for transactional emails

**Steps:**
1. User views subscription plans on pricing page
2. User selects plan (monthly/yearly)
3. User chooses payment method (Stripe/PayPal)
4. Frontend creates checkout session via server action
5. User completes payment on payment provider's interface
6. Webhook from Stripe/PayPal updates subscription status
7. User's role is upgraded to premium in database
8. Email confirmation sent via Resend
9. Frontend updates UI to reflect premium status

**Error Handling:**
- Payment failure → provider handles retries, frontend shows status
- Webhook delivery failure → provider retry mechanism
- Sub expired → cron job or webhook event → downgrade role
