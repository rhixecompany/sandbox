# Code Exemplars — Rhixe Scans (Comic Reader)

> Generated by code-exemplars-blueprint-generator

**Project:** projects/rhixe_scans/  
**Type:** Next.js Comic Reader  
**Stack:** Next.js 15 + TypeScript + Prisma + Tailwind  
**Source Files:** TypeScript (app, components, lib, hooks, auth)

## Summary

A full-featured comic reading platform with Next.js 15, Prisma ORM, NextAuth, and Stripe/PayPal payments. Well-structured with server actions, Prisma data access layer, and comprehensive validation.

## Exemplars

### 1. Server Actions — `src/lib/actions/comic.actions.ts`

'use server' actions with Prisma queries, Zod validation, and cache revalidation.

**Pattern:** Server Action Pattern  
**Category:** API / Data Access

```typescript
'use server';
import db from '@/lib/prisma';
import { Prisma } from '@prisma/client';
import { revalidatePath } from 'next/cache';
import { z } from 'zod';

export async function getLatestComics() {
  const data = await db.comic.findMany({
    take: LATEST_COMICS_LIMIT,
    orderBy: { createdAt: 'desc' },
  });
  return convertToPlainObject(data);
}

export async function getAllComics({
  query, limit = PAGE_SIZE, page, sort,
}: {
  query: string;
  limit?: number;
  page: number;
  sort?: string;
}) {
  const queryFilter: Prisma.ComicWhereInput =
    query && query !== 'all'
      ? { title: { contains: query, mode: 'insensitive' } as Prisma.StringFilter }
      : {};
  // ... pagination and sorting
}
```

**Why it's exemplary:** Type-safe Prisma queries, server-side execution with Next.js cache revalidation, reusable filtering with type safety.

### 2. Prisma Data Access Layer — `src/lib/data/`

Organized data access modules for each domain entity.

**Pattern:** Repository Pattern  
**Category:** Data Access

```typescript
// lib/data/comic.ts — Comic queries
// lib/data/chapter.ts — Chapter queries
// lib/data/user.ts — User queries
// lib/data/genre.ts, author.ts, artist.ts, category.ts
```

**Why it's exemplary:** Clean separation of data access per domain entity, consistent query patterns, and reusable database operations.

### 3. Auth Configuration — `src/auth.ts` / `src/auth.config.ts`

NextAuth v5 configuration with Prisma adapter.

**Pattern:** Authentication with Adapter Pattern  
**Category:** Auth

```typescript
import NextAuth from 'next-auth';
import authConfig from './auth.config';

export const { handlers, auth, signIn, signOut } = NextAuth({
  ...authConfig,
});
```

**Why it's exemplary:** Clean delegation pattern, exports only the minimum API surface needed by the application.

### 4. Zod Validators — `src/lib/validators.ts`

Centralized validation schemas for all domain entities.

**Pattern:** Schema-Based Validation  
**Category:** Validation

```typescript
// Zod schemas for comics, chapters, users
// Reusable across server actions and forms
// Type inference for TypeScript types
```

**Why it's exemplary:** Single source of truth for validation rules, enables type-safe forms and API validation simultaneously.
