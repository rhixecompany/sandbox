# Code Exemplars — Cookiecutter Django Tailwind

> Generated by code-exemplars-blueprint-generator

**Project:** projects/cookiecutter-django-tailwind/  
**Type:** Django Project Template  
**Stack:** Python / Django 5.x + Tailwind CSS  
**Source Files:** Python (setup.py, cookiecutter.json, hooks, template files)

## Summary

A Cookiecutter template for scaffolding production-ready Django projects with Tailwind CSS. Demonstrates template engineering patterns, project scaffolding, and reusable configuration.

## Exemplars

### 1. Package Setup — `setup.py`

Standard Python package setup with calendar versioning and comprehensive classifiers.

**Pattern:** Package Configuration  
**Category:** Distribution

```python
version = "2024.07.31"

setup(
    name="cookiecutter-django",
    version=version,
    description="A Cookiecutter template for creating production-ready Django projects quickly.",
    author="Daniel Roy Greenfeld",
    license="BSD",
    classifiers=[
        "Development Status :: 4 - Beta",
        "Framework :: Django :: 5.0",
        "Programming Language :: Python :: 3.12",
    ],
    keywords=(
        "cookiecutter, Python, projects, project templates, django, "
        "skeleton, scaffolding, project directory, setup.py"
    ),
)
```

**Why it's exemplary:** Clear versioning scheme, appropriate classifiers for PyPI, descriptive long description.

### 2. Cookiecutter Configuration — `cookiecutter.json`

Cookiecutter template configuration with project variables and defaults.

**Pattern:** Template Configuration  
**Category:** Project Scaffolding

```json
{
  "project_slug": "my_project",
  "description": "My awesome Django project",
  "use_drf": "y",
  "use_celery": "n",
  "use_tailwind": "y",
  "use_htmx": "n",
  // ... more options
}
```

**Why it's exemplary:** Well-organized configuration options with sensible defaults for rapid project initialization.

### 3. Managed Settings — `{{cookiecutter.project_slug}}/config/settings/`

Multi-environment Django settings with base/local/production separation.

**Pattern:** Environment-Specific Configuration  
**Category:** Django Settings

```python
# base.py — shared settings
# local.py — development overrides
# production.py — production overrides
```

**Why it's exemplary:** Clean inheritance pattern with django-environ for secrets management. Production-safe defaults with explicit overrides.

### 4. Template Hooks — `hooks/`

Pre- and post-generation hooks for automated project setup.

**Pattern:** Lifecycle Hooks  
**Category:** Automation

```python
# pre_gen_project.py — validation and cleanup before scaffold
# post_gen_project.py — initialization after generation
```

**Why it's exemplary:** Automates repetitive setup tasks, validates inputs before generation, and initializes git/post-generation dependencies automatically.
