# E2E Test Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all failing E2E Playwright tests and stabilize the test suite

**Architecture:** Run tests individually (not in parallel) due to shared auth/DB state. Increase timeouts to accommodate PlaidProvider delays. Replace networkidle waits with explicit waits.

**Tech Stack:** Playwright, Next.js 16, Drizzle ORM, Plaid/Dwolla

---

## Batch 1: Verify payment-transfer.spec.ts Fixes

**Objective:** Confirm the timeout fixes to payment-transfer.spec.ts work

### Task 1: Run payment-transfer tests individually

- [ ] **Step 1: Run Test 1 - unauthenticated redirect**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/payment-transfer.spec.ts --project=chromium --grep 'should redirect unauthenticated'"
```

Expected: PASS

- [ ] **Step 2: Run Test 2 - render form**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/payment-transfer.spec.ts --project=chromium --grep 'should render payment transfer form'"
```

Expected: PASS (may take 10-15s due to PlaidProvider)

- [ ] **Step 3: Run Test 7 - successful transfer**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/payment-transfer.spec.ts --project=chromium --grep 'should complete successful transfer'"
```

Expected: PASS

- [ ] **Step 4: Run Test 8 - dwolla_transfers DB check**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/payment-transfer.spec.ts --project=chromium --grep 'should create dwolla_transfers row'"
```

Expected: PASS

- [ ] **Step 5: Run all payment-transfer tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/payment-transfer.spec.ts --project=chromium"
```

Expected: All 10 tests pass

---

## Batch 2: Fix networkidle timeouts

**Objective:** Replace networkidle waits with explicit waits in affected tests

### Task 2: Fix my-wallets.spec.ts

**Files:**

- Modify: `src/tests/e2e/my-wallets.spec.ts`
- Test: `src/tests/e2e/my-wallets.spec.ts`

- [ ] **Step 1: Read current my-wallets.spec.ts**

```typescript
// Find all instances of waitForLoadState("networkidle")
// Replace with: waitForLoadState("domcontentloaded") or remove entirely
```

- [ ] **Step 2: Run my-wallets tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/my-wallets.spec.ts --project=chromium"
```

Expected: PASS

### Task 3: Fix transaction-history.spec.ts

**Files:**

- Modify: `src/tests/e2e/transaction-history.spec.ts`
- Test: `src/tests/e2e/transaction-history.spec.ts`

- [ ] **Step 1: Read current transaction-history.spec.ts**

- [ ] **Step 2: Replace networkidle with domcontentloaded**

- [ ] **Step 3: Run transaction-history tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/transaction-history.spec.ts --project=chromium"
```

Expected: PASS

### Task 4: Fix plaid-script.spec.ts

**Files:**

- Modify: `src/tests/e2e/specs/plaid-script.spec.ts`
- Test: `src/tests/e2e/specs/plaid-script.spec.ts`

- [ ] **Step 1: Replace networkidle with domcontentloaded**

- [ ] **Step 2: Run plaid-script test**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/specs/plaid-script.spec.ts --project=chromium"
```

Expected: PASS

---

## Batch 3: Fix soft-delete.spec.ts

**Objective:** Rewrite soft-delete tests to actually test soft-delete behavior

### Task 5: Rewrite soft-delete tests

**Files:**

- Modify: `src/tests/e2e/soft-delete.spec.ts`
- Test: `src/tests/e2e/soft-delete.spec.ts`

- [ ] **Step 1: Read current soft-delete.spec.ts**

- [ ] **Step 2: Identify what soft-delete actually does in this codebase**

Check:

- `src/database/schema.ts` - Look for `deletedAt` columns
- `src/dal/*.ts` - Look for soft-delete filtering patterns

- [ ] **Step 3: Rewrite tests to verify actual soft-delete behavior**

Instead of catch-all patterns, test:

1. Deleted user cannot access dashboard (auth fails)
2. Deleted wallet is filtered from wallet list API
3. Deleted transaction is filtered from transaction API

- [ ] **Step 4: Run soft-delete tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/soft-delete.spec.ts --project=chromium"
```

Expected: PASS (or skip if soft-delete not implemented)

---

## Batch 4: Fix transfer-idempotency.spec.ts

**Objective:** Use page object models to avoid extra navigation

### Task 6: Optimize transfer-idempotency tests

**Files:**

- Modify: `src/tests/e2e/transfer-idempotency.spec.ts`
- Test: `src/tests/e2e/transfer-idempotency.spec.ts`

- [ ] **Step 1: Read current transfer-idempotency.spec.ts**

- [ ] **Step 2: Replace dashboard navigation with direct page access**

Current pattern:

```typescript
await page.goto("/dashboard");
await page.click('a:has-text("Transfers")');
```

Should be:

```typescript
// Use paymentTransferPage fixture directly
await paymentTransferPage.navigate();
```

- [ ] **Step 3: Run transfer-idempotency tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/transfer-idempotency.spec.ts --project=chromium"
```

Expected: PASS

---

## Batch 5: Fix wallet-linking.spec.ts

**Objective:** Increase timeouts for wallet linking tests

### Task 7: Update wallet-linking timeouts

**Files:**

- Modify: `src/tests/e2e/wallet-linking.spec.ts`
- Test: `src/tests/e2e/wallet-linking.spec.ts`

- [ ] **Step 1: Read current wallet-linking.spec.ts**

- [ ] **Step 2: Increase timeouts from 15s to 30s for authenticated tests**

- [ ] **Step 3: Run wallet-linking tests**

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/wallet-linking.spec.ts --project=chromium"
```

Expected: PASS

---

## Batch 6: Verify remaining core tests

**Objective:** Ensure auth, admin, dashboard, settings tests pass

### Task 8: Run auth tests

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/auth.spec.ts --project=chromium"
```

### Task 9: Run admin tests

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/admin.spec.ts --project=chromium"
```

### Task 10: Run dashboard tests

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/dashboard.spec.ts --project=chromium"
```

### Task 11: Run settings tests

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/settings.spec.ts --project=chromium"
```

---

## Batch 7: Run integration and mock tests

### Task 12: Run mock-tokens tests

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/mock-tokens.spec.ts --project=chromium"
```

### Task 13: Run link-and-transfer integration test

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bunx playwright test src/tests/e2e/integration/link-and-transfer.spec.ts --project=chromium"
```

---

## Batch 8: Final verification

### Task 14: Run full test suite

```bash
cmd /c "cd /d C:\Users\Alexa\Desktop\SandBox\Banking && set PLAYWRIGHT_PREPARE_DB=true && bun run test:ui"
```

Expected: All tests pass

---

## Summary

| Batch | Tests | Status |
| --- | --- | --- |
| 1 | payment-transfer.spec.ts | 🔴 To verify |
| 2 | my-wallets, transaction-history, plaid-script | ⏳ To fix |
| 3 | soft-delete.spec.ts | ⏳ To fix |
| 4 | transfer-idempotency.spec.ts | ⏳ To fix |
| 5 | wallet-linking.spec.ts | ⏳ To fix |
| 6 | auth, admin, dashboard, settings | ⏳ To verify |
| 7 | mock-tokens, integration | ⏳ To verify |
| 8 | Full suite | ⏳ Final verification |

---

## Notes

- **Always run tests individually** - Use `--grep` to run specific tests
- **PlaidProvider adds 7-12s delay** - All authenticated tests need 30s timeout
- **Use page object models** - Reduces navigation overhead
- **Replace networkidle** - Use domcontentloaded or explicit waits
