# Workflow Analysis — Banking (Fintech App)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Banking
**Type:** Fintech Web Application
**Stack:** Next.js 16 + Drizzle + PostgreSQL + Plaid + Dwolla
**Architecture Pattern:** Next.js App Router + Server Actions + DAL Layer
**Entry Point:** API routes (`src/app/api/`) + Page routes (`src/app/`)

Full-stack fintech banking platform with secure transaction flows, Plaid integration for financial data linking, and Dwolla for ACH transfers.

## Workflows

### Workflow 1: Bank Account Linking via Plaid

**Description:** User connects their external bank account using Plaid Link, enabling balance tracking and fund transfers.

**Entry Points:**
- **UI:** Sign-up / My Wallets page
- **API:** `src/lib/plaid.ts` — Plaid client interactions
- **Webhook:** Plaid webhook events

**Steps:**
1. User clicks "Link Bank Account" on frontend
2. Frontend calls Plaid Link token endpoint (`/api/plaid/link-token`)
3. Backend generates a Plaid Link token using Plaid SDK
4. Plaid Link modal opens in the browser
5. User authenticates with their bank via Plaid's secure interface
6. Plaid returns a `public_token` to the frontend
7. Frontend sends `public_token` to backend exchange endpoint
8. Backend exchanges `public_token` for `access_token` via Plaid SDK
9. Backend stores the `access_token` securely in DB via `schema.ts`
10. Backend fetches initial account balances and transactions via Plaid API
11. Plaid webhook notifies backend of transaction updates
12. Frontend displays linked accounts, balances, and transactions

**Sequence Diagram:**
```
User/Client              Next.js App               Plaid API               Database (Drizzle)
    |                        |                        |                        |
    |-- "Link Bank" -------->|                        |                        |
    |                        |-- create link token -->|                        |
    |                        |<-- link_token ---------|                        |
    |<-- link_token ---------|                        |                        |
    |                        |                        |                        |
    |-- open Plaid Link ---->|                        |                        |
    |   (modal)              |                        |                        |
    |                        |                        |                        |
    |-- auth in Plaid ------>|                        |                        |
    |                        |                        |                        |
    |<-- public_token -------|                        |                        |
    |                        |                        |                        |
    |-- exchange token ----->|                        |                        |
    |                        |-- exchange public ---->|                        |
    |                        |   for access_token     |                        |
    |                        |<-- access_token -------|                        |
    |                        |                        |                        |
    |                        |-- store access_token -->|                        |
    |                        |   + account info        |--- INSERT plaid_items -|
    |                        |                        |                        |
    |                        |-- fetch balances ------>|                        |
    |                        |<-- balances + txs ------|                        |
    |                        |                        |                        |
    |<-- linked account -----|                        |                        |
    |    + balances          |                        |                        |
```

**Error Handling:**
- Plaid Link failure → display error message, allow retry
- Token exchange failure → log error, return 500, no data stored
- Webhook signature verification → validate Plaid webhook signature
- Rate limiting → Redis-based rate limiting (via `src/lib/validations/`)

**Test Patterns:**
- `src/tests/unit/dwolla.dal.test.ts` — Dwolla DAL unit tests
- E2E page objects: `sign-up.page.ts`, `my-wallets.page.ts`, `payment-transfer.page.ts`
- MSW mock server for API route testing

---

### Workflow 2: Fund Transfer (Dwolla ACH)

**Description:** User initiates a fund transfer between their linked accounts or to another user via Dwolla ACH.

**Entry Points:**
- **UI:** Payment Transfer page
- **API:** `src/lib/dwolla.ts` — Dwolla client
- **Validation:** `src/lib/validations/transfer.ts` — transfer validation schemas

**Steps:**
1. User fills transfer form (amount, source account, destination)
2. Frontend validates input via Zod schemas
3. Frontend submits transfer to `/api/transfers`
4. Backend validates user authentication (NextAuth.js)
5. Backend validates transfer (sufficient balance, valid accounts)
6. Backend calls Dwolla API to create ACH transfer
7. Dwolla processes transfer (ACH standard: 2-3 business days)
8. Dwolla sends webhook on transfer completion/failure
9. Backend updates transaction history in database via DAL layer
10. Frontend receives confirmation and displays updated balance

**Error Handling:**
- Insufficient balance → validation error, no Dwolla call made
- Dwolla API failure → log error, return failure to user, retry logic
- Webhook timeout → fallback polling of Dwolla transfer status
- Validation via `src/lib/validations/transfer.ts`

**Test Patterns:**
- Unit tests: `dwolla.dal.test.ts`
- E2E: `payment-transfer.page.ts`
- Mock Dwolla responses via MSW
