# DAL Schedule — Backlog for Complex DAL Work

This document lists pages that were marked as DAL‑intensive during audits and are postponed for a dedicated DAL effort.

How to use

- When a page audit reveals non-trivial DAL work (multiple joins, schema changes, or cross-entity migrations), add an entry here with a short rationale and an estimated effort.
- During Implementation Mode, these entries should be triaged and scheduled separately.

Backlog

- (empty) — will populate as audits identify DAL‑intensive pages.
