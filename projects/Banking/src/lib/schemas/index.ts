export * from "./auth.schema";

// Central schema index — add additional schema exports here as new files are
// introduced (e.g., transfer.schema.ts, profile.schema.ts). Tests and pages
// should import from this central location when relying on shared schemas.
