read and understand /create-implementation-plan  /codebase-overhaul-phase-0-5.plan /memory-banks   commands then As the Reviewer persona Create a detailed comprehensive plan with specs,code samples and file references for implement the features listed below using the question tool ask any clarifying and needed questions then using the question tool keep asking until you dont have any clarifying and needed questions  Ensure to never run typecheck/lint/tests until all tasks is completed:
0: Map the codebase and save all context to `docs/custom-components.md`, `docs/app-pages.md`,`docs/test-context.md` ensure it List, triage and read all nextjs pages and configurations in `./app/**`,List, triage and read all components in `./components` skip `./components/ui`, List,triage,Catalog by vitest or playwright and read all test in `./tests` and List,triage,Catalog all custom typescript scripts,bash,powershell,bat scripts in `./scripts/**` .

1: consolidate, dead duplicate or dead code, use any remaining unused components and enhance all components in `./components` skip `./components/ui` ensure to create and use any needed, recommended hooks and stores.

2: consolidate, dead duplicate or dead code,  all test in `./tests` and  enhance all test.

3:   Start with the First group route layouts which should be `(auth)` then `(admin)` then `(root)` and lastly `app\page.tsx` and locate all its custom components,dal,actions,tests,stores.
4:   For All its components,dal,actions,tests,stores Modify them to be fully functional ensure to create and use any reusable dynamic generic custom components and saved to `./components/layouts` ensure to verify and valid all reusable dynamic generic custom components in `./components/layouts` use dry practices Update all test,helpers and configurations in `docs/test-context.md` use dry practices, validate and use all stores for state management Harden all vitest and Playwright E2E specs by removing skipped/non-deterministic tests, standardizing assertions, and making authenticated scenarios deterministic with a seeded user Work through all Next.js pages sequentially and repeat the process.
5:    Update all custom typescript scripts,bash,powershell,bat scripts in `./scripts/**` enhance with ts-morph  AST-safe, dry-run functionality, create/ update/ consolidate any needed scripts and delete all dead code Ensure bash,powershell,bat scripts are just orchestrators and all functionality are in custom typescript scripts then update package.json accordingly validate all scripts are working.

6: use the brainstorming,plan-specs skills Create a detailed comprehensive plan with a detailed features specs,code samples and file references for implement list,Catalog,triage accordingly all custom typescript scripts scripts in `./scripts/**` for each script found enhance use best practices and dry practices ensure to create any handler,helpers functions ensure each scenarios to be AST-safe and enhanced with re-run in dry-run functionality then update package.json accordingly and remove all bash,powershell,bat scripts from the package.json the enhance Makefile for the repo it is the main cli use best practices and dry practices ensure all bash,powershell,bat scripts are used fix any scenarios delete all dead code, or unreachable code.

10: @planner start by modify npm run format to use the repo bash scripts with echo telling us what running then it will fail if bash is available and then default to the powershell,bat scripts if it works update all validation scripts such as lint/type-check/test/format/build


Create `.opencode\commands\init-enhanced.md` to be a reusable prompt that Update all Agentic Documentation and Rules, enhance  AGENTS.md to be canonical, accurate source of truth for agent behavior in this repo, Rewrite overlapping instruction,Rules surfaces so they do not drift (they should mostly point to AGENTS.md and a small set of canonical rules/instructions),Standardize prompts location on .opencode/commands/ across all agent docs/rules, Standardize specs location on .opencode/specs/ across all agent docs/rules.

10: Update Agentic Documentation and Rules, Make AGENTS.md the canonical, accurate source of truth for agent behavior in this repo, Rewrite overlapping instruction surfaces so they do not drift (they should mostly point to AGENTS.md and a small set of canonical rules/instructions),Standardize plan location on .opencode/commands/ across all agent docs/rules, Use when the user asks to mine prior chats, maintain `AGENTS.md`, or run the continual-learning loop..

11: Delete and Create `.opencode\commands\init-enhanced.md` to be a reusable prompt that Update Agentic Documentation and Rules, Make AGENTS.md the canonical, accurate source of truth for agent behavior in this repo, Rewrite overlapping instruction surfaces so they do not drift (they should mostly point to AGENTS.md and a small set of canonical rules/instructions),Standardize plan location on .opencode/commands/ across all agent docs/rules, Use when the user asks to mine prior chats, maintain `AGENTS.md`, or run the continual-learning loop..

@implementer   Read and update `.opencode\commands\init-enhanced.md` to be Comprehensive documentation sync for all agentic coding tools (OpenCode, Cursor, GitHub Copilot) operating in this Banking repository. This document is a reproducible checklist to bring documentation in sync with source code and to collect actionable issues for maintainers using the question tool ask all clarifying/needed questions keep asking until you dont have anymore clarifying/needed questions then show me the plan.


1: List, triage,Catalog and read all markdown files in `.opencode/skills/*.md`  identify any inconsitency with the repo fix all then consolidate, dead duplicate or dead markdown files,  verify  all markdown files and  enhance all markdown files format all markdown files markdownlint,.markdownlintrc.json.

5: using the question tool ask any clarifying and needed questions then using the question tool keep asking until you dont have any clarifying and needed questions.

@implementer   modify @scripts\mcp-runner.ts to be a custom handler for installing mcp servers in Opencode/Copilot/Cursor. using the bash tool run `docker mcp gateway run --profile adminbot` to get a list of docker mcp servers the conferences it with @.opencode\mcp_servers.json if an mcp server is found at @.opencode\mcp_servers.json Catalog it then create all helpers for enhanced this file then update @opencode.json with all mcp servers in the Catalog using the @scripts\mcp-runner.ts file then delete all mcp servers in the Catalog from docker leave any mcp server not found ensure all mcp servers are load via @scripts\mcp-runner.ts

@implementer    create custom typecheck functions that extend and enhance docker mcp tool  `mcp-find`, `mcp-add` ,`mcp-remove` , `mcp-exec` , `mcp-config-set` , `mcp-create-profile` , `mcp-activate-profile` , `code-mode` , `mcp-discover` then using the custom typecheck function for `mcp-create-profile` , `mcp-activate-profile`  create profiles for adminbot then using custom typecheck function for `mcp-remove` remove  `mcp/context7`           | `sha256:1174e6...` |
| `mcp/fetch`              | `sha256:302c62...` |
| `mcp/filesystem`         | `sha256:35fcf0...` |
| `mcp/memory`             | `sha256:db0c2d...` |
| `mcp/next-devtools-mcp`  | `sha256:3064e3...` |
| `mcp/playwright`         | `sha256:64d024...` |
| `mcp/sequentialthinking` | `sha256:cd3174...` |
| `mcp/time`               | `sha256:9c46a9...` |
| `mcp/youtube-transcript` | `sha256:b70b13...` | servers and install the remote npx versions using @scripts\mcp-runner.ts then update @opencode.json with all mcp_servers

opencode plugin opencode-handoff
 opencode plugin opencode-beads
  opencode plugin @tarquinen/opencode-dcp
   opencode plugin @ramarivera/opencode-model-announcer
    opencode plugin github:JRedeker/opencode-morph-fast-apply
     opencode plugin open-plan-annotator
      opencode plugin opentmux
       opencode plugin opencode-ignore
        opencode plugin opencode-mem
         opencode plugin opencode-synced
          opencode plugin opencode-snippets
           opencode plugin opencode-mystatus
            opencode plugin @gotgenes/opencode-agent-identity
             opencode plugin opencode-agent-memory
              opencode plugin opencode-agent-skills
               opencode plugin opencode-devcontainers
                opencode plugin @zenobius/opencode-background
                 opencode plugin @slkiser/opencode-quota
                  opencode plugin opencode-sessions
                   opencode plugin @franlol/opencode-md-table-formatter
                    opencode plugin @liannnix/opencode-ntfy
                     opencode plugin @howaboua/opencode-planning-toolkit

 opencode plugin micode

list,triage and read  .opencode\agent\*.md files the identify any inconsitency with the repo fix all then enhance them for the repo


"opencode-handoff",
    "@tarquinen/opencode-dcp",
    "superpowers@git+https://github.com/obra/superpowers.git",
    "@howaboua/opencode-planning-toolkit@latest",
    "smart-codebase",
    "octto",
    "@nick-vi/opencode-type-inject",
    "@zenobius/opencode-skillful",
    "@spoons-and-mirrors/subtask2@latest",
    "@tarquinen/opencode-smart-title",
    "opencode-optimal-model-temps",
    "@openhax/codex",
    "micode",
    "opencode-froggy",
    "@zenobius/opencode-background"
