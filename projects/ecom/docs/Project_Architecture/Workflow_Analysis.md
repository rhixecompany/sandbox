# Workflow Analysis — Ecom (Django + React Ecommerce)

> Generated by workflow-analysis-blueprint-generator
> Date: 2026-06-25

## Project Overview

**Project:** Ecom
**Type:** Ecommerce Platform
**Stack:** Django REST Framework + React/Redux + PostgreSQL + PayPal
**Architecture Pattern:** Dual-Stack (Django Backend + React SPA Frontend)
**Entry Point:** API (`/api/v1/`) via DRF, React SPA via `npm start`

A full-stack ecommerce platform with product management, shopping cart, checkout, and order management.

## Workflows

### Workflow 1: Product Browsing → Cart → Checkout → Payment

**Description:** Complete customer purchase journey from product discovery through PayPal payment processing.

**Entry Points:**
- **Frontend:** React SPA (`frontend/`)
- **Backend:** Django REST API at `/api/v1/`
- **Models:** `base/models.py` — Product, Order, Cart models

**Steps:**
1. **Browsing**: User browses product catalog on React frontend
2. **API Call**: Frontend fetches products from `GET /api/v1/products/`
3. **Add to Cart**: User adds item to cart → `POST /api/v1/cart/`
4. **Cart View**: Cart page shows items, quantities, totals
5. **Checkout**: User proceeds to checkout, fills shipping details
6. **Order Creation**: Backend creates Order record via `order_views.py`
7. **Payment Redirect**: Frontend redirects to PayPal for payment
8. **PayPal Processing**: PayPal processes payment, sends IPN webhook
9. **Order Confirmation**: Backend updates order status to paid
10. **Email Notification**: Signal sends order confirmation (via `signals.py`)
11. **Stock Update**: Product inventory is decremented

**Sequence Diagram:**
```
React SPA               Django DRF API              Database              PayPal
    |                        |                          |                     |
    |-- Browse products ---->|                          |                     |
    |   GET /api/products/   |                          |                     |
    |                        |-- query products ------->|                     |
    |                        |<-- product list ---------|                     |
    |<-- product grid -------|                          |                     |
    |                        |                          |                     |
    |-- Add to cart -------->|                          |                     |
    |   POST /api/cart/      |                          |                     |
    |                        |-- create/update cart --->|                     |
    |                        |<-- cart saved -----------|                     |
    |<-- cart updated -------|                          |                     |
    |                        |                          |                     |
    |-- Checkout ----------->|                          |                     |
    |                        |-- create order --------->|                     |
    |                        |<-- order created --------|                     |
    |<-- PayPal redirect ----|                          |                     |
    |                        |                          |                     |
    |-- PayPal payment ----->|                          |                     |
    |                        |                          |-- process payment ->|
    |                        |                          |<-- payment result --|
    |                        |                          |                     |
    |                        |<-- IPN webhook ----------|                     |
    |                        |                          |                     |
    |                        |-- update order status -->|                     |
    |                        |-- decrement stock ------>|                     |
    |                        |                          |                     |
    |<-- order confirmation -|                          |                     |
```

**Error Handling:**
- Out of stock → validation error on add to cart
- Payment failure → PayPal handles retry, order kept as "pending"
- API validation → DRF serializers with field-level errors
- Model-level signals for inventory consistency

**Test Patterns:**
- Backend: `python manage.py test` — `base/tests.py`
- Frontend: `npm test` — React component tests
- API testing via DRF's APITestCase

---

### Workflow 2: Admin Order Management

**Description:** Admin user manages orders, updates status, and views sales analytics via Django admin.

**Entry Points:**
- Django admin panel
- Admin-specific API endpoints

**Steps:**
1. Admin logs into Django admin
2. Views list of orders with status, date, amount
3. Filters orders by status (pending, paid, shipped, delivered)
4. Views order detail with line items and customer info
5. Updates order status (e.g., mark as shipped)
6. Order status change triggers email notification to customer
7. Admin views sales dashboard with aggregate data

**Error Handling:**
- Invalid status transitions → validation in admin
- Missing tracking info → optional field, warning if omitted
