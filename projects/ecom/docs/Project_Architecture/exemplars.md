# Code Exemplars — Ecom (Django Ecommerce)

> Generated by code-exemplars-blueprint-generator

**Project:** projects/ecom/  
**Type:** Django Ecommerce Application  
**Stack:** Django REST Framework + React  
**Source Files:** Python (models, views, serializers, urls), JavaScript (React frontend)

## Summary

A full-featured ecommerce platform built with Django REST Framework. Demonstrates clean DRF patterns, well-structured models, and comprehensive API design.

## Exemplars

### 1. Product Model — `base/models.py`

Well-structured Django models with proper field types, relationships, and string representations.

**Pattern:** Domain Model with Rich Fields  
**Category:** Data Modeling

```python
class Product(models.Model):
    user = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    name = models.CharField(max_length=200, null=True, blank=True)
    image = models.ImageField(null=True, blank=True, default='/placeholder.png')
    brand = models.CharField(max_length=200, null=True, blank=True)
    category = models.CharField(max_length=200, null=True, blank=True)
    description = models.TextField(null=True, blank=True)
    rating = models.DecimalField(max_digits=7, decimal_places=2, null=True, blank=True)
    numReviews = models.IntegerField(null=True, blank=True, default=0)
    price = models.DecimalField(max_digits=7, decimal_places=2, null=True, blank=True)
    countInStock = models.IntegerField(null=True, blank=True, default=0)
    createdAt = models.DateTimeField(auto_now_add=True)
    _id = models.AutoField(primary_key=True, editable=False)

    def __str__(self):
        return self.name
```

**Why it's exemplary:** Proper use of `DecimalField` for monetary values, `auto_now_add` for timestamps, null/blank handling, explicit primary key.

### 2. DRF Serializers — `base/serializers.py`

Clean serializer hierarchy with nested serialization and JWT token handling.

**Pattern:** ModelSerializer with Custom Fields  
**Category:** API Serialization

```python
class UserSerializer(serializers.ModelSerializer):
    name = serializers.SerializerMethodField(read_only=True)
    _id = serializers.SerializerMethodField(read_only=True)
    isAdmin = serializers.SerializerMethodField(read_only=True)

    class Meta:
        model = User
        fields = ['id', '_id', 'username', 'email', 'name', 'isAdmin']

    def get_name(self, obj):
        name = obj.first_name
        if name == '':
            name = obj.email
        return name

class UserSerializerWithToken(UserSerializer):
    token = serializers.SerializerMethodField(read_only=True)

    def get_token(self, obj):
        token = RefreshToken.for_user(obj)
        return str(token.access_token)

class ProductSerializer(serializers.ModelSerializer):
    reviews = serializers.SerializerMethodField(read_only=True)

    def get_reviews(self, obj):
        reviews = obj.review_set.all()
        serializer = ReviewSerializer(reviews, many=True)
        return serializer.data
```

**Why it's exemplary:** Serializer inheritance for token extension, `SerializerMethodField` for computed fields, nested serialization with `get_reviews`.

### 3. API Views — `base/views/product_views.py`

DRF view functions with pagination, filtering, and permission control.

**Pattern:** DRF View Functions  
**Category:** API Views

```python
@api_view(['GET'])
def getProducts(request):
    query = request.query_params.get('keyword')
    if query == None:
        query = ''
    products = Product.objects.filter(name__icontains=query).order_by('-createdAt')
    page = request.query_params.get('page')
    paginator = Paginator(products, 5)
    try:
        products = paginator.page(page)
    except PageNotAnInteger:
        products = paginator.page(1)
    except EmptyPage:
        products = paginator.page(paginator.num_pages)
    serializer = ProductSerializer(products, many=True)
    return Response({'products': serializer.data, 'page': page, 'pages': paginator.num_pages})

@api_view(['POST'])
@permission_classes([IsAdminUser])
def createProduct(request):
    product = Product.objects.create(user=request.user, name='Sample Name', ...)
    serializer = ProductSerializer(product, many=False)
    return Response(serializer.data)
```

**Why it's exemplary:** Clean pagination error handling, keyword search filtering, admin-only creation endpoints, consistent response format.

### 4. URL Routing — `base/urls/`

Organized URL routing with clear endpoint separation.

**Pattern:** Modular URL Configuration  
**Category:** Routing

```python
# Separate URL modules for products, orders, users
# Clear endpoint naming conventions
# RESTful resource mapping
```

**Why it's exemplary:** Modular URL structure that mirrors the view architecture, making it easy to navigate and extend.
