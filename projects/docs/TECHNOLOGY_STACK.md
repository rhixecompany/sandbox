# 🏗 Technology Stack Blueprint - docs

**Project Path:** `projects/docs`
**Generated:** 2026-07-31
**Status:** Maintenance — Documentation-only reference

---

## Core Technologies

| Category | Technology | Version | License |
|----------|-----------|---------|---------|
| **Format** | Markdown (GFM) | - | - |
| **Linter** | markdownlint-cli2 | Latest | MIT |
| **Linter** | cspell | Latest | MIT |
| **Docs Tooling** | draw.io (architecture diagrams) | - | - |
| **Docs Tooling** | Mermaid (diagrams in MD) | - | - |

---

## Dependencies

No runtime dependencies. Markdown-only documentation repository. Tooling inherited from workspace root (`package.json`).

```text
markdownlint-cli2   (via root npx)
cspell              (via root npx)
```

---

## Project Structure

```
docs/
├── README.md                   # Project overview and navigation
├── DEPENDENCY_AUDIT.md         # Dependency audit findings across workspace
├── RESEARCH_APPENDIX.md        # Supplementary research documentation
├── architecture.md             # Architecture overview
├── folder-structure.md         # Directory tree
├── tech-stack.md               # Technology summary
├── AGENTS.md                   # Agent context file
├── docs/
│   └── Project_Architecture/   # Workflow analysis and exemplars
└── .vscode/                    # Shared VS Code settings, launch, extensions, tasks
```

---

## Key Features

### 1. Centralized Documentation Reference
Serves as a read-only reference for:
- Dependency audit findings (`DEPENDENCY_AUDIT.md`)
- Research appendices (`RESEARCH_APPENDIX.md`)
- Shared VS Code configuration (`.vscode/`)
- Architecture blueprints and workflow analyses

### 2. DRY Documentation Conventions
- GitHub-Flavored Markdown (GFM)
- Reference rather than duplicate content
- Cross-reference with root `AGENTS.md` and per-project AGENTS.md files
- Architecture diagrams via draw.io / Mermaid

---

## Configuration

### VS Code (`.vscode/`)
Shared workspace configuration files:
- `extensions.json` — recommended extensions
- `launch.json` — debug launch configurations
- `settings.json` — workspace settings
- `tasks.json` — build/lint tasks

---

## Commands

```bash
# Markdown linting (from workspace root)
npx markdownlint-cli2 "projects/docs/**/*.md"

# Spell check
npx cspell "projects/docs/**/*.md"
```

No build or run commands — static documentation only.

---

## Coding Conventions

| Convention | Standard |
|------------|----------|
| **Format** | GitHub-Flavored Markdown |
| **DRY** | Reference shared templates instead of duplicating |
| **Line length** | ≤ 120 chars where practical |
| **Headings** | ATX-style (`#`..`######`) with blank line before/after |
| **Tables** | Pipe tables with header separator row |

---

## CI/CD

None required — pure documentation repository. Markdown linting is inherited from the workspace root tooling.

---

## Maintenance Notes

- Update `DEPENDENCY_AUDIT.md` when dependencies change
- VS Code settings in `.vscode/` are shared across workspace
- Keep docs DRY — reference rather than duplicate
- Cross-reference with root `AGENTS.md` and individual project AGENTS.md files

---

*Generated by Hermes Agent Technology Stack Blueprint Generator*
