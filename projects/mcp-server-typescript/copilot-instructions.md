# Copilot Instructions — mcp-server-typescript

**Canonical reference:** See `../../AGENTS.md` for workspace-wide rules and `../../.hermes.md` for Hermes-specific overrides.

Use this project's `AGENTS.md`, `package.json`, and local tests before editing. Preserve the project's MCP server conventions and use the local validation commands.

- For new or changed requests, follow the clarification and timestamped artifact protocol in `../../AGENTS.md`.
