# mcp-server-typescript — AI Agent Context

## Overview

TypeScript-based MCP server reference implementation. This project is a focused protocol server and should be treated as a library/tooling project.

## Stack

- TypeScript
- Node.js/Bun-compatible runtime
- MCP protocol server implementation

## AI Guidance

- Follow the project's TypeScript and MCP protocol conventions exactly.
- Keep protocol contracts, schemas, and tool definitions well-documented.
- Prefer small server changes with explicit validation of protocol behavior.
- Update README or server docs when tool names, request payloads, or auth contracts change.
