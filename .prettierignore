# Prettier ignore — only format project code, not the entire workspace
# Runtime/agent artifacts that prettier should never touch
.omo/
.omo/**
.hermes/plans/**/*.json
.hermes/plans/**/*.jsonl
# Runtime-generated artifacts (auth_inventory, package_inspector, fanout reports)
scripts/.runtime/
# Judge-skill CLI output (regenerated each session)
judge_results/
# Root workspace files we want to check
!packages/openrouter-client/**
!packages/openrouter-client/**
# Everything else is ignored by default
.github/**
.hermes/**
.vscode/**
.copilot/**
projects/**
docs/**
