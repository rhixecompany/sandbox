{
  "inputs": [
    {
      "type": "promptString",
      "id": "workspaceFolder",
      "description": "Workspace root path"
    }
  ],
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    }
  }
}
