# Webhook adapter environment variables (reference — copy to ~/.hermes/.env)
# Verified from docs §Environment Variables table.
WEBHOOK_ENABLED=true
WEBHOOK_PORT=8644
WEBHOOK_SECRET=global-fallback-secret-placeholder  # REPLACE in production; not for commit
# Per-route secrets configured in config.yaml; this is the fallback only.
# For production: rotate secrets; never use INSECURE_NO_AUTH on non-loopback interfaces.
