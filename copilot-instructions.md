# Copilot Context Pointer — SandBox

The canonical repository context is [`AGENTS.md`](AGENTS.md). The GitHub Copilot adapter is [`.github/copilot-instructions.md`](.github/copilot-instructions.md); Hermes-specific overrides live in [`.hermes.md`](.hermes.md). Workspace MCP mappings live in [`.github/mcp.json`](.github/mcp.json).

Read the nearest `AGENTS.md`, project manifest, and relevant tests before editing. Treat `projects/*` as autonomous subprojects, use their local commands, preserve unrelated worktree changes, protect secrets, and verify the exact requested behavior.
