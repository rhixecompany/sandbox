/goal /scope /audit /architect /develop repair, verify, and enhance $HERMES_HOME/config.yaml to ensure it is fully functional, error-free, and optimized for performance. Use the following steps:

1. Use clarify tool to resolve unknown or confusing requirements for the goal and its subgoal, then close the remaining open items.
2. Use the applicable planning, implementation, debugging, and verification tools and skills: /goal, /using-superpowers, /brainstorming, /user-communication-preferences, /mcp-sequential-thinking, /mcp-filesystem, /mcp-ast-grep, /mcp-memory, /writing-clearly-and-concisely, /subagent-driven-development, /plan, /plan-mode, /plans-and-specs, the implementation plan/spec/prompt commands, and /executing-plans, /executing-specs, and /executing-prompts.
3. Create, update, or refactor the implementation specs, plans, prompts, scripts, and skills needed to fully implement and execute the subgoal in the fastest practical parallel order.
4. Include the required steps, phases, rules, tasks, actions, timelines, milestones, and resource allocation. Use the available Hermes tools and skills and follow the approved authorization for destructive operations.

/goal /scope /audit /architect /develop define the exact goals, scope, and audit requirements for repairing, verifying, and enhancing $HERMES_HOME/config.yaml; use /debug and /systematic-debugging to resolve all identified conflicts, issues, errors, and warnings, then verify the configuration with hermes doctor and hermes config.

/goal /scope /audit /architect /develop search for all {SOUL|USER|MEMORY}.md at ./. merge all found files into $HERMES_HOME/SOUL.md, $HERMES_HOME/memories/{USER|MEMORY}.md and delete ./{SOUL|USER|MEMORY}.md and $HERMES_HOME/{USER|MEMORY}.md.

/goal /scope /audit /architect /develop create,update,tests the three distinct skills: /writing-spec, /writing-plan, and /writing-prompt. For writing-spec first create a folder at $HERMES_HOME/specs/{spec-filename-timestamp-subject}/ all the spec files,scripts should be in that folder the spec itself must contain valid code-blocks,create,verify,execute all its custom python,typescript via either a powershell-scripts or a bash-scripts. For the writing-plan skill it first create a folder at $HERMES_HOME/plans/{plan-filename-timestamp-subject}/ all the plan must live here with steps,goal,subgoal,todos,phases,tasks,subtasks,gates,checklists,actions and any needed,optional,recommended specs created,verified,linked,executed. For the writing-prompt first create a folder at $HERMES_HOME/prompts/{prompt-category}/{prompt-trigger} all the prompt files and its templates files must live there with plans, formatters,rules,skills,best-practices,deps-core,section-skeleton,,milestones, personas, profile, personality, model.

/goal /scope /audit /architect /develop update protocol skills such as multi-file-change-protocol,identity-context-protocol,multi-file-crud-protocol skills to use /goal /scope /audit /architect /develop /debug /sync /using-superpowers /brainstorming /user-communication-preferences /mcp-sequential-thinking /mcp-filesystem /mcp-ast-grep /mcp-memory /writing-clearly-and-concisely /subagent-driven-development /systematic-debugging /plan /plan-mode /plans-and-specs /implementation-{skill|specs|plans|prompts} /update-implementation-{skill|specs|plans|prompts} /create-implementation-{skill|specs|plans|prompts} /executing-{specs|plans|prompts} /{skill|specs|plans|prompts}-judge /writing-{spec|plan|prompt} .
