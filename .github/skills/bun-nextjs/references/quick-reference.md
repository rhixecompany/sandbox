# Bun + Next.js Quick Reference

## Common Commands
```bash
bun create next-app
bun run dev
bun run build
bunx shadcn-ui@latest init
```

## Key Features
- Bun is the runtime + package manager + bundler
- Uses `bun run` instead of `npm run`
- Native TypeScript support, no ts-node needed
