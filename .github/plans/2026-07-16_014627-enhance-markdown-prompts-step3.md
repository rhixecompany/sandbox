# Enhance Markdown for All Prompts – Step 3

**Goal**: Continue the full‑pass enhancement of every prompt in `./prompts`.

**State after Step 2**

- The front‑matter validator now correctly skips template files.
- Ran the validator and found a single offending prompt (`update-implementation-plan.prompt.md`).
- Patched that file with a proper YAML front‑matter block (see diff).  The file now begins with a valid mapping containing all required fields (`name`, `description`, `version`) and optional fields.

**Next Concrete Action**

1. **Re‑run the validator** to confirm no remaining errors.
2. If the output is `All prompt front‑matter valid.`, we can move to the next phase of the plan:
   - Deduplication of prompts (identify exact or near‑duplicate bodies).
   - Consolidation of shared sections into `prompts/templates/_shared/`.
   - Enhancement of tags, toolsets, and script references.
   - Final validation via `skill‑judge` and `dry_run_prompts.py`.
3. Record the successful validation in the master plan and update the plan status to `phase‑2‑validated`.

**If validation fails** (any remaining prompts report missing fields), we will:

- Generate a per‑file patch (similar to the one applied to `update-implementation-plan.prompt.md`).
- Append those patches to the plan under a new "Front‑Matter Fixes" section.
- Re‑run validation after applying them.

**Risk & Mitigation**

- *Risk*: Some prompts might still be missing optional fields like `tags` or `toolsets`. *Mitigation*: The validator will flag them; we will add empty lists (`tags: []`, `toolsets: []`) to keep the schema happy.
- *Risk*: Patches could unintentionally overwrite custom content. *Mitigation*: All changes are recorded in the plan; the user can review the diff before execution.

**Open Questions**

- Should we enforce a canonical ordering of front‑matter keys (e.g., `name`, `title`, `description`, `version`, `trigger`, `tags`, `dependencies`, `skills`, `metadata`)?
- Do we want to auto‑generate `tags` based on filename keywords (see `references/prompt-tag-inference.md`), or leave them manual?

**Plan status**: `phase‑2‑validation‑pending`

---
*Generated by the `plan` skill on 2026‑07‑16 at 01:46:27+01:00.*
> **Execution:** Use the  skill to dispatch tasks via subagents with two-stage review (spec compliance → code quality).

> Execution: Use the `subagent-driven-development` skill to dispatch tasks via subagents with two-stage review (spec compliance to code quality).
