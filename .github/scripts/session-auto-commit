#!/usr/bin/env bash
# Wrapper: delegates to hooks/session-auto-commit/hook.sh using native Windows paths
set -euo pipefail
LOCALAPPDATA="${LOCALAPPDATA:-C:/Users/Alexa/AppData/Local}"
HOOK_SH="$LOCALAPPDATA/hermes/hooks/session-auto-commit/hook.sh"
cd "$LOCALAPPDATA/hermes"
exec bash "$HOOK_SH"
