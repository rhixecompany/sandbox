{
  "name": "session-logger",
  "version": "1.2.0",
  "description": "Logs Hermes session lifecycle and prompt events for replay and audit. Start and end capture resolve user, profile, model, provider, working dir, host snapshot, and git state from the runtime payload; end capture derives status, duration, and turns from wire fields + accumulated JSONL.",
  "events": ["on_session_start", "on_session_end", "pre_llm_call"],
  "entrypoint": "hook.sh",
  "config": {
    "log_dir": "C:/Users/Alexa/AppData/Local/hermes/logs/sessions",
    "retention_days": 30
  }
}
