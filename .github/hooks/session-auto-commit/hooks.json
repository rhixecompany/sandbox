{
  "name": "session-auto-commit",
  "version": "1.0.1",
  "description": "Automatically commits session changes at session end. Resolves the repo from the envelope cwd field.",
  "events": ["on_session_end"],
  "entrypoint": "hook.sh",
  "config": {
    "commit_message_prefix": "chore(session): ",
    "include_untracked": false
  }
}
