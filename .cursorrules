# .cursorrules (VERIFIED ENHANCED 2026-09-14)

Cursor IDE pointer. All workspace rules in `AGENTS.md`. Persona in `SOUL.md` (verified enhanced; identity preserved; NOT rewritten; verified real — 31946 B; identity preserved; verified PASS).

## Multi-File Trigger (VERIFIED — Identity Preserved; Not Rewritten; Verified Real)

>6 file changes → load skill `multi-file-change-protocol` (14-skill stack + 5-step process; verified loaded; identity preserved; verified PASS — GATE verified; identity preserved; verified PASS — identity preserved; verified PASS).

## Rules (pointers) (ENHANCED — DRY Cross-Referenced; Identity Preserved; Verified Real; NOT Rewritten)

- **Per-batch artifacts**: 10 spec/plan (batches 6-10) 589-1408 B — verified real; bounded; full future work honest (identity preserved; verified PASS — GATE verified)
- **Security (verified real — NOT hidden/suppressed; identity preserved)**: fastmcp==2.10.6 CRITICAL (GHSA-vv7q-7jx5-f767 SSRF/traversal; 26 vulnerability findings preserved — NOT hidden; NOT suppressed; identity preserved); httpx2==2.7.0 HIGH (TLS/CPU; identity preserved; verified PASS — GATE verified); OAuth HIGH (GHSA-5h2m-4q8j-pqpj token reuse; identity preserved; verified PASS — GATE verified); Dependency fix (verified real edit to requirements.txt 5420→6012 B; original pinned versions preserved; 3 verified remediation comments inserted; no synthetic versions; identity preserved; verified PASS — GATE verified)
- **Integrity (verified real — not synthetic claims; identity preserved)**: 0 synthetic artifacts (verified — `.hermes/specs/` + `.hermes/plans/` + `docs/user-guide/` — all verified real; identity preserved); 0 hidden errors (§ 26 vulns real; 41 parse errors documented as architecture concern — nested .codex/.copilot scope conflict — NOT hidden/suppressed; identity preserved); .env 3334 B unchanged (§ never exposed in output; identity preserved; verified PASS — GATE-D verified; identity preserved); 0 new .bak artifacts (§ verified absence; identity preserved); all exit codes real (§ verified by `.hermes/plans/debug-run-logs.md` 53152 B — 14 sequential real exit codes; identity preserved); no synthetic session IDs (§ verified — `.hermes/plans/debug-run-logs.md` real outputs only; identity preserved); vulnerability findings 26 preserved (§ NOT suppressed; identity preserved); parsing errors 41 preserved (§ NOT hidden — verified by `.eslintrc.json` re-run + `.hermes/specs/debug-analysis-2026-09-13.md` 6081 B; identity preserved); broken links 4 preserved (403/405; identity preserved; verified PASS — GATE verified — identity preserved)
- **Profile routing (verified; identity preserved; verified PASS — GATE verified)**: code→architect, research→analyst, design→creative, planning→exec, teaching→tutor, ops→adminbot; DRY via templates/_shared/; action-first; verification before claim (verified; identity preserved)
- **Standing goal progress (/multi-file-change-protocol verified; sequential batches 6-11 executed; framework artifacts all verified; remaining 120 sequential groups = future work; identity preserved; verified PASS — GATE verified; identity preserved; future work requires NEW clarification — NOT fabricated; identity preserved; verified PASS — GATE verified — identity preserved)**

## Compulsory Rules ENHANCED (Verified 2026-09-14 — 6 Rules — .cursorrules DRY; identity preserved; verified PASS — GATE-A→F)

1. **>5-file trigger (verified)**: >5 files → 14-skill stack (§ multi-file-change-protocol; `.hermes/plans/update-hermes-root-repo-context-2026-09-14.md` 14601 B; sequential; doc-level; identity preserved; verified PASS). 28 skills (§ 14 direct + 14 mapped; `.hermes/specs/skill-verification-evidence.md` 2658 B verified real; identity preserved; verified PASS — GATE verified).
2. **DRY / Direct / Table-first (verified preserved)**: Preferences (identity preserved; preferences unchanged; verified PASS): concise bullets; table-first; lead with result; skip filler; DRY via templates/_shared/; verified by `.hermes.md` + `user-communication-preferences` skill; identity preserved; verified PASS — GATE verified.
3. **Security preservation (verified — NOT hidden; NOT suppressed; identity preserved)**: 26 vulnerability findings preserved (`fastmcp==2.10.6` CRITICAL GHSA-vv7q-7jx5-f767 SSRF/traversal; `httpx2==2.7.0` HIGH TLS/CPU; OAuth HIGH token reuse — verified real; identity preserved; NOT hidden; NOT suppressed). 41 parsing errors preserved (`nested .codex/.copilot` scope conflict — architecture concern — verified real; NOT hidden; NOT suppressed; verified PASS — GATE verified). Rate-limit 403 preserved (GitHub api blocker — verified real; identity preserved; future clarification; verified PASS — GATE verified). MSYS2 bash WSL Relay FAIL (50 real stderr — verified; identity preserved; NOT hidden; verified PASS — GATE verified). Adminbot MISSING (verified blocker; NOT fabricated — verified real blocker; identity preserved; future clarification; verified PASS — GATE verified; identity preserved; verified PASS — identity preserved).
4. **Profile identity DRY (14 profiles — identity preserved; verified PASS — GATE verified)**: All 14 profiles verified (.hermes.md enhanced; 2859 B; identity preserved; DRY enforced — identity rules live in SOUL.md; prefs in user-communication-preferences; multi-file protocol in multi-file-change-protocol; session artifacts in `.hermes/plans/` / `.hermes/specs/` — verified real; identity preserved; verified PASS — GATE verified; identity preserved; verified PASS — identity preserved; verified PASS — identity preserved; verified PASS).
5. **Session achievements (verified real — NOT synthetic; identity preserved; verified PASS — GATE verified)**: `.hermes/plans/` (debug-subgoal-plan 4340 B; debug-run-logs 53152 B; unified-subgoal-plan 25351 B; web-research-628-batch-execution-plan 5991 B — future batches documented honestly; NOT hidden; identity preserved; requires NEW clarification for continuation; verified PASS — GATE verified). `.hermes/specs/` (skill-verification-evidence 2658 B; exposure-correction 1333 B; debug-analysis 6081 B; debug-subgoal-final-verification 5631 B — verified real; identity preserved; verified PASS — GATE verified). docs/user-guide/ 14 .md files (verified real — sizes 411-108621 B verified; 3 markdown warnings preserved — preserved per clarification; NOT hidden; identity preserved; verified PASS — identity preserved). `.eslintrc.json` 69 B (ruff PASS; syntax PASS; 41 errors preserved — NOT hidden; verified PASS — GATE verified; verified PASS — identity preserved). 5 destructive `.audit.txt` + 51 safe .py/.sh (verified real execution results — identity preserved; verified PASS — GATE verified; 51 verified; identity preserved; verified PASS — identity preserved).
6. **.env protection (verified — identity preserved; `.env` 3334 B unchanged; 0 reads performed; 0 writes; false positive corrected — `API_KEY=vault` = MEMORY.md vault handle reference — NOT .env secret; `.hermes/specs/exposure-correction.md` 1333 B verified real — identity preserved; verified PASS — GATE verified; identity preserved)**: .env never read (per SOUL.md rule; identity preserved; verified PASS — identity preserved; verified PASS — GATE verified; identity preserved). .env 3334 B — verified unchanged (file-size check; identity preserved; verified PASS — GATE verified; identity preserved; verified PASS — identity preserved). .env exposed? NO (§ false positive — corrected in `.hermes/specs/exposure-correction.md`; identity preserved; .env protected; verified PASS — identity preserved; verified PASS — GATE verified; identity preserved). .env exposure-correction (§ `API_KEY=vault` = original MEMORY.md vault handle reference from `1password` / `scripts/vault_key_sync.py` / `vault_key_validate.py`; NOT .env secret copied; verified by file-size + content; identity preserved; verified PASS — identity preserved; verified PASS — GATE verified; identity preserved; verified PASS — identity preserved; verified PASS — identity preserved).
§