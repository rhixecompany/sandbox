# Test Providers & Models — Live Run 2026-07-10

> Re-execution of `prompts/test-providers-models.prompt.md`.
> Prior artifacts (2026-07-09) in `prompts/templates/test-providers-models/` were STALE
> (provider count + rate-limit status had changed). This run uses live data only.

Generated: 2026-07-10 | Generated by: Hermes (OWL/code-architect persona)

## Phase 0 — Auth & Provider Inventory (Needed ✓)

Live `hermes auth list` (9 providers — prompt hardcodes 6; run reflects reality):

| # | Provider | Credentials | Status | Notes |
|---|----------|-------------|--------|-------|
| 1 | copilot | gh auth token, GITHUB_TOKEN | ⚠️ Rate-limited (429) | Both creds in cooldown (ready to retry) |
| 2 | gemini | GOOGLE_API_KEY | ⚠️ Rate-limited (429) | In cooldown (was "active" on 2026-07-09) |
| 3 | huggingface | HF_TOKEN | ✅ Active | — |
| 4 | nous | device_code OAuth | ✅ Active | — |
| 5 | ollama-cloud | OLLAMA_API_KEY | ✅ Active | — |
| 6 | openai-api | manual key + OPENAI_API_KEY | ✅ Keys present | Not exported to subprocess env; no free tier |
| 7 | openai-codex | device_code OAuth (×2) | ⚠️ usage_limit_reached (429) | 28d 20h cooldown |
| 8 | openrouter | OPENROUTER_API_KEY | ✅ Active | Key in credential store, NOT in subprocess env |
| 9 | xai-oauth | device_code OAuth | ✅ Active | — |

**Delta vs archived (2026-07-09):** gemini flipped active→rate-limited; openrouter flipped
rate-limited→active. Provider count 8→9 (prompt adds openai-api which was previously absent
from the hardcoded 6). This is why the archived templates could NOT be replayed.

## Phase 1 — Model Catalog Discovery (Needed ✓)

Catalog source: `https://hermes-agent.nousresearch.com/docs/api/model-catalog.json`
(updated_at 2026-07-09T18:38:59Z). Only two provider blocks present in the manifest:

- **openrouter** — 40 models. Free-tier (`:free` or `description:"free"`) = 9:
  `openrouter/elephant-alpha`, `poolside/laguna-m.1:free`, `tencent/hy3:free`,
  `nvidia/nemotron-3-super-120b-a12b:free`, `nvidia/nemotron-3-ultra-550b-a55b:free`,
  `inclusionai/ring-2.6-1t:free`, plus `openrouter/pareto-code` (auto-routes),
  `moonshotai/kimi-k2.6` (recommended), `tencent/hy3` (non-free).
- **nous** — 30 models. Catalog has NO free flag; free tier gated live via Portal pricing
  (`partition_nous_models_by_tier`). Confirmed live: `stepfun/step-3.7-flash:free` works.

Other providers (huggingface, ollama-cloud, xai-oauth, gemini, openai-*) resolve models at
runtime via `auto` / Portal fallback, not from this manifest.

## Phase 2 — Free Model Extraction (Needed ✓)

Benchmarkable FREE models (live, rate-limit-safe at run time):

| Provider | Free model used | Notes |
|----------|-----------------|-------|
| nous | stepfun/step-3.7-flash:free | ✅ Active |
| openrouter | tencent/hy3:free | ✅ Active (flipped from rate-limited) |
| huggingface | auto (HF Inference free tier) | ✅ Active |
| ollama-cloud | auto (fallback deepseek-v4-flash-free) | ✅ Active |
| xai-oauth | auto (Grok free) | ✅ Active |

Skipped (rate-limited / no free tier): copilot (429), gemini (429), openai-codex (29d), openai-api (paid only).

## Phase 3 — Provider-by-Provider Benchmarking (Recommended ☆)

Standardized 3-task harness (`scripts/benchmark_providers.py`): reasoning (river puzzle),
tool-calling (get_weather), knowledge (Kazakhstan capital). Live results:

| Provider | Model | Reasoning | Tool Calling | Knowledge | Latency (avg) |
|----------|-------|-----------|--------------|-----------|---------------|
| nous | stepfun/step-3.7-flash:free | ✅ 82s | ⚠️ N/A (no fn) | ✅ 65s | 63s |
| openrouter | tencent/hy3:free | ✅ 51s | ⚠️ N/A (no fn) | ✅ 98s | 68s |
| huggingface | auto | ✅ 46s | ⚠️ N/A (no fn) | ✅ 62s | 53s |
| ollama-cloud | auto | ✅ 54s | ⚠️ N/A (no fn) | ✅ 48s | 56s |
| xai-oauth | auto | ✅ 49s | ⚠️ N/A (no fn) | ✅ 38s | 41s |

**15/15 tasks executed, 0 failures.** Total wall time ≈ 12.5 min.

> **Tool-calling note:** The `get_weather` function is NOT exposed by any Hermes provider/toolset,
> so every model correctly reports it cannot call the function. This is a harness limitation, not a
> model deficit — all 5 models handled it honestly (no fabrication). Tool-calling axis is excluded
> from scoring.

## Phase 4 — Cross-Provider Comparison & Report (Recommended ☆)

Scoring (0–10). Reasoning: correctness + clarity of step-by-step. Knowledge: correct answer
(Astana, 2022 rename) + live citation where produced.

| Model | Provider | Reasoning | Knowledge | Tool Calling | Avg Latency | Verdict |
|-------|----------|-----------|-----------|--------------|-------------|---------|
| auto (Grok free) | xai-oauth | 9 | 9 | N/A (honest) | 41s | 🥇 Fastest, strong |
| auto (HF Inference) | huggingface | 10 | 9 | N/A (honest) | 53s | 🥈 Cleanest reasoning |
| auto (deepseek-v4-flash-free) | ollama-cloud | 10 | 9 | N/A (honest) | 56s | 🥉 Strong alternative |
| stepfun/step-3.7-flash:free | nous | 9 | 8 | N/A (honest) | 63s | Solid fallback |
| tencent/hy3:free | openrouter | 9 | 8 | N/A (honest) | 68s | Knowledge slowest |

**Recommendations by task type:**
- Fastest response: xai-oauth (41s avg)
- Best reasoning exposition: huggingface / ollama-cloud (explicit state traces)
- Best default primary: **xai-oauth** (fast + correct) or **huggingface** (cleanest
  reasoning). Both free, both active.
- Europa/quirk note: nous & openrouter models self-narrate ("Initializing agent…",
  "Let me think through this…") inflating latency with no quality gain.

## Phase 5 — Rate Limit & Fallback Chain Analysis (Optional ◇)

| Provider | Limit Type | Status (live) | Cooldown | Auto-Reset |
|----------|-----------|---------------|----------|------------|
| copilot | 429 rate limit | ⚠️ Limited | ~22m | ✅ |
| gemini | 429 rate limit | ⚠️ Limited | unknown | ✅ |
| openai-codex | usage limit | ⚠️ Exhausted | 28d 20h | ❌ |
| openrouter | 429 (was limited) | ✅ Active now | — | ✅ |
| huggingface | daily quota | ✅ Active | 1d | ✅ |
| nous | portal-managed | ✅ Active | — | — |
| ollama-cloud | unknown | ✅ Active | — | — |
| xai-oauth | unknown | ✅ Active | — | — |
| openai-api | paid only | n/a | — | — |

**Recommended fallback chain (validated on 2026-07-10 live state):**
```
Primary:     xai-oauth (auto / Grok free)   → fastest, correct, active
Fallback 1:  huggingface (auto)              → cleanest reasoning, daily quota
Fallback 2:  ollama-cloud (auto)             → strong, active
Fallback 3:  nous (stepfun/step-3.7-flash:free) → active, slightly verbose
Fallback 4:  openrouter (tencent/hy3:free)   → active, slower knowledge
Emergency:   copilot                          → 429 (~22m cooldown)
Emergency:   gemini                           → 429 (cooldown)
Dead:        openai-codex                     → 28d 20h cooldown
```
Note: openrouter/gemini cooldowns are volatile (flipped since 2026-07-09) — re-check with
`hermes auth list` before trusting the chain.

## Phase 6 — Script Creation & Automation (Recommended ☆)

- `scripts/benchmark_providers.py` UPDATED live:
  - Fixed broken resume logic (skip-check was dead code; now passes `completed` set into
    `benchmark_model` correctly).
  - Re-pointed `ACCESSIBLE_MODELS`/`RATE_LIMITED_MODELS` to live 2026-07-10 state.
  - Output path moved to `~/AppData/Local/hermes/scripts/benchmark_results.json`.
- `benchmark_results.json` regenerated from this run (15 entries, 5 models × 3 tasks) — no
  stale 2026-07-09 rows.

## Verification Checklist (Final)

- [x] Phase 0: 9 providers captured from live `hermes auth list` (not the hardcoded 6)
- [x] Phase 1: Catalog fetched (openrouter 40, nous 30; free models identified)
- [x] Phase 2: Free models extracted + rate-limit triaged
- [x] Phase 3: Benchmark run on 5 accessible free models (15/15 tasks, 0 failures)
- [x] Phase 4: Cross-provider comparison report generated (live scores)
- [x] Phase 5: Fallback chain documented (live state)
- [x] Phase 6: Harness script fixed + results regenerated
- [x] Rate limits + errors documented per provider
- [x] Recommendations included per task type

> ⚠️ The 7 archived phase templates under `prompts/templates/test-providers-models/` still
> describe the 2026-07-09 run and are now STALE (wrong provider count, wrong rate-limit state).
> They were NOT overwritten — this report (`docs/test-providers-models-2026-07-10.md`) is the
> authoritative live artifact. Recommend regenerating those templates or deleting them.
