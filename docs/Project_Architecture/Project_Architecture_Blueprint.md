# SandBox Workspace — Architecture Blueprint

## Project Overview

- **Project Name:** SandBox Workspace
- **Project Type:** Multi-project Monorepo Workspace
- **Architecture Pattern:** Workspace Orchestration
- **Technology Stack:** Bun/TypeScript, PowerShell, Git
- **Package Manager:** Bun 1.3.14+
- **Node Version:** >= 18

## Technology Stack Details

| Category | Technology |
|---|---|
| Runtime | Bun 1.3.14+, Node.js >= 18 |
| Language | TypeScript (strict), Shell (bash), PowerShell |
| Linting | ESLint 10.x, Prettier 3.x, Markdownlint, CSpell |
| Type Checking | TypeScript (tsc --noEmit) |
| Testing | Vitest 4.x, Playwright |
| Git Hooks | Husky 9.x, lint-staged |
| CI/CD | GitHub Actions |

## Directory Structure

```
SandBox/
├── AGENTS.md              # Workspace context & conventions
├── .hermes/               # Hermes AI config, plans, profiles
├── .github/               # Copilot config, hooks, prompts
├── Bash/                  # Primary automation toolkit
│   ├── scripts/           # PowerShell orchestration scripts
│   ├── Banking/           # Banking sub-project scripts
│   ├── tests/             # Shell test suite
│   └── docs/              # Documentation
├── Resume_maker/          # Job docs generator
├── projects/              # 15+ sub-projects workspace
│   ├── Banking/           # Next.js fintech app
│   ├── comicwise/         # Next.js comic streaming
│   ├── cookiecutter-django-tailwind/  # Django template
│   ├── Django-Scrapy-Selenium/        # Scraping platform
│   ├── docs/              # Documentation-only project
│   ├── ecom/              # Django + React ecommerce
│   ├── profile/           # Django blog/CMS
│   ├── Python-projects/   # Python beginner scripts
│   ├── rhixe_scans/       # Next.js comic reader
│   ├── rhixecompany-comics/ # Django + Next.js dual-stack
│   ├── selenium_webdriver/ # Node.js Selenium scraper
│   ├── university-libary-jsm/ # Next.js library mgmt
│   ├── xamehi.tv/         # Django + React streaming
│   ├── xamehi/            # Django + Express + React
│   └── youtube-downloader/ # Python CLI downloader
├── docs/                  # Extracted documentation
├── prompts/               # AI prompt assets
└── reports/               # Session & audit reports
```

## Architectural Patterns

### 1. Workspace Pattern
The root is a multi-project workspace managed via Git submodules. Each sub-project lives under `projects/` with its own `AGENTS.md` context file and independent technology stack.

### 2. Toolkit Orchestration
`Bash/` contains PowerShell scripts that orchestrate tooling across the workspace — discovery, cloning, triage, cross-referencing, and verification phases.

### 3. DRY Context Hierarchy
Context flows via: `SOUL.md` → `USER.md` → `AGENTS.md` → `.hermes.md`, with global rules in `PROJECT_RULES.md` and `MASTER_RULES.md`.

## Data Flow

```
User Request → Hermes Agent → Tool Execution
    ├── Bash/ (automation scripts)
    ├── projects/ (project-specific tasks)
    └── docs/ (documentation output)
```

## Key Dependencies

- **Runtime:** `bun`, `node`, `python3`
- **Dev Tools:** `eslint`, `prettier`, `typescript`, `vitest`, `playwright`
- **Scripting:** `tsx` (TypeScript execution), PowerShell modules

## Architecture Decisions

1. **Bun-first tooling** — All TypeScript projects use Bun for package management and script execution
2. **Per-project AGENTS.md** — Each project has an independent context file for AI-assisted development
3. **Git submodules for isolation** — Projects maintain independent git histories
4. **Hermes profiles for routing** — Task type determines which AI profile/model handles the request
5. **Documentation-first** — Architecture blueprints, tech stacks, and folder structures documented per project

## Extensibility Points

- New projects added under `projects/` with `AGENTS.md`
- New orchestration scripts in `Bash/scripts/`
- New Hermes profiles in `.hermes/profiles/`
- New CI workflows in `.github/workflows/`

---

*Generated by architecture-blueprint-generator — comprehensive analysis*
