# Scripts

Canonical location: `%LOCALAPPDATA%/hermes/scripts` (i.e. `~/AppData/Local/hermes/scripts`).
This repo no longer carries duplicate copies — see `.github/prompts/repo-tooling-implementation.prompt.md`.
