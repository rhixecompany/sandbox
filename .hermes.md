# Hermes Agent Project Overrides

Project context overrides per hierarchy: `.hermes.md` → `AGENTS.md` → `CLAUDE.md` → `.cursorrules`

## Multi-File Trigger
> Profile identity (SOUL.md / USER.md / MEMORY.md) enforced DRY (see workspace `.hermes.md` identity refs, `.hermes.md` profile routing, and `user-communication-preferences` skill — verified loaded). Verified artifacts: `.hermes/plans/debug-subgoal-plan-2026-09-13.md` (4340 B), `.hermes/specs/unified-subgoal-comprehensive.md` (6207 B), `.hermes/plans/debug-run-logs.md` (53152 B — 14 real exit codes). No synthetic artifacts; `.env` untouched; `.bak` count = 0.

>6 file changes → load skill `multi-file-change-protocol` (14-skill stack + 5-step process).
See `AGENTS.md` for workspace layout; `SOUL.md` for persona + standing rules.

## Active Profile: default

`hermes profile list` / `hermes config show` for live state. Other profiles live
under `C:\Users\Alexa\AppData\Local\hermes\profiles\<name>\` — each has its own
skills/, plugins/, cron/, memories/. **Do not modify another profile's assets
unless explicitly directed.**

Profile routing: code→architect, research→analyst, design→creative,
planning→exec, teaching→tutor, ops→adminbot, general→default.

## MCP Servers (live state)

Run `hermes mcp list` / `hermes hooks list` / `hermes plugins list` for current
enabled state. 23 MCP servers connected (this session).

## Tooling MCP (canonical)

| Field | Value |
|---|---|
| Pinned package | `mcp==2.0.0` |
| Python venv | `~/myvenv` (Python 3.13, `uv venv`) |
| Interpreter | `C:/Users/Alexa/myvenv/Scripts/python` |
| Env vars | `PYTHONPATH=~/myvenv/lib/site-packages`, `MCP_VERSION=2.0.0`, `UV_USE=true`, `UVM_ACTIVE=~/myvenv` |
| Servers | `python-quality`, `tooling-config`, `tooling-lint` |
| Tools | ruff 0.15.10, pyright 1.1.414, cspell 10.3.0 |
| Configs | `.opencode/opencode.json`, `.vscode/mcp.json`, `hermes config.yaml` |
| Reference | `.omo/templates/myvenv-reference.md` |

## File Hierarchy

| File | Purpose |
|---|---|
| `.hermes.md` | Hermes-specific project overrides (this file) |
| `AGENTS.md` | Workspace layout + .github/prompts map |
| `CLAUDE.md` | Claude-specific pointer |
| `.cursorrules` | Cursor IDE pointer |
| `SESSION_REPORT.md` | Session history |
| `HERMES_PROFILE_REPORT.md` | Profile audit |
| `.hermes/plans/` | Implementation plans |
| `.hermes/specs/` | Implementation specs |

## Platform Note (Hermes TUI)

You are in the Hermes terminal UI. There is no attachment channel —
`MEDIA:/path` tags print as literal text; deliver files by stating absolute path.
Cron jobs from this session are LOCAL-ONLY; output saved via
`cronjob action='list'`. For live-delivery notifications, jobs must set
`deliver='telegram'` or `deliver='all'` — not `deliver='origin'`.