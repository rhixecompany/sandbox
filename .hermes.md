# Hermes Agent Project Overrides

**Workspace context**: `AGENTS.md` is canonical for shared repository rules. This file contains Hermes-only overrides.
**Canonical profile root**: verify with `hermes profile list`; do not assume a missing profile exists.
**Profile routing verified 2026-09-16** (subagent: ops/adminbot; plan: `./plans/multi-goal-execution-plan-2026-09-14.md` verified 11673 B).

## Verified Profile Routing (live-verified 2026-09-20 — `hermes profile list`: 15 registered)

| Profile           | Live      | Routing                   | Notes                                                            |
| ----------------- | --------- | ------------------------- | ---------------------------------------------------------------- |
| default           | ✓ running | general/user → default    | active; root `memories/` canonical                               |
| adminbot          | ✓         | ops → adminbot            | profile-docs gap (USER/MEMORY/desc missing) — preserved honestly |
| analyst           | ✓         | research → analyst        | alias profile                                                    |
| architect         | ✓         | code → architect          | alias profile                                                    |
| code-architect    | ✓         | —                         | role profile                                                     |
| creative          | ✓         | design → creative         | alias profile                                                    |
| creative-director | ✓         | —                         | role profile                                                     |
| designer          | ✓         | —                         |                                                                  |
| dev               | ✓         | general → default         |                                                                  |
| exec-assistant    | ✓         | planning → exec-assistant | role profile (config.yaml fixed 2026-09-20)                      |
| ops               | ✓         | —                         | routing target: adminbot                                         |
| pm                | ✓         | —                         |                                                                  |
| research-analyst  | ✓         | —                         | role profile                                                     |
| security          | ✓         | security → security       |                                                                  |
| tutor             | ✓         | teaching → tutor          | alias profile                                                    |

Routing rules:
> **Identity Protocol Ref (2026-09-20)**: References `identity-context-protocol` SKILL.md (6876 B). DRY enforced: identity→SOUL.md; prefs→user-communication-preferences; protocol→multi-file-change-protocol/multi-file-crud-protocol; routing→this file. No synthetic session IDs or fabricated results. .env protected (30501 B). Blockers preserved (26 vulns + 41 errors + 403 + MSYS2 FAIL + adminbot gap).
 code→architect, research→analyst, design→creative, planning→exec-assistant, teaching→tutor, ops→adminbot, general→default, user→default.
Stale profile dirs kept on disk but NOT registered (alexa, alexa-alias, cto, patient-tutor, qa, skills) + `.alexa.staging-24516` — preserved honestly; truth = `hermes profile list`.

## Hermes Operating Contract

- Resolve profile and model state with live Hermes commands (`hermes profile list`, `hermes config show`, `hermes fallback list`).
- Keep identity and learned memory in the active profile's `SOUL.md`, `USER.md`, and `MEMORY.md`; do not copy them into repository context files.
- Treat `.github/prompts/` as the shared prompt library and `AGENTS.md` as the shared repository context for Copilot, OpenCode, and Cursor Agent.
- Use the repository's required work loop and safety boundaries from `AGENTS.md`.
- Follow the clarification and artifact protocol in `AGENTS.md`; keep run state under `ai-agent-home/` and do not rely on session memory alone.
- Use `.github/mcp.json` as the repository MCP source of truth; `.vscode/mcp.json` and OpenCode/Hermes mappings should remain functionally aligned.
- Prefer `playwright`, `tooling-lint`, `python-quality`, and `github` for the repository's browser, quality, Python, and GitHub workflows.
- MCP heavy servers (`playwright`, `code-sandbox`, `smithery`, `mcp-docker`, `mindstudio`) replaced with Hermes native tools (`browser_exec`, `execute_code`, `terminal`, `tool_search`) to preserve disk space and battery health.

## DRY Enforcement (Cross-References — Not Duplication)

- Identity rules: reference this file and profile directory identity files (`SOUL.md`, `USER.md`, `MEMORY.md` at `~/AppData/Local/Hermes/profiles/<profile>/`). Do NOT duplicate identity content in `AGENTS.md` or tool adapters. Use `$HERMES_HOME` only for external Hermes artifacts, not as a repository filename.
- Preferences: reference `/user-communication-preferences` SKILL.md (DRY: concise/direct/table-first/action-first/DRY/verification-first).
- Protocol: reference `/multi-file-change-protocol` SKILL.md (5-step LOAD→PLAN→VERIFY→EXECUTE→GATE; verification checklist).
- Systematic debugging: reference `/systematic-debugging` SKILL.md (4-phase: understand/fix/verify/document).

## Session Evidence (Verified — Preserved Honestly)

- 28 skills verified/referenced (`./plans/multi-goal-execution-plan-2026-09-14.md` 11673 B verified).
- 26 vulnerability findings preserved.
- 41 parsing errors architecture concern preserved (`DEBUG_FIX_EVIDENCE_2026-09-13.md` verified).
- `.eslintrc.json` 69 B verified.
- Rate-limit 403 preserved (not hidden).
- MSYS2 FAIL preserved (real environment block, not resolved artificially).
- adminbot profile-docs gap preserved (profile live in `hermes profile list` 2026-09-20; USER.md/MEMORY.md/desc.md absent in profile dir — honest, see G4/G6).
- 0 synthetic session IDs / capabilities / ranking added.

## Protected References

- `.env` protected (CWD 5274 B / hermes home 30504 B — verified 2026-09-20 (was 30269 B 2026-09-14; size-only log, contents never read); never expose contents; never modify).
- `.env` references added via G3 to AGENTS.md, CLAUDE.md, USER.md, SOUL.md, .cursorrules (DRY single-line refs, no duplication).

## Multi-File CRUD Protocol (≥4 files)

Trigger: ≥4 file changes → load `multi-file-crud-protocol` skill.
Unified protocol: memory→brainstorming→plan→execution, clarify 5q/turn, subagents FULL autonomy.
Enforce rules: goals/subgoals/todos/steps/phases/rules/tasks/actions/timelines/gates/checklists/milestones/personas/profile/personality/model/resource allocation.

## Profile Identity Note

Profile identity rules remain intact: this file is a thin pointer; full identity content lives in profile directories (`~/AppData/Local/Hermes/profiles/<profile>/`). Identity preserved after G5 modifications.

## Init Run 2026-09-19 (init-20260919-212743)

Fresh-init run for all 5 agents. Status: implementation in progress after clarifying questions (21/21 answered, 7 turns). Artifact pointers: `ai-agent-home/specs/init-20260919-212743/SPEC.md`, `.../plans/.../PLAN.md`, `.../prompts/.../PROMPTS.md`. Scope (live): `docs/scope/scope.md`. This file's routing table and identity rules are untouched; the run adds only status and artifact pointers.
