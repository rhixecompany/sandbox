# Hermes Agent Project Overrides

Project context overrides per hierarchy: `.hermes.md` → `AGENTS.md` → `CLAUDE.md` → `.cursorrules`

## Profiles

| Profile           | Model                      | Provider     | Purpose             |
| ----------------- | -------------------------- | ------------ | ------------------- |
| **default** ⬤     | deepseek-v4-flash-free     | opencode-zen | General purpose     |
| alexa             | google/gemma-4-31b-it:free | openrouter   | Operations          |
| code-architect    | google/gemma-4-31b-it:free | openrouter   | Code/debug/refactor |
| creative-director | google/gemma-4-31b-it:free | openrouter   | Design/content      |
| exec-assistant    | google/gemma-4-31b-it:free | openrouter   | Planning/admin      |
| patient-tutor     | google/gemma-4-31b-it:free | openrouter   | Tutorials           |
| research-analyst  | google/gemma-4-31b-it:free | openrouter   | Research            |

## MCP Servers (21)

- `honcho` — HTTP MCP gateway | `ast-grep` — code search/replace via npx | `code-sandbox` — Node.js jest sandbox
- `github` — GitHub API via npx | `mcp-docker` — containers via docker CLI | `memory` — persistent memory via npx
- `mindstudio` — MindStudio CLI | `neon` — HTTP Neon MCP | `playwright` — browser automation via npx
- `sequential-thinking` — reasoning via npx | `smithery` — registry via bearer token
- `python-quality` — ruff + pyright via local venv script | `tooling-lint` — eslint/prettier/markdownlint/cspell via local script
- `tooling-config` — pre-commit/git-cliff/.gitignore validation via local script
- `context7` — HTTP docs MCP | `sentry` — HTTP Sentry MCP | `tavily` — HTTP search MCP with API key
- `parallel-search` — parallel web search | `parallel-task` — parallel task execution | `fetch` — web fetch via npx | `filesystem` — file ops via npx

## Hooks (3 shared)

All hooks run via `C:/Program Files/Git/usr/bin/bash.exe`:

- `session-logger` — session start/end/pre-LLM logging
- `session-auto-commit` — automatic session commits
- `governance-audit` — governance checks on session events

## Plugins (enabled subset)

`basic`, `custom-provider`, `disk-cleanup`, `huggingface-provider`, `langfuse`, `nous`, `nous-provider`, `ollama-cloud-provider`, `openai-codex`, `openai-codex-provider`, `opencode-zen-provider`, `openrouter-provider`, `security-guidance`, `web-tavily`, `project-planning`, `context-engineering`, `gh-skills-builder`, `where-was-i`, `browser-browser-use`, `browser-browserbase`, `browser-firecrawl`, `image_gen/openai-codex`, `image_gen/xai`, `model-providers/custom`, `model-providers/huggingface`, `model-providers/nous`, `model-providers/ollama-cloud`, `model-providers/opencode-zen`, `model-providers/openrouter`, `model-providers/xai`, `observability/langfuse`, `superpowers`, `superpowers-developing-for-claude-code`, `superpowers-marketplace`, `the-elements-of-style`, `ntfy-platform`, `copilot-acp-provider`, `dashboard_auth/nous`

## Live Provider/Model State

Use live Hermes config as source of truth:

- `hermes profile list`
- `hermes config show`
- `hermes fallback list`

Hardcoded models below are accurate as of 2026-07-24. Verify with `hermes profile list` if drift is suspected.

## Toolsets (16)

`web`, `browser`, `terminal`, `file`, `code_execution`, `vision`, `image_gen`, `tts`, `skills`, `todo`, `memory`, `context_engine`, `session_search`, `clarify`, `delegation`, `cronjob`

## Python Toolchain

python3=3.13.14 · python=3.11.15 · pip→python3.11 · PEP 668=y(venv/uv) · uv · Bun=1.3.14+

## Notes

- SOUL.md loaded from `~/AppData/Local/hermes/SOUL.md` (root, NOT profiles/default/)
- AGENTS.md in subdirectories discovered progressively
- Default profile at root `config.yaml`, NOT `profiles/default/`
- Profile audit available: `HERMES_PROFILE_REPORT.md` at workspace root
- See `PROJECT_RULES.md` + `MASTER_RULES.md` for full rules
- See `CLAUDE.md` for Claude-specific guidance
- See `.cursorrules` for Cursor IDE rules

## Session Startup Reference

- Read `SESSION_REPORT.md` first
- Load 5 mandatory skills: using-superpowers, user-communication-preferences, session-audit-report, hermes-profiles, validate-memories
- Run `hermes profile use <name>` per task type
- Profile routing: code→architect, research→analyst, design→creative, planning→exec, teaching→tutor, ops→alexa, general→default

## File Hierarchy

| File                       | Purpose                           |
| -------------------------- | --------------------------------- |
| `.hermes.md`               | Hermes-specific project overrides |
| `AGENTS.md`                | General agent guidance            |
| `PROJECT_RULES.md`         | Workspace-level rules             |
| `MASTER_RULES.md`          | Universal agent rules             |
| `CLAUDE.md`                | Claude-specific guidance          |
| `.cursorrules`             | Cursor IDE rules                  |
| `SESSION_REPORT.md`        | Session history                   |
| `HERMES_PROFILE_REPORT.md` | Profile audit                     |
