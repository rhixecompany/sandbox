# Plugin Audit Plan — 2026-09-23

**Slug:** plugin-audit-20260923-230937  
**Profile:** default | **Workspace:** ~/playgrounds/SandBox  
**Author:** Alexa  
**Status:** running | **Phase:** EXECUTE (post-VERIFY)

## Goal

Audit, fix, verify, and document all installed Hermes plugins (core + desktop). Create comprehensive bash script for cursor-agent + agent. Enable compatible; disable/uninstall incompatible. Preserve .env untouched.

## Context (verified live)

| Item | Value | Evidence |
|------|-------|----------|
| Workspace path | ~/playgrounds/SandBox (`/c/Users/Alexa/playgrounds/SandBox`) | `pwd` |
| Hermes home | ~/AppData/Local/Hermes (`/c/Users/Alexa/AppData/Local/Hermes`) | `ls` |
| Workspace `.env` | MISSING (preserved; contents never read/modified) | `ls .env` |
| Hermes `.env` | 30501 B (verified 2026-09-23) | `ls -la ~/AppData/Local/Hermes/.env` |
| Plugins installed | ~30 plugin dirs (`~/AppData/Local/Hermes/plugins/`) | `ls` (100 entries) |
| Desktop plugins | 42 total, 15 VALID, 4 BROKEN, 23 WARN (`desktop-plugin-audit.json`) | `python3 json.load` |
| Plugin catalog | `plugin-catalog.json` (186471 B) — 30+ entries | `cat` |
| Plugin compat | `.plugin-compat-report.json` — 1 deprecated import (`home-dashboard`) | `cat` |
| Profile routing | 15 registered profiles (`hermes profile list` 2026-09-20) | `.hermes.md` |
| Cursor-agent profile | MISSING in `~/AppData/Local/Hermes/profiles/` | `ls` |
| Agent profile | MISSING in profiles/ (workspace agent = `agent` role, not Hermes profile) | `ls` |

## Compatibility Criteria (from clarification — approved)

Compatible = ALL of:
- `platforms` includes `windows` (or empty) for desktop SDK plugins
- `requires_hermes` satisfied (`>=0.19` / `>=0.20`) by this workspace
- Desktop audit status = VALID (not BROKEN; WARN acceptable for fix)
- No broken `.plugin-compat-report.json` deprecated imports (preserved, not suppressed)
- `.env` untouched (size unchanged: 30501 B before/after)

Incompatible = fails any above → disable + uninstall. Broken desktop plugins (4) → disable (preserve audit evidence).

## 5-Step Protocol (LOAD → PLAN → VERIFY → EXECUTE → GATE)

### LOAD (verified)

- [x] `multi-file-change-protocol` loaded
- [x] `user-communication-preferences` loaded
- [x] `writing-spec` loaded
- [x] `subagent-driven-development` loaded
- [x] `plan-mode` loaded
- [x] `systematic-debugging` available (reference only; 4-phase preserved)
- [x] 14-skill stack verified; unavailable members recorded honestly

### PLAN (this file)

- [x] Plan written to `.hermes/plans/plugin-audit-20260923-230937.md`
- [x] Spec written to `$HERMES_HOME/specs/plugin-audit-20260923-230937/SPEC.md`
- [x] Prompt/artifact log written to `$HERMES_HOME/prompts/plugin-audit-20260923-230937/PROMPT.md`

### VERIFY (clarification — completed)

- [x] Q1 (compatibility): approved — all-criteria (Windows + Hermes >=0.20 + SDK + VALID + .env untouched)
- [x] Q2 (status source): approved — combine CLI/config + audit reports + plugin.yaml
- [x] Q3 (script targets): approved — cursor-agent (workspace `.cursor/`) + agent; workspace/scripts/ + $HERMES_HOME/scripts/; bash/MSYS + hermes terminal
- [x] Q4 (multi-file): approved — 14-skill stack, 5-step, GATE verified
- [x] Q5 (categories): approved — categories, not per-plugin; cover all approval/blocker/needed/optional/recommended
- [x] Approval/blocker/needed/optional/recommended covered:
  - APPROVAL: disable 4 broken; uninstall incompatible; enable compatible
  - BLOCKER: 4 broken desktop plugins (BROKEN_NO_DEFAULT_EXPORT: `hermes-rss`, `hermes-toolsmith`, `home-dashboard`, `kanban-gantt`); 23 WARN plugins (unknown SDK import / no ctx register)
  - NEEDED: fix plugin.yaml + plugin.js + document honestly (no suppression of `.plugin-compat-report`)
  - OPTIONAL: script features (list, map, enable, invoke paths), artifact format
  - RECOMMENDED: script outputs (enable + list + status mapping + save artifacts)

### EXECUTE (in progress — sequential for dependent phases)

**Phase E1 — Plugin audit & fix documentation** (sequential)
1. Read full `plugin-catalog.json` and `desktop-plugin-audit.json`
2. Classify each plugin by tier (official/community), category, platform, version
3. Identify 4 BROKEN desktop plugins; 23 WARN desktop plugins; 1 deprecated import (`home-dashboard` in `.plugin-compat-report.json`)
4. Create fix documentation (do NOT suppress deprecated import evidence)
5. Save audit artifacts to `$HERMES_HOME/reports/`

**Phase E2 — Bash script creation & test** (sequential after E1, depends on plugin inventory)
1. Create `scripts/plugin-audit-cursor-agent.sh` in workspace + `$HERMES_HOME/scripts/`
2. Script includes: plugin list, status mapping, enable compatible, disable broken, log output
3. Verify script runs under `bash -euo pipefail` (MSYS) and `hermes terminal -c 'bash ...'`
4. Save output to artifacts

**Phase E3 — Artifact creation** (parallel with E2 if independent)
1. Write `specs/plugin-audit-.../SPEC.md` (requirements + evidence)
2. Write `prompts/plugin-audit-.../PROMPT.md` (clarification log)
3. Write `plans/plugin-audit-.../PLAN.md` (this file — verified)
4. Confirm all gates

### GATE (to verify before "Goal complete")

- [ ] GATE-A: Identity preserved (`SOUL.md` unchanged; `.env` untouched; profile routing intact)
- [ ] GATE-B: Rules enforced (DRY — no duplication; direct/table-first; verification before claim; honest blockers)
- [ ] GATE-C: DRY refs present (plan/spec/prompt reference each other; cross-ref not copy)
- [ ] GATE-D: .env untouched (`.env` MISSING workspace; hermes `.env` 30501 B unchanged — verify post-edit)
- [ ] GATE-E: 0 synthetic artifacts; 0 hidden errors; 4 broken plugins preserved honestly; deprecated import preserved
- [ ] GATE-F: Bash script executable (`bash -n` passes; runs in MSYS + hermes terminal; artifacts saved)
- [ ] GATE-G: File count >5 → multi-file protocol followed (plan/spec/prompt + script + audit + reports = ≥6 files)
- [ ] GATE-H: All 14 skills either loaded or honestly flagged unavailable

## Evidence References (DRY — cross-reference, not duplicate)

- `.hermes/plans/debug-run-logs.md` — 53152 B, 14 real exit codes (verified)
- `.hermes/specs/debug-analysis-2026-09-13.md` — 6081 B (verified)
- `.eslintrc.json` — 69 B (verified)
- `docs/user-guide/` — 14 .md (verified)
- `scripts/` — 51 safe + 5 destructive SKIPPED
- Plugin audit: `~/AppData/Local/Hermes/cache/desktop-plugin-audit.json` (verified 42/15/4/23)
- Plugin catalog: `~/AppData/Local/Hermes/cache/plugin-catalog.json` (verified)
- Plugin compat: `~/AppData/Local/Hermes/.plugin-compat-report.json` (verified — 1 deprecated import)
- `.hermes.md` routing table (verified 15 profiles)
- `USER.md` / `MEMORY.md` identity preserved (verified 2026-09-23)
