---
name: SandBox-root
title: "SandBox-root — Plan"
description: "Plan for SandBox-root generated by repo-plan-spec.py"
version: 1.0.0
status: in_progress
created: 2026-08-01
tags: [plan, repo]
---

# SandBox-root — Plan

## Overview

Canonical agent guidance. First file agents should read. Other instruction files are thin stubs that defer here.

## Stack

- Languages: node, python
- Frameworks/libraries: fastapi, requests
- Entrypoints: none detected

## Repository Notes

- **Shared CI**: `.github/workflows/` at workspace root applies to `projects/Bash/` and any subproject with matching workflows
- **Prompt Library**: All `.github/prompts/*.prompt.md` are the single source of truth — consumed by Copilot, OpenCode, and Hermes agents
- **Hermes Profile Routing**:
- Code/debug/refactor → `code-architect` profile
- Research/synthesis → `research-analyst` profile
- Design/content → `creative-director` profile

## Phases

1. **Inventory** — confirm live config matrix, repo state, and git submodule status on disk.
2. **Research** — per `repo-research-pipeline.prompt.md`: verify RESEARCH_REPORT.md current, links live,
   cross-references symmetric.
3. **Planning** — per `repo-management.prompt.md` outputs: branch normalization
   (`development` + `production`), ignore-file audit, dependency audit, CI workflow.
4. **Tooling** — apply python-quality / tooling-lint / tooling-config stacks (ruff, pyright, eslint,
   prettier, cspell, markdownlint, pre-commit, git-cliff) where applicable. Never overwrite
   customized configs — only create missing.
5. **Check** — run every applicable checker via `tooling_full_check.py`; record findings with
   exit-code classification (exit 2 = TOOLING FAIL blocker).
6. **Fix** — apply safe auto-fixes only (ruff --fix, prettier --write on code globs, F821, broken
   hook imports); flag curated debt as REPORT scope.
7. **Validate** — re-run checkers; write verification summary into the master plan.

## Acceptance

- [x] RESEARCH_REPORT.md current (or created) with live links
- [x] Branch structure: development + production only
- [x] All applicable checkers run without tooling-level failure
- [x] Findings triaged (FIX vs REPORT)
- [x] Verification recorded

## Instructions Standardization — Progress (2026-08-15)

Actions completed:

- Inventory of external instruction files at `C:\Users\Alexa\Desktop\instructions` (189 files).
- Implemented validator and conservative auto-fix tools under `tools/`:
  - `tools/validate_instructions_frontmatter.py`
  - `tools/apply_instruction_fixes.py`
- Backups saved to `artifacts/backup_instructions_20260815` and patch export to `patches/`.
- Conservative auto-fixes applied and artifacts exported to `artifacts/instructions_auto_fixes.zip`.
- Copied validated instruction files into repository under `docs/instructions/`.
- Ran validator and produced `artifacts/validate_run_after_import.json` (summary: 189 total, 189 ok, 0 fail).
- Added `artifacts/redaction_report.md` listing files with <REDACTED_*> placeholders and excerpts for reviewer convenience.

Pending/next steps:

- Human editorial review of placeholder redactions (`<REDACTED_*>`) in files listed in `artifacts/redaction_report.md`.
- Merge branch `chore/instructions-auto-fix` after review and enable CI workflow `.github/workflows/instructions-validate.yml` (mock smoke-tests enabled by default).
- Decide on enabling live LLM smoke-tests (requires credential & governance approval).

Status: in_progress

---

## Recent actions (2026-08-15T17:04:29+01:00)

- Added `artifacts/redaction_report.md` with 19 files and 37 redaction locations.
- Re-ran validation and prepared to produce an updated `artifacts/validate_run_after_review.json` (next step).
- Generated a plan to create a `artifacts/patches_preview.md` for reviewers summarizing `patches/` contents.

## Next automated steps (performed now):

1. Re-run `tools/validate_instructions_frontmatter.py` against `docs/instructions/` and emit JSON report to `artifacts/validate_run_after_review.json`.
2. Create `artifacts/patches_preview.md` with a human-friendly summary of patches in `patches/`.
3. Commit `PLAN.md`, `artifacts/validate_run_after_review.json` (when produced), and `artifacts/patches_preview.md` to `chore/instructions-auto-fix` branch and push.

---
