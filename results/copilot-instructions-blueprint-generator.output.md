# GitHub Copilot Instructions — SandBox Workspace

> **Generated by:** `copilot-instructions-blueprint-generator`
> **Generated on:** 2026-07-09
> **Scope:** Repository root `C:\Users\Alexa\Desktop\SandBox` (a.k.a. `SandBox` monorepo)
> **Method:** Evidence-based. Every statement below is grounded in files actually present in the workspace. No assumptions were invented.
> **⚠️ Staleness alert:** The existing root file `.github/copilot-instructions.md` is **out of date** — it references `Bash/` and `Resume_maker/` at the repo root, but per git history (commits `a1e13174`, `734113eb`) those were migrated to `projects/Bash/` and `projects/Resume_maker/`. This blueprint supersedes it with current paths. Replace the stale file with this content.

---

## Priority Guidelines

When generating code for this repository:

1. **Version Compatibility** — Always detect and respect the exact versions of languages, frameworks, and libraries in use (see *Technology Version Detection*). Never use features beyond the detected version.
2. **Nearest-local first** — Use the nearest `AGENTS.md`, `copilot-instructions.md`, and `.github/instructions/*.instructions.md` to the file being edited. Root guidance is fallback only.
3. **Codebase Patterns** — When local context files don't give specific guidance, scan the codebase for established patterns and follow them.
4. **Architectural Consistency** — Maintain the **multi-phase orchestration / script-toolkit** architectural style (see *Architecture*). Respect boundaries: TypeScript owns logic, shell wrappers stay thin.
5. **Code Quality** — Prioritize **maintainability, security, and testability** in all generated code (dry-run safety and reversibility are first-class concerns here).

---

## Technology Version Detection

Detected from actual files in the workspace (not assumed):

| Layer | Version / Setting | Evidence |
|---|---|---|
| Bun runtime | **1.3.14+** | `bun --version`; `projects/Bash/AGENTS.md` ("Bun 1.3.14+"); `projects/Bash/tech-stack.md` |
| Node.js | **v25.9.0** | `node --version` |
| TypeScript | **strict, `target`/`lib` = ESNext**, `moduleResolution: bundler`, `verbatimModuleSyntax`, `noUncheckedIndexedAccess` | `tsconfig.json` (root) |
| Bash toolkit TS | **stricter still**: `nodenext`, `exactOptionalPropertyTypes`, `isolatedModules`, `noUnusedLocals`, `noUnusedParameters`, `noUncheckedSideEffectImports` | `projects/Bash/tsconfig.json` |
| ESLint | **Flat config** `eslint.config.mts`, `--max-warnings=0` strict mode | `projects/Bash/package.json` (`lint:strict`) |
| Testing | **Vitest** for TS; `tests/verify-dryrun.sh` + `test-all.sh` for shell | `projects/Bash/package.json`, `projects/Bash/tests/` |
| PowerShell | **5.1+** | `projects/Bash/AGENTS.md` |
| Python | **`python3` → 3.14.6**, **`python` → 3.11.14**; `pip` resolves to 3.11 (mismatched — use `python3 -m pip`) | root `copilot-instructions.md`; `requirements.txt` pinned (e.g. `anthropic==0.87.0`, `boto3==1.42.89`) |
| Markdown lint | **markdownlint-cli2** with `.markdownlintrc.json` | `projects/Bash/package.json` |
| Formatting | **Prettier** via `.prettierrc.ts` | `projects/Bash/package.json` |
| EditorConfig | **Tabs, indent 2, CRLF, UTF-8, trim trailing whitespace, final newline** | `.editorconfig` (`root = true`) |

**Rule:** Generate code compatible with the above. Never use APIs unavailable in the detected versions (e.g. do not use TypeScript features beyond ESNext; do not assume a Node API newer than v25).

---

## Workspace Shape (current)

- `projects/Bash/` — **Primary automation toolkit** (Bun/TypeScript + PowerShell + Bash). Operational root.
  - `projects/Bash/src/` — TypeScript implementations (`upgrade.ts`, `cache-clean.ts`, `clean-dep.ts`, `git-commit-batches.ts`).
  - `projects/Bash/src/core/` — shared utilities: `dry-run.ts` (DryRunExecutor), `script-runner.ts`, `ast-transformer.ts`.
  - `projects/Bash/src/migration/` — migration helpers + `ts-morph-helper.ts` code-transform layer.
  - `projects/Bash/*.sh` / `*.ps1` / `*.bat` — thin wrappers delegating to TypeScript.
  - `projects/Bash/docs/` — `CODE_STYLE.md`, `ARCHITECTURE.md`, `tech-stack.md`, exemplars.
  - `projects/Bash/tests/` — `verify-dryrun.sh`, `behavior/`, `integration/`.
- `projects/` — ~15 subproject apps (Next.js, Django, Python, full-stack, etc.), each with its own package manager and local `AGENTS.md`. **Do not assume Bash conventions apply inside a subproject** — defer to its local instructions.
- `projects/Resume_maker/` — standalone Bun/TypeScript resume-PDF generator.
- `.github/` — **shared AI assets** for the whole workspace:
  - `instructions/` — **186** `*.instructions.md` files (apply via `applyTo` glob).
  - `agents/` — **174** `*.agent.md` files.
  - `skills/` — curated skill hub (accelerate, docker-management, stable-diffusion, subagent-driven-development, etc.).
  - `plugins/`, `scripts/`, `workflows/` (incl. `bash-scripts-ci.yml`, `copilot-setup-steps.yml`).
- `prompts/` — 185+ prompt files (template-driven, MIT-licensed frontmatter).
- Root: `AGENTS.md`, `PROJECT_RULES.md`, `SESSION_REPORT.md`, `index.ts`, `package.json`, `tsconfig.json`, `requirements.txt`.

---

## Source of Truth (use before generating)

1. `projects/Bash/package.json` — scripts and task entrypoints.
2. `projects/Bash/README.md` — toolkit overview & script reference.
3. `projects/Bash/docs/CODE_STYLE.md` — naming, logging, wrapper conventions.
4. `projects/Bash/docs/ARCHITECTURE.md` / `tech-stack.md` — architecture & versions.
5. `reports/inventory/refresh-agent-inventory-summary-*.md` — **read before creating new prompts, skills, agents, hooks, or plugins** (conflict check).
6. Nearest subproject `AGENTS.md` for anything inside `projects/<name>/`.

Workspace inventory snapshot (from `.github/copilot-instructions.md`): Instructions 34 / Agents 159 / Prompts 185 / Skills 289 / Hooks 2 / Plugins 21. *Note: live `.github` counts are higher (instructions 186, agents 174) — treat the inventory report as authoritative per-project but verify against `ls .github` when adding assets.*

---

## Context Files (priority order)

Prioritize, in order, if present:
1. `projects/Bash/copilot-instructions.md` (and any subproject-local `copilot-instructions.md`).
2. `projects/Bash/AGENTS.md`.
3. `.github/instructions/*.instructions.md` matched by `applyTo` glob (e.g. `a11y.instructions.md` applies to `**`).
4. `projects/Bash/docs/CODE_STYLE.md`, `ARCHITECTURE.md`, `tech-stack.md`.
5. Root `AGENTS.md` / `PROJECT_RULES.md`.

---

## Codebase Scanning Instructions

When context files don't give specific guidance:
1. Identify files similar to the one being created/edited.
2. Analyze patterns for: **naming conventions, code organization, error handling, logging, documentation style, testing patterns**.
3. Follow the most consistent pattern found.
4. On conflict, prefer newer files or files with higher test coverage.
5. **Never introduce a pattern not already present in the codebase.**

### Observed canonical patterns (evidence-based exemplars)

**TypeScript dry-run / destructive-op pattern** (`projects/Bash/src/core/dry-run.ts`):
- File header block: `#!/usr/bin/env bun` + JSDoc (`Version:`, `Features:`).
- Exported interfaces for options/operations (e.g. `DryRunOptions`, `FileOperation`, `ExecResult`).
- A class (e.g. `DryRunExecutor`) holding state privately; constructor takes an options object.
- Every destructive op supports `dryRun` flag and logging via injectable `logger?`.
- Section dividers as `// ─── Section ───` comments.

**Shell wrapper parity** (`projects/Bash/*.sh` / `*.ps1` / `*.bat`):
- Each logical script ships as **three wrappers** (`.sh`, `.ps1`, `.bat`) with identical behavior.
- Bash: `set -uo pipefail`, color via `RED`/`GREEN`/`NC`, `trap cleanup EXIT`.
- PowerShell: `param()` block, PascalCase vars, try/catch, `$LASTEXITCODE` checks.
- **Never duplicate logic across layers** — logic lives in `src/*.ts`; wrappers only delegate.
- All destructive scripts support `--dry-run` / `-DryRun` and require confirmation unless `--auto` / `-Auto`.

---

## Architecture

- This is a **script-focused monorepo**, not a single compiled app.
- The Bash toolkit favors **dry-run safety, reproducible execution, and parity** across Bash/PowerShell/batch.
- **Multi-phase orchestration**: 6-phase pipeline — Discovery → Clone → Triage → Debug → Remediation → Cross-Reference (per `projects/Bash/AGENTS.md`).
- Logs → `logs/` with timestamped filenames (`action_YYYYMMDD_HHMMSS.log`).
- CI expectations encoded in `.github/workflows/bash-scripts-ci.yml` and `.github/workflows/copilot-setup-steps.yml`.

---

## Code Quality Standards

- Write self-documenting code with clear naming.
- Keep edits **minimal and scoped** to the requested behavior.
- Preserve wrapper behavior across `.sh`, `.ps1`, `.bat`.
- Keep destructive actions behind confirmations or explicit flags (`--dry-run`).
- Keep logs and console output actionable and consistent.
- TypeScript: **strict mode; no `any` without explicit justification** (per `projects/Bash/AGENTS.md`).
- Run `bun run lint:strict` (zero warnings) and `bun run typecheck` before considering work done.

---

## Documentation Requirements (Standard)

- Match the JSDoc/XML style and completeness of existing code (see `dry-run.ts` header pattern).
- Document parameters, returns, and exceptions in the same style as sibling files.
- Follow existing patterns for usage examples and class-level docs.
- For markdown assets (instructions/agents/prompts): use the YAML frontmatter convention observed across `.github/instructions/*.instructions.md` (`description` + `applyTo`) and `.github/agents/*.agent.md` (`name`, `description`, `tools`, `model`).
- Never create `.bak`/`.backup`/`.old`/timestamped backup files — use git for rollback.

---

## Testing Approach

- **TypeScript:** Vitest. Match the structure/style of existing tests under `projects/Bash/src/**/__tests__/` and `projects/Bash/tests/`.
- **Shell:** `tests/verify-dryrun.sh` and `test-all.sh` (run from `projects/Bash/`).
- Single Vitest file example: `bunx vitest run src/migration/__tests__/ts-module-template.test.ts`.
- Prefer behavior + integration suites mirroring the existing `tests/behavior/` and `tests/integration/` layout.

---

## Conventions

### Naming
- Bash scripts: `lowercase-with-hyphens.sh`; vars `UPPER_SNAKE_CASE`; functions `lower_snake_case()`.
- PowerShell scripts: `PascalCase.ps1`; vars `$PascalCase`; functions `Verb-Noun`.
- TypeScript: follow existing file names in `src/`; interfaces in `PascalCase`; exported classes in `PascalCase`.

### Python toolchain
- Use `python3 -m pip` explicitly (do not rely on `pip` → resolves to 3.11 while `python3` is 3.14).
- Per root instructions: avoid inline one-off Python; create scripts under `C:/Users/Alexa/AppData/Local/hermes/scripts/` and run from there. (Re-evaluate against current `PROJECT_RULES.md` before acting.)

### AI asset reuse
- Reuse existing prompts, skills, agents, hooks, plugins **before** adding new ones.
- Check root `.github` assets → subproject-local assets → inventory report for conflicts.
- Hermes hooks live in `~/AppData/Local/hermes/hooks/`; plugins in `~/AppData/Local/hermes/plugins/`.

---

## Version Control Guidelines (Semantic, per detected usage)

- Follow Semantic Versioning patterns as applied in the codebase (toolkit `version: "1.0.0"` in `package.json`).
- Match existing patterns for documenting breaking changes and deprecation notices.
- `git` history shows clean, scoped commits (e.g. `cleanup:`, `fix:`, `feat:`); prefer Conventional Commit style.

---

## Security

- Never commit secrets, tokens, credentials, or local environment files.
- Validate and sanitize external paths and arguments.
- Prefer least-privilege execution.
- Keep cleanup/destructive operations explicit and reversible where possible (dry-run + confirmation).

---

## Hermes Integration (workspace-specific)

- Profiles: `adminbot` (active), `default`, `code-architect`, `creative-director`, `exec-assistant`, `patient-tutor`, `research-analyst`.
- Switch profile: `hermes profile use <name>`. Match profile to task (`code-architect` for code/debug/refactor; `research-analyst` for research; `adminbot` for DevOps).
- Hooks: `session-logger`, `session-auto-commit`, `governance-audit`. Hook scripts must use `jq -c`, `awk` for float comparison, and support SKIP flags.
- Prefer MCP server tools when available (`filesystem`, `github`, `ast-grep`, `memory`, `playwright`, `fetch`, `code-sandbox`, `mcp-docker`, `sequential-thinking`, `cli`).

---

## Session Start

1. Read `SESSION_REPORT.md` at workspace root before proceeding.
2. Prefer the nearest local `AGENTS.md` / `copilot-instructions.md` for the target subtree.
3. Switch to the correct Hermes profile for the task.

---

## Expected Output

A `copilot-instructions.md` (this file) that guides GitHub Copilot to produce code **perfectly compatible with the detected versions** (Bun 1.3.14+, TypeScript strict/ESNext, ESLint flat config, Vitest, PowerShell 5.1+) and **faithful to the established patterns** (dry-run safety, three-wrapper parity, agent/instruction YAML frontmatter, minimal scoped edits) of the SandBox workspace.

## Template References Used (source of truth)

- `projects/Bash/AGENTS.md`, `projects/Bash/docs/CODE_STYLE.md`, `projects/Bash/docs/ARCHITECTURE.md`, `projects/Bash/tech-stack.md`
- `projects/Bash/package.json`, `projects/Bash/tsconfig.json`, root `tsconfig.json`, `.editorconfig`
- `.github/instructions/a11y.instructions.md`, `.github/agents/hermes.agent.md`, `.github/copilot-instructions.md`
- `projects/Bash/src/core/dry-run.ts` (exemplar)
- `requirements.txt`, `git` log (migration history)
