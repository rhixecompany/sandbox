# G2 Evidence — Skills Deduplication (2026-09-11)

**Status: PASS (dedupe) · judge running in background**

## Counts

| Metric                                         | Before | After                                   |
| ---------------------------------------------- | ------ | --------------------------------------- |
| SKILL.md on disk (readable)                    | 1258   | **1139**                                |
| Duplicate clusters (exact-name)                | 110    | **0**                                   |
| Near-dup pairs (≥0.9 similarity)               | 0      | 0                                       |
| `hermes skills list` rows (initial-skills.txt) | 1133   | ← updated-skills.txt pending post-judge |
| Quarantined & deleted (logged)                 | —      | 109 dirs + nested-copy cleanup          |

## Method

1. `hermes skills list` → `initial-skills.txt` (name-truncated table; on-disk frontmatter `name:` used as truth).
2. `scripts/analyze_skill_duplicates.py` — parses frontmatter names (1258 readable), groups exact-name clusters (110), content-similarity near-dups (0).
3. `scripts/dedupe_skills_execute.py` — keeper = category-placed copy (relative depth ≥3) then most lines; merged missing sections into keeper; duplicate dirs moved to `.quarantine-2026-09-11` (recoverable), logged to `results/skills-deduped-2026-09-11.md`.
4. Quarantine deleted after verification.

## Debugging Log (trace before fix)

1. **Pass-1 keeper bug**: keeper key used `len(p.parts)` on ABSOLUTE paths (always ≥4) → flat copies kept, category copies quarantined — reversed policy. Fixed to `len(p.relative_to(SKILLS).parts) >= 3`.
2. **Restore nested-dir pathology**: `shutil.move(dir → existing-dir)` nests instead of replaces → `security/1password/SKILL.md/{references,SKILL.md}` style damage. Fixed: `reconstruct_skills_tree.py` flattens nested `SKILL.md/` dirs via temp-dir shuffle (110 nested dirs flattened, 1 dir moved home: `github`→`software-development/github`).
3. **Analyzer exclusions**: `.quarantine in path.parts` is EQUALITY, not substring → quarantine copies still counted as duplicates. Fixed to `any(".quarantine" in p ...)`.
4. **Pre-existing tree damage (not ours)**: ~110 skill dirs contained nested unreadable `SKILL.md/` duplicate copies (PermissionError on read) — flattened; all 1139 remaining skills readable.

## Anomaly Log

- `honcho-legacy/` and `honcho/` both carried `name: honcho` — mislabeled duplicate pair; substantive copy (honcho-legacy, MCP gateway) kept, thin MCP stub removed.
- `github` flat copy moved into `software-development/github/` (cluster with one side missing post-restore).

## Files

- `scripts/analyze_skill_duplicates.py`, `scripts/dedupe_skills_execute.py`, `scripts/reconstruct_skills_tree.py`, `scripts/restore_dedupe.py`
- `results/skills-dup-analysis-2026-09-11.json`, `results/skills-deduped-2026-09-11.md`
- `initial-skills.txt` (G2 check-in artifact, root)
