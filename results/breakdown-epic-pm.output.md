# Epic PRD — Stale Reference Remediation & Inventory Accuracy

> **Generated by:** `breakdown-epic-pm` prompt (v1.0.0)
> **Date:** 2026-07-09
> **Authoritative workspace source:** `AGENTS.md §13` (known stale references), `docs/agents-cross-reference*.md`, `reports/inventory/`
> **Status:** DRAFT Epic PRD — single source of truth for the downstream technical architecture spec.
> **Skipped reference:** `templates/breakdown-epic-pm/output_format.md` and `phases.md` were referenced by the prompt but do **not** exist in the workspace. Structure below follows the standard Epic PRD contract described inline in the prompt.

---

## 1. Epic Name

**Stale Reference Remediation & Inventory Accuracy (SRRIA)**

A focused epic to eliminate the 6 categories of known-stale pointers in the SandBox workspace, make inventory counts a live (not hardcoded) source of truth, and prevent regression through CI gates.

---

## 2. Epic Idea (from workspace evidence)

`AGENTS.md §13` explicitly lists 6 stale references discovered 2026-07-09 that are currently wrong in the repo:

1. `.github/copilot-instructions.md` references a root `Bash/` toolkit that does not exist (toolkit is at `projects/Bash/`). 13 wrong references.
2. `.github/workflows/bash-scripts-ci.yml` path filter is `Bash/**` but toolkit lives at `projects/Bash/**` — CI may not trigger.
3. Root `AGENTS.md` (prior version) pointed to a nonexistent `SOUL.md`.
4. `README.md` references a nonexistent `PROJECT_RULES.md`.
5. Prompt boilerplate says "always use pnpm" — wrong; toolchain is bun.
6. Inventory counts drift — `copilot-instructions.md` cites a 2026-05-30 snapshot; live source is `reports/inventory/` refresh summary.

The epic converts these from "documentation-only known issues" into fixed, verified, and regression-guarded state.

---

## 3. Target Users

| User | Need |
|---|---|
| Contributors editing `.github/` assets | Correct paths so their edits land in the right place and CI actually runs |
| Copilot / Codex / Hermes agents operating here | Accurate `AGENTS.md`, `copilot-instructions.md`, `README.md` so they don't follow dead pointers |
| CI / release engineers | `bash-scripts-ci.yml` triggers on real toolkit path |
| Workspace owners | Live, trustworthy inventory counts instead of hardcoded numbers |

---

## 4. Goals

- **G1** — All 6 stale-reference categories resolved and verified against real files.
- **G2** — Inventory counts become derived from `reports/inventory/` at authoring time, never hardcoded.
- **G3** — CI guards prevent re-introduction of the `Bash/` vs `projects/Bash/` path mismatch.
- **G4** — Documentation that agents read first (`AGENTS.md`, `copilot-instructions.md`, `README.md`) is internally consistent.

## 5. Non-Goals (Out of Scope)

- Rewriting the automation toolkit logic itself (`projects/Bash/src`).
- Adding new agents/instructions (only fixing existing references).
- Migrating file layout (e.g., actually moving `projects/Bash/` to `Bash/`).

---

## 6. Success Metrics

| Metric | Baseline | Target |
|---|---|---|
| Stale-reference categories open | 6 | 0 |
| `Bash/` vs `projects/Bash/` path mismatches in `copilot-instructions.md` | 13 | 0 |
| CI workflow path-filter correctness | mismatch | matches `projects/Bash/**` |
| Inventory count source | hardcoded 2026-05-30 snapshot | live `reports/inventory/` reference |
| Dead internal links (`SOUL.md`, `PROJECT_RULES.md`) in root docs | 2 | 0 |

---

## 7. Decomposed Stories

### Theme A — Path & Toolchain Reference Fixes

**A1. Fix `copilot-instructions.md` toolkit path (13 refs)**

- *As a* contributor, *I want* `copilot-instructions.md` to point at `projects/Bash/` (not `Bash/`), *so that* I can find the real toolkit.
- Acceptance: 0 occurrences of `Bash/src`, `Bash/docs`, `Bash/tests`, `Bash/package.json`; all map to `projects/Bash/...`.

**A2. Correct CI workflow path filter**

- *As a* CI engineer, *I want* `bash-scripts-ci.yml` to watch `projects/Bash/**`, *so that* toolkit changes actually trigger the pipeline.
- Acceptance: workflow YAML path filter matches `projects/Bash/**`; a test commit to `projects/Bash/` triggers the job.

**A3. Replace "always use pnpm" boilerplate with bun**

- *As an* agent, *I want* prompts to say bun (per `packageManager: bun@1.3.14`), *so that* I don't run wrong package-manager commands.
- Acceptance: no prompt file asserts pnpm as the workspace standard; bun referenced where package manager is named.

### Theme B — Dead Link Remediation

**B1. Resolve `SOUL.md` reference**

- *As an* agent reading root `AGENTS.md`, *I want* no pointer to a missing `SOUL.md`, *so that* I don't waste a lookup.
- Acceptance: root `AGENTS.md` no longer references a nonexistent `SOUL.md`; principles are consolidated in `AGENTS.md`.

**B2. Resolve `PROJECT_RULES.md` reference in README**

- *As a* human reader, *I want* `README.md` to point at real rules, *so that* links work.
- Acceptance: `README.md` references `AGENTS.md` / `.github/instructions/` instead of `PROJECT_RULES.md`.

### Theme C — Live Inventory Accuracy

**C1. Make inventory counts live, not hardcoded**

- *As a* workspace owner, *I want* counts sourced from `reports/inventory/` refresh summary, *so that* they don't drift.
- Acceptance: `copilot-instructions.md` cites the latest `refresh-agent-inventory-summary-*.md` date and instructs readers to treat `reports/inventory/` as live source; no hardcoded stale snapshot.

**C2. Refresh inventory post-fix**

- *As a* contributor, *I want* the inventory regenerated after edits, *so that* counts are current.
- Acceptance: latest `refresh-agent-inventory-summary-*.md` updated; cross-reference docs in `docs/` reconciled.

### Theme D — Regression Guard

**D1. Add a stale-reference link/consistency check to CI**

- *As a* workspace owner, *I want* CI to fail on dead internal links or the `Bash/` vs `projects/Bash/` mismatch, *so that* these don't return.
- Acceptance: a workflow step (extend `validate-readme.yml` or `bash-scripts-ci.yml`) detects missing referenced files and the path mismatch; fails the build on violation.

---

## 8. Dependencies & Constraints

- **Deps:** `.github/scripts/` audit tooling (`inventory-agents.ps1`, `update_agents_md.py`) for C2; existing CI workflows for D1.
- **Constraints:** follow bun (not pnpm); CRLF line endings (`.editorconfig`); no backup/`.bak` files (use git); keep frontmatter conventions for any touched prompt files.
- **Risk:** editing `copilot-instructions.md`/`AGENTS.md` changes behavior for all Copilot users — changes must be deliberate and verified.

---

## 9. Milestones (suggested)

| Milestone | Stories | Exit criterion |
|---|---|---|
| M1 Path fixes | A1, A2, A3 | Toolkit references correct; CI triggers on `projects/Bash/**` |
| M2 Link fixes | B1, B2 | No dead internal links in root docs |
| M3 Inventory | C1, C2 | Counts live; summary refreshed |
| M4 Guard | D1 | CI blocks re-introduction |

---

## 10. Handoff to Architecture Spec

This Epic PRD is the input for a technical architecture specification covering: the CI workflow patch design (D1), the inventory-generation pipeline (C1/C2), and the doc-editing runbook (A/B). Recommend the architecture spec enumerate exact file paths, the specific YAML diff for `bash-scripts-ci.yml`, and the link-checker tool choice.

---
*Verification note: Structure derived from the `breakdown-epic-pm` prompt's inline Epic PRD contract. Epic scope and all 6 stale-reference categories are traced to `AGENTS.md §13` (verified in-workspace). Template file `templates/breakdown-epic-pm/output_format.md` was not present and its guidance was skipped; this PRD follows the prompt's described format instead.*
